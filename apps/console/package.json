{
  "name": "pdpp-console",
  "version": "1.0.0",
  "private": true,
  "//dev": "TEMPORARY: --webpack works around a Turbopack dev-server async-hook leak in Next 16.2.x (RangeError: Map maximum size exceeded). Remove the flag once vercel/next.js#91396 is fixed.",
  "//build": "TEMPORARY: --webpack keeps local/CI builds off Turbopack after repeated native SIGSEGV during static page generation on Next 16.2.x.",
  "scripts": {
    "//dev-node-options": "NODE_OPTIONS adds Node.js diagnostic reports on fatal V8 errors and uncaught exceptions. Reports land in tmp/node-reports/ at the repo root (gitignored), excluding env/network sections so reports do not persist secrets.",
    "dev": "sh -c 'mkdir -p ../../tmp/node-reports && WEB_PORT_ARGS=${PDPP_WEB_PORT:+--port $PDPP_WEB_PORT} && NODE_OPTIONS=\"--report-on-fatalerror --report-uncaught-exception --report-exclude-env --report-exclude-network --report-directory=$(cd ../../tmp/node-reports && pwd)\" next dev --webpack --hostname 0.0.0.0 $WEB_PORT_ARGS'",
    "build": "next build --webpack",
    "start": "next start",
    "types:check": "next typegen && tsc --noEmit",
    "test:view-models": "TSX_TSCONFIG_PATH=tsconfig.json node --test --import tsx \"**/*.test.ts\" \"**/*.test.mjs\"",
    "//test-stream": "The accounting authority owns console discovery and execution, including the stream route's bracketed path segments.",
    "test:stream": "TSX_TSCONFIG_PATH=tsconfig.json node --test --import tsx \"**/*.test.ts\" \"**/*.test.mjs\"",
    "check": "ultracite check && pnpm --dir ../.. generated-artifacts:check",
    "fix": "ultracite fix",
    "verify": "pnpm types:check && pnpm test:view-models && pnpm test:stream && pnpm check && pnpm build"
  },
  "dependencies": {
    "@base-ui/react": "^1.6.0",
    "@demodesk/neko": "1.6.32",
    "@opendatalabs/remote-surface": "^1.5.1",
    "@pdpp/brand": "workspace:*",
    "@pdpp/brand-react": "workspace:*",
    "@pdpp/operator-ui": "workspace:*",
    "@tailwindcss/postcss": "^4.3.3",
    "@xterm/addon-fit": "^0.11.0",
    "@xterm/addon-web-links": "^0.12.0",
    "@xterm/xterm": "^5.5.0",
    "class-variance-authority": "^0.7.1",
    "clsx": "^2.1.1",
    "fumadocs-core": "^16.13.0",
    "fumadocs-mdx": "^14.2.11",
    "fumadocs-ui": "^16.13.0",
    "imapflow": "^1.6.3",
    "lucide-react": "^1.27.0",
    "next": "16.3.0-canary.93",
    "pdpp-reference-implementation": "workspace:*",
    "postcss": "^8.5.24",
    "react": "^19.2.8",
    "react-dom": "^19.2.8",
    "react-markdown": "^10.1.0",
    "react-xtermjs": "^1.0.12",
    "remark-gfm": "^4.0.1",
    "server-only": "^0.0.1",
    "shadcn": "^4.16.0",
    "tailwind-merge": "^3.6.0",
    "tailwindcss": "^4.3.3",
    "tw-animate-css": "^1.4.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/mdx": "^2.0.14",
    "@types/node": "^26.1.2",
    "@types/react": "^19.2.17",
    "@types/react-dom": "^19.2.3",
    "typescript": "^7.0.2"
  }
}
