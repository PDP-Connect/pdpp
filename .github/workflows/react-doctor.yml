name: react-doctor

# Regression-only React health check on the Next.js apps/site surface.
#
# Why regression-only: the existing app has an established baseline of
# React Doctor diagnostics. We do not gate on an absolute floor; we only fail
# when a PR introduces *new* findings against main. Owner can promote to a
# score floor later once the baseline is paid down.
#
# apps/console (the operator dashboard) was removed by Move B — the
# reference implementation and its React surface now live canonically in
# PDP-Connect/data-connect. Only apps/site remains in this repo.

on:
  pull_request:
    paths:
      - "apps/site/**"
      - ".github/workflows/react-doctor.yml"
  workflow_dispatch: {}

concurrency:
  group: react-doctor-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read
  pull-requests: read
  issues: write

jobs:
  react-doctor:
    name: regression scan vs main
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0

      - name: Run React Doctor (changed PR files)
        uses: millionco/react-doctor@013f7373f91a3b9e68bd1dc7d4d354f4b041b117 # v2.2.9
        with:
          scope: changed
          blocking: warning
          comment: false
          review-comments: false
          commit-status: false
