{
  "name": "@pdpp/cli",
  "version": "0.0.0",
  "description": "Command-line tools for PDPP providers.",
  "type": "module",
  "bin": {
    "pdpp": "./dist/bin/pdpp.js"
  },
  "exports": {
    ".": {
      "types": "./dist/src/index.d.ts",
      "import": "./dist/src/index.js"
    },
    "./cache-layout": {
      "types": "./dist/src/cache-layout.d.ts",
      "import": "./dist/src/cache-layout.js"
    },
    "./package-info": {
      "types": "./dist/src/package-info.d.ts",
      "import": "./dist/src/package-info.js"
    }
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "scripts": {
    "build": "rm -rf dist && tsc -p tsconfig.build.json && node scripts/postbuild.mjs",
    "prepare": "pnpm build",
    "prepack": "pnpm build",
    "test": "node --test --import tsx \"test/*.test.js\"",
    "validate:package": "pnpm build && node scripts/validate-package.mjs",
    "pack-install-run": "pnpm build && node scripts/pack-install-run.mjs",
    "pack-install-run:node-22.14": "node scripts/verify-node-22-14-artifact.mjs",
    "verify": "pnpm test && pnpm validate:package && pnpm pack-install-run",
    "pack:dry-run": "pnpm build && npm pack --dry-run --ignore-scripts"
  },
  "keywords": [
    "pdpp",
    "personal-data",
    "oauth",
    "cli"
  ],
  "license": "Apache-2.0",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/PDP-Connect/pdpp.git",
    "directory": "packages/cli"
  },
  "bugs": {
    "url": "https://github.com/PDP-Connect/pdpp/issues"
  },
  "homepage": "https://github.com/PDP-Connect/pdpp#readme",
  "engines": {
    "node": ">=22.14.0"
  },
  "publishConfig": {
    "access": "public",
    "provenance": false,
    "registry": "https://registry.npmjs.org/",
    "tag": "latest"
  }
}
