{
  "lib/postgres-spine.ts": {
    "allowed_mass": 132,
    "date": "2026-07-20",
    "reason": "mergeEventRows fix for a silent data-loss bug (distinct rows sharing a null/duplicate event_seq collapsed onto the same dedup key). The fix needs a two-key (event_seq, event_id) comparator, which Biome's cognitive-complexity scoring counts as added mass even when extracted into its own named function (compareEventRowOrder) — tried extraction first; it did not reduce the file-level score. Correctness over a lower number here."
  },
  "lib/spine.ts": {
    "allowed_mass": 110,
    "date": "2026-07-30",
    "reason": "loadEventsForSummaries batches the summary-hydration N+1 (one query per correlation id -> one query for the whole page) via a ROW_NUMBER()-partitioned fetch plus a per-id grouping pass (building the id->rows Map, iterating placeholders, hydrating rows). The 2026-07-30 connector-summary change adds the same bounded partitioned aggregation one layer earlier for every connector id in an authorized page, then returns an id-keyed map to preserve the existing connection matcher and singleton fallback. The explicit empty/backend branches and per-row fail-closed hydration are load-bearing backend parity, not incidental extraction candidates; the SQLite 50-to-200 slope and no-write oracle plus PostgreSQL authority cover them. Authority Slice A (2026-07-30, generalize-run-history-write-authority): emitSpineEvent's SQLite path gains one dirty-independent branch (isRunHistoryRelevantEventType) that hooks the kind-neutral run_history writer for run.started/terminal events, plus toRunHistorySpineEvent, a small pure mapping function from the already-normalized spine event into the writer's input shape. This is the single seam every run kind (scheduled/manual/browser/cancelled) already funnels through; adding the hook here, rather than at each of the ~15 individual call sites across runtime/index.ts and runtime/scheduler.ts, is the whole point of the design (one seam, not N call-site edits). Proven load-bearing by test/run-history-writer-authority.test.ts (6 tests: started->finalized transition per run kind, idempotency under retried started/terminal events, terminal-only fallback insert)."
  },
  "runtime/browser-surface/allocator-observation.ts": {
    "allowed_mass": 5,
    "date": "2026-07-30",
    "reason": "Bounded runtime-observation seam (2026-07-30): known-id reads must distinguish an empty input (no allocator call), unavailable allocator (every requested id unknown), a missing surface, and an unreadable per-id status without falling back to the global listSurfaces diagnostic. The cap and explicit outcome branches are the contract that prevents unbounded background work and loss of missing-versus-unknown evidence; extracting them would split one small observation projection without reducing the required state distinctions. Focused 0/1/25 tests assert bounded calls, global-call exclusion, explicit outcomes, and filtered-global parity."
  },
  "runtime/browser-surface-lease-sweep-timer.ts": {
    "allowed_mass": 1,
    "date": "2026-08-02",
    "reason": "repair-terminal-evidence-residual: the opt-in immediate-sweep branch is the timer activation contract for connector-summary maintenance. It reuses the existing runTick error boundary and keeps the ordinary interval intact; removing the branch reintroduces the first-interval stale-evidence gap, while extracting it would relocate the only decision without reducing mass. Proven by the timer regression that asserts one immediate pass, one interval, and no repeat on a second start."
  },
  "runtime/browser-surface/run-coordinator.ts": {
    "allowed_mass": 106,
    "date": "2026-07-31",
    "reason": "2026-07-31 (orphan-capacity reconcile, live evidence: pdpp-neko-amazon-dd6ffc79c80ddfd3/bs_4f3d919a and pdpp-neko-amazon-83b5b114cb3b7f72/bs_1ec177c0 stayed running+Docker-healthy and capacity-consuming after every sweep tick while their durable browser_surfaces rows sat at health=unhealthy, no lease, last_used_at ~23:51). Root cause: reconcileSurfacesWithAllocator's own eviction branch (live.health === unhealthy -> invalidateSurface) removes a wedged surface from the lease manager's in-memory map and persists it unhealthy, but invalidateSurface never calls allocator.stopSurface -- and cleanupIdleSurfaces only ever considers surfaces still present in the manager's map at health ready. Once a surface reaches persisted unhealthy with no lease, nothing in this file or in @opendatalabs/remote-surface ever calls stopSurface for it again, so its allocator container and host-port capacity slot leak forever -- distinct from, and does not touch, the reconcileSurfacesWithAllocator/cleanupIdleSurfaces upstream-fix path from the prior same-day stale-capacity incident. Fix: retireOrphanedUnhealthyBrowserSurfaces, composed into the existing periodic sweep after cleanupIdleBrowserSurfaces, reads the durable store directly (the manager has already forgotten these rows) for health=unhealthy + no active lease + not retained + aged past idleTtlMs, then retireOrphanedUnhealthyBrowserSurface issues one bounded allocator.stopSurface(reason: reconcile) per candidate after re-checking the manager still agrees no non-terminal lease owns it. A live deployment can accumulate hundreds of historical unhealthy/no-lease rows (see MAX_ORPHAN_SURFACE_RETIREMENTS_PER_SWEEP), so the eligible set is sorted deterministically (oldest last_used_at first, surface_id tiebreaker) and sliced to a small per-tick batch rather than firing an unbounded burst of allocator calls every sweep interval; each retired row leaves the eligibility set (health -> stopping) whether stopSurface succeeds or reports the container already gone, so the batch converges across a few ticks with no separate retry bookkeeping, and one candidate's transient stopSurface failure leaves only that row untouched (naturally retried next tick) without blocking the rest of the batch. Net mass increase (83->92, +9, re-derived via measure-mass.ts --files): two new functions (retireOrphanedUnhealthyBrowserSurfaces, the per-item retireOrphanedUnhealthyBrowserSurface) plus one new injectable clock dep field (nowMs) threaded through the factory destructure and wired at the one call site in sweepBrowserSurfaceLeases, after cleanupIdleBrowserSurfaces. All prior mass (idle cleanup, boot reconcile, expiry/reclaim) is untouched -- confirmed via git diff showing zero changes to cleanupIdleBrowserSurfaces or reconcileBrowserSurfacesWithAllocatorAtBoot themselves. || 2026-07-31 (bounded mid-run phase-scoped surface acquire/release): adds the phase lifecycle a `surfaceScope: \"phase\"` connector needs so it stops reserving a scarce managed surface for its whole run (Slack: ~1h of archive/backfill to cover four quick gap streams). Net 92->106 (+14, re-derived via measure-mass.ts --files) across four small functions, each individually UNDER Biome's cognitive-complexity threshold (biome lint --only=complexity/noExcessiveCognitiveComplexity reports zero diagnostics for this file): (1) acquireManagedBrowserSurfaceForPhase, whose branches are the contract itself -- not-managed short-circuit, idempotent-reuse arm, and the `waiting_for_browser_surface` interception that cancels the just-queued wait so a phase request NEVER blocks its run on capacity; (2) releaseManagedBrowserSurfaceForPhase, whose branches are the ownership fence (delete-record-before-release so a duplicate/concurrent call cannot double-release, and release under the RECORDED fencing token so a stale call cannot free a newer grant); (3) grantFromPhaseLease, whose guard exists only to satisfy noUncheckedIndexedAccess without a non-null assertion; (4) phaseUnavailableReasonForEarlyReturn, a 2-branch status mapping. Reduction was attempted and measured, not assumed: extracting the nested idempotent-reuse block into `reusablePhaseGrant` lowered the file 111->106 and was kept. Further extraction was rejected as clean-code theater -- it would split one lifecycle contract across more shallow helpers without removing a single state distinction, and every remaining branch is a correctness guard (no double-release, no leak, no unbounded wait, no capacity bypass) rather than incidental structure. Also fixes a proven defect in the same file: reconcileBrowserSurfaceLeasesAfterBoot built activeRunIds from persisted run_ids only, and reconcileAfterRestart (@opendatalabs/remote-surface surface-lease-manager.js:553-560) releases any leased lease whose run_id is absent from that set -- so a controller restart mid-phase silently released a surface still in use; the derived phase session id is now included per active run."
  },
  "runtime/classify-runtime-failure.ts": {
    "allowed_mass": 5,
    "date": "2026-07-31",
    "reason": "fix-runtime-gate-state-commit-on-unexplained-rs-ingest-rejections (2026-07-31): classifyRuntimeFailure's single exported function is a flat if/else-if chain mapping a thrown Error's message to a stable failure_reason string -- every existing reason (interaction_handler_invalid_response, connector_protocol_violation) is already a single large boolean condition inside this same function, and the new ingest_rejected branch (`message.startsWith(\"Resource server rejected \") && message.includes(\" ingested record(s) across stream(s):\")`) reuses that identical shape rather than introducing a new dispatch mechanism. Splitting the new branch into a helper would not lower complexity (the caller would still need one more branch to route to it) and would separate one classification rule from the fifteen others it must stay next to for a reader auditing 'what failure_reason values exist and how are they detected'. Mass increase (3->5, i.e. complexity 8->10) is exactly the cost of the two added boolean predicates (startsWith + includes) ANDed into the existing chain; no other line in the function changed. Proven by test/collection-profile.test.ts's new end-to-end regression: a real RS-side ingest rejection (identity mismatch) throws the exact message this branch matches, and the test asserts the run fails with failure_reason ingest_rejected rather than falling through to runtime_error."
  },
  "runtime/connection-health.ts": {
    "allowed_mass": 83,
    "date": "2026-07-31",
    "reason": "fix-credential-continuity-honest-degrade (2026-07-31): `credentialContinuityCondition`'s own doc comment is explicit that process-bound continuity is 'a diagnostic health overlay, never a repair authority', yet `isDegradingCondition` had no exclusion for `CredentialContinuity` (unlike the existing `CredentialsValid` exclusion at the line above), so a warning-severity unproven-continuity condition alone forced a fresh, fully-collected ChatGPT connection into `degraded` -- and `pickDominantConditionId`'s `degraded` case never lists `CredentialContinuity`, so the resulting pill had no dominant condition or reason code, an unlabeled degraded state masking real connector health rather than honestly naming missing instrumentation. The added branch is the same shape as the existing `CredentialsValid` exclusion two lines above it. Proven by test/connection-health-acceptance.test.ts's new regression and test/connection-health.test.ts's corrected 'current replacement continuity overlays allocator availability' acceptance (both assert `healthy`, not `degraded`, for unproven continuity alone)."
  },
  "runtime/controller.ts": {
    "allowed_mass": 251,
    "date": "2026-07-28",
    "reason": "TypeScript migration closure (2026-07-28): runtime/controller.ts strict-plus type fixes add two conditional branches — one to handle ConnectorDetailGapReadStore unknown cast (line 2140) and one for the exactOptionalPropertyTypes baseUrl spread (line 2388). Both are honest narrowing guards with no behavior change; the alternative is a cast that would violate the strict-plus baseline."
  },
  "runtime/detail-gap-paging.ts": {
    "allowed_mass": 24,
    "date": "2026-07-25",
    "reason": "restore-detail-gap-lease-guard: createDetailGapPageReader silently skipped CAS recovery leasing when the injected detailGapStore did not implement claimPendingGaps, letting a stale page serve a row another run already owned. The added guard fails closed with a clear error before any lease bookkeeping starts, rather than continuing to serve unleased gaps. Proven load-bearing by an oracle (detail-gap page reader fails clearly when CAS leasing is unavailable, detail-gap-page-request-oracle.test.js) that fails deterministically without the guard."
  },
  "runtime/index.ts": {
    "allowed_mass": 464,
    "date": "2026-08-01",
    "reason": "fix-runtime-gate-state-commit-on-unexplained-rs-ingest-rejections (2026-07-31): the RS's rs.records.ingest operation already reports records_accepted/records_rejected honestly per batch, but nothing previously stopped a run whose ingest batches were partially or fully rejected from committing its STATE checkpoint and reporting success -- a batch RS rejects in full (records_accepted: 0) could vanish silently while run and connection health both stayed green (root-caused during fleet-green Gmail work: a live duplicate/backfill Gmail connection had a delta batch rejected 82/82 by RS ingest with no run failure and no known gap). Three branches close this, each reusing an existing primitive rather than inventing a new one: (1) `recordIngestRejections` (new function, mass 8) is a per-stream tally gated by the same early-return-on-non-positive shape used throughout this file, and reuses `boundGapStringList` -- the existing bounded-evidence primitive already used for other terminal known_gap diagnostics -- to cap the per-record error sample instead of unbounded accumulation; (2) `buildKnownGapsForTerminal`'s added loop (mass +2) pushes one `buildKnownGap` entry per rejected stream, mirroring the adjacent `assertDetailCoverageSatisfiedBeforeCommit`/detail-coverage known_gap it sits next to line-for-line; (3) `flushBatch`'s added branch (mass +7) calls `recordIngestRejections` when `result.records_rejected > 0` and makes `run.batch_ingested`'s status reflect `records_rejected` instead of the previous hardcoded `succeeded`, in the same per-batch spine-event call that already existed. `assertNoUnexplainedIngestRejectionsBeforeCommit` (called from the existing STATE-commit gate alongside `assertDetailCoverageSatisfiedBeforeCommit`) is the actual enforcement point -- moving the `if (result.records_rejected > 0)` guard inside `recordIngestRejections` was attempted and measured: it does not lower total mass (the guard's cognitive cost simply relocates and the function count grows), so the caller-side guard was kept. Proven end-to-end by test/collection-profile.test.ts's new regression, which reproduces a real RS-side rejection (identity mismatch -> invalid_record_identity) and asserts the run fails, STATE is not committed, and the terminal known_gaps/timeline carry the ingest_rejected evidence. || 2026-07-31 (bounded mid-run phase-scoped browser surface): adds handleBrowserSurfaceRequest, the controller-side half of the new BROWSER_SURFACE_REQUEST/RESPONSE round-trip that lets a `surfaceScope: \"phase\"` connector borrow a managed surface for a bounded phase instead of reserving one for its whole run. Net 423->431 (+8, re-derived via measure-mass.ts --files) for ONE handler registered in the existing protocolHandlers table, alongside handleDetailGapsPageRequest which it deliberately mirrors. The branches are the handler's four required outcomes, not incidental structure: unrecognized-action/unwired-host -> `unavailable`/`not_managed`; `release` -> release hook then `released`; `acquire` granted -> the surface fields; `acquire` refused -> `unavailable` with the typed reason. Unlike DETAIL_GAPS_PAGE_REQUEST this handler is deliberately non-throwing on a malformed message: a protocol hiccup must degrade to the connector's honest per-stream skip rather than failing the whole run. Reduction was attempted and measured, not assumed: collapsing the four near-identical writeChildStdin reply blocks into one local `reply()` helper (which also folds the duplicated not_managed guard into a single condition) lowered the file 432->431 and was kept. Extracting the remaining outcomes into separate top-level functions was rejected -- it would scatter one protocol contract across several shallow helpers, each needing the same requestId/hook closure, without removing a single state distinction. || uat-cancel-race-0801 (2026-08-01, +1, re-derived via measure-mass.ts --files; note: file mass had already drifted to 438 against this entry's 431 cap from unrelated prior commits on this branch (95c71a4cf, 62bc8ccae) that did not update this justification -- that 7-point gap predates this change and is flagged here for separate cleanup, not claimed by it): fixes a live owner-cancellation race where a browser-driven connector's own SIGTERM-teardown reject (e.g. Playwright's \"Target closed\") reports a legitimate DONE(failed) that raced the owner's cancel_requested. proc.on(\"close\")'s dispatch gains one reordered branch -- ownerCancelRequested is now checked ahead of doneMessage (mirroring the file's existing runTimedOut precedence over doneMessage), routing to the owner-cancellation close path unless the raced DONE was \"succeeded\" (a connector that finishes before teardown interrupts it is still honored as a success) -- plus a doneMessage=null reset on that path so the existing downstream connector_error/resolution-reason reads don't leak the teardown symptom into a cancelled result. Reduction was attempted: folding the new condition into the existing doneMessage branch (checking ownerCancelRequested inside handleDoneClose instead) was measured and did not lower mass, and would have required duplicating handleOwnerCancellationClose's emit-and-resolve shape inside handleDoneClose instead of reusing it. Proven by a regression stub in test/runtime-cancel-run.test.ts that reports DONE(failed) from its own SIGTERM handler -- fails on pre-fix code (status=failed instead of cancelled) and passes after. || gmail-too-large-settlement-0802 (2026-08-02, allowed_mass 439->464): the runtime-side terminal DETAIL_GAP contract adds independent fail-closed validation for exact terminal reason/class, served identity, locator shape, and bounded evidence; a record-flush-before-CAS fence; the current-lease lookup and terminal CAS postcondition; and terminal event evidence recording. These branches preserve the distinction between accepted policy coverage, provider attempts, transient pending work, and stale leases. Collapsing them would weaken an ownership or evidence fence or move the protocol contract into shallow wrappers. Proven by the four-row starvation/recovery test, stale-lease CAS test, and exact-scope repair tests."
  },
  "runtime/rendered-verdict.ts": {
    "allowed_mass": 77,
    "date": "2026-07-31",
    "reason": "fix-phantom-owner-interaction-cta (2026-07-31): schedule-fallback attention evidence synthesized purely from a human_attention_needed boolean (no durable connector_attention_records row, no real id/action_target/owner_action) was still producing a dead 'Complete the requested action' provider_interaction CTA and 'Collecting in the background.' progress copy even with no active run — reproduced on connection cin_11deac1e728b244aaeb56765 after an explicit chatgpt_session_required terminal failure that never routes through attention-writer.ts (which only reacts to INTERACTION/ASSISTANCE wire messages, not terminal DONE/connector_error failures). Two branches close the gap: hasActionableAttention gates the add_info/provider_interaction CTA on real identity (id/actionTarget/ownerAction present) so bare fallback evidence degrades tone/pill only, never a dead button; and progressHeadline's terminal-disposition branch now also covers a non-terminal open reauth action while nothing is syncing (credential failure and coverage-driven disposition are derived independently, so a reauth action can be open on any disposition). Both are load-bearing honesty guards, not incidental structure — removing either restores the phantom CTA or the false 'collecting' claim. Proven by test/rendered-verdict.test.ts's phantom-provider_interaction-CTA regressions (suppressed for schedule-fallback evidence, preserved for real structured attention)."
  },
  "runtime/scheduler.ts": {
    "allowed_mass": 50,
    "date": "2026-08-01",
    "reason": "notification-narrow-0801: closes the UAT gate's P1 finding that a scheduled interaction's pending-interaction push and the next tick's needs-human escalation push (gateNeedsHuman, pre-run-gate.ts) both fire for the same transition -- a connector run becoming human-required -- because isNeedsHuman treats an active/pending-interaction run as needs-human even when the owner was already notified. markNeedsHumanDedupeFromInteraction (exported, pure, unit-tested directly) reuses the EXISTING notifiedNeedsHumanSkips Set gateNeedsHuman already consults, keyed by the same connector_instance_id withSchedulerInteractionContext (run-executor.ts) already stamps onto every interaction; no new storage, Set, or cross-module plumbing was added. The wrapper (onInteractionWithNeedsHumanDedupe) is the minimal shape that can intercept onInteraction before it reaches createRunExecutor while keeping runtime.notifiedNeedsHumanSkips in scope (it is not accessible from inside run-executor.ts's narrower RunExecutorRuntimeState type). Mass increase is the one added branch (structural guard on the interaction shape) plus the new exported function boundary; reduction was not attempted beyond extracting the pure predicate, since inlining it back into the wrapper would not lower complexity, only remove the direct-unit-test seam. Proven by test/scheduler-push-escalation-l8.test.ts's two new markNeedsHumanDedupeFromInteraction unit tests (pre-arms the real key; no-op for a malformed/missing-id interaction) plus the existing unrelated-connector-instance escalation regression, which still passes unchanged."
  },
  "runtime/scheduler/run-executor.ts": {
    "allowed_mass": 81,
    "date": "2026-08-02",
    "reason": "scheduler-lifecycle-residual-0802: the managed controller bridge must distinguish a typed run_browser_surface_queued collision from a real controller invocation failure. The added branch is the lifecycle contract: it converts the controller-owned busy result into the existing skipped surface-unavailable path, while the adjacent controller-failure builder records only the stable controller_run_now_failed cause. REVISE (2026-08-02, 78->81): the controller boundary can catch literally unknown JS throw values, including null-prototype objects and hostile Proxies, so controllerRunNowErrorCode and controllerRunNowLegacyMessage separate safely-readable lifecycle evidence from mapping policy. Their guards are load-bearing: typed code is read under try/catch; legacy matching is limited to actual strings or Error messages; every other value reaches the stable redacted controller failure without String() coercion. Folding these guards back into the policy mapper exceeds the per-function cognitive-complexity limit; eliminating either would reintroduce either unsafe property access or unsafe coercion. Proven by test/scheduler-managed-surface-routing.test.ts T3d/T3e/T6/T6b/T7 and mutation checks: deleting the typed branch changes T3d from skipped to failed, while reintroducing String(err) prevents the hostile-value regression from receiving a terminal record."
  },
  "server/auth-middleware.ts": {
    "allowed_mass": 18,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): request and response boundaries retain the existing auth-gate route classification, inactive-token telemetry, and owner/client token-kind checks. The added structural guards make previously implicit middleware values explicit without assertions or changed authorization outcomes; extraction keeps the route projection and response mapping locally auditable."
  },
  "server/auth.ts": {
    "allowed_mass": 395,
    "date": "2026-07-27",
    "reason": "TypeScript migration (2026-07-27) + strict-typing closure (2026-07-28): auth.ts migrated from auth.js and fully typed under strict-plus contracts. Baseline tightened from 581 to 395 after explicit type annotations eliminated implicit branching from unknown/object-typed parameters. D4 complexity: gates registerConnector's derived-column repair backfill behind the same backfillRetrievalIndexes flag that guards retrieval-index backfill, requiring at least one extra branch to express two different flag-gated behaviors sharing one backend check."
  },
  "server/cimd.ts": {
    "allowed_mass": 46,
    "date": "2026-07-27",
    "reason": "P2 TypeScript migration (2026-07-27): CIMD's public fetch boundary must retain the checked DNS-result union, typed error/cache paths, and the explicit absent-versus-null client_secret check rather than erase them through a cast. The added branches keep hostname-bearing failures and public-client validation closed over every outcome."
  },
  "server/connection-identity.ts": {
    "allowed_mass": 16,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): connection identity still distinguishes a missing connector instance, a missing active binding, and a connector mismatch before forming the public wire binding. These runtime checks are the existing fail-closed behavior made explicit at an unknown storage boundary; removing them would require a cast or conflate those states."
  },
  "server/connector-key.ts": {
    "allowed_mass": 4,
    "date": "2026-07-26",
    "reason": "Lane C3c TypeScript migration (2026-07-26): canonicalConnectorKey's legacy-alias branch indexed LEGACY_LOCAL_ALIASES[trimmed] with an unchecked string in the original .js; under strict-plus typing that requires trimmed to be narrowed to the object's literal key union first. Added a 4-line isLegacyLocalAliasKey(value): value is LegacyLocalAlias type guard (baseline 2 -> current 4) rather than an unsafe index signature or an `as` assertion on the lookup. No behavior change: the guard's body is the same LEGACY_LOCAL_ALIAS_SET.has(value) check the pre-migration code already performed one line earlier before this branch was ever reached."
  },
  "server/connector-maintenance-sweep.ts": {
    "allowed_mass": 18,
    "date": "2026-07-30",
    "reason": "P0/P1/P2 fleet evidence convergence correction (2026-07-30): periodic and startup bounded evidence maintenance must retain a keyset cursor through a durable cross-backend store, acquire a generation-and-token fenced lease before the full read/sweep/write unit, reject malformed/non-resumable results without overwriting the prior cursor, reject a malformed completed result that carries a cursor, retain progress on failure, and discard a completion when its lease was superseded. An incomplete NULL result is valid only when the leased current cursor is also NULL, because the bounded sweep uses the cursor-before-page to retry a heavy first-page fold; after a non-null cursor it remains fail-closed. Startup must likewise retry the valid first-page NULL only through its existing round cap. These independent scheduling-safety branches sit at the orchestration boundary; extracting them would conceal ownership/error ordering or split a small module into shallow helpers. SQLite/real-PostgreSQL restart, cursor-loss, heavy-first-page, overlap, and stale-writer regressions prove the paths."
  },
  "server/connector-manifest-validation.ts": {
    "allowed_mass": 124,
    "date": "2026-07-06",
    "reason": "Port of main's external-tool detection hardening (security fix e6e96933b) + owner-cancel/recovery surfaces from #199/#190 reconciliation; behavior required by main's test contract. Queued for decomposition in the Phase-1 drain."
  },
  "server/connector-schema-builder.ts": {
    "allowed_mass": 9,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): schema construction retains the existing grant, manifest, and freshness absence checks before assembling metadata. The branches preserve the distinction between owner and grant-scoped visibility and avoid asserting untrusted manifest fields."
  },
  "server/connector-summary-evidence-engine.ts": {
    "allowed_mass": 75,
    "date": "2026-07-29",
    "reason": "Terminal-gate re-revision (2026-07-29): per the gate's explicit instruction to prefer the existing maintenance sweep consuming already-durable canonical signals over a new parallel outbox, classifyCandidate gained two additional dirty-independent staleness checks -- schedule_mismatch (compares the evidence row's stored schedule_checkpoint against the live connector_schedules.updated_at) and lifecycle_checkpoint_lag (compares against the live MAX(spine_events.event_seq), unfiltered by event_type, unlike the existing terminal-only checkpoint) -- plus the matching discovery-context batch reads, repair-candidate reads, and upsert column wiring on both SQLite and PostgreSQL backends. The added branches are the load-bearing mechanism that makes a swallowed best-effort dirty mark on a schedule mutation or a non-terminal run-lifecycle event (e.g. run.started) self-heal on the next maintenance sweep using only signals the canonical mutation already committed durably -- no new table, no new write path. Proven by dedicated fault-injection tests that mutate the canonical tables directly, deliberately never calling any dirty-mark function, then assert the sweep still detects and repairs (reconcile-schedule-and-lifecycle-checkpoints.test.ts, 4/4 passing across SQLite and real PostgreSQL, deterministic across repeated runs)."
  },
  "server/connector-summary-read-model.ts": {
    "allowed_mass": 124,
    "date": "2026-08-01",
    "reason": "Bounded evidence phase-progress fix (2026-07-30, allowed_mass 103), REVISED by the within-page phase-fairness fix (2026-07-31, allowed_mass 103->105): live evidence proved unconditional fold-first was itself the starvation, not its prevention -- a page whose terminal fold was genuinely slow (few events, high per-write latency) consumed the whole shared deadline every round, leaving generic repair (record_checkpoint_mismatch, dirty) zero remaining budget on every visit to that page, forever, even though the fold's own checkpoint kept advancing and the cross-page cursor was otherwise progressing normally. Fold and generic repair write disjoint evidence columns, so neither phase's correctness requires the other to run first except missing-repair before fold (a nonexistent row has nothing to fold) -- runBoundedObservationPhases now alternates which phase (first fold attempt vs. generic repair) gets first claim on the shared deadline, keyed off the caller's already-durable, already-atomically-incremented ConnectorMaintenanceCursorLease.generation (connector-maintenance-sweep.ts) -- no new column, no new query, no in-process-only counter. The two-branch turn-order dispatch (genericRepairGoesFirst) plus the small buildPageObservationOptions extraction (kept to hold runBoundedSummaryEvidenceSweep under the per-function complexity ceiling) are the +2 mass over the prior 103: threading one more optional field through three call sites and branching on its parity is the minimum mechanism that gives either phase the FULL remaining deadline on its turn, which a fixed time-fraction split could not guarantee (a repair or fold unit slower than any plausible reserved fraction still converges once it gets a genuine turn). Proven by test/connector-summary-evidence-phase-fairness.test.ts: an exact live-symptom reproduction (fold-always-first starves a dirty connection's generic repair forever under a permanently-slow fold), the fix converging the same scenario within a small bounded round count under alternating turn parity, an adversarial case where BOTH phases are individually slower than the round budget (discriminating this fix from a fixed-fraction reservation, which could not converge it), and a no-regression check that a brand-new (missing) connection still creates its row and folds its terminal history in one convergent pass on either turn parity. || REVISE (2026-08-01, discovery de-duplication, allowed_mass 105->108): live evidence after the phase-fairness and starved-page-rewind fixes still showed a live/active connection's list_summary_projection_state going stale 163s+, exceeding the accepted 120s two-turn bound -- root-caused to runBoundedObservationPhases's missing and generic repair phases each independently calling reconcileConnectorSummaryEvidence, which each paid its own full discoverCandidates round-trip against the same page's ids. On a 25-connection page this doubled discovery cost was frequently enough alone to exhaust the page's 2000ms shared deadline before repair work finished, skip-ping a newly-dirtied candidate (skipped, unlike starved, does not rewind the sweep cursor) for a full extra round-robin turn. missing and every GENERIC_REPAIR_CANDIDATE_REASONS entry are mutually exclusive per row (classifyCandidate returns exactly one reason, missing first), so one discovery pass safely serves both phases. The +3 mass is the minimum mechanism: a nullable `discovery` local (typed against the engine's own discovery-result shape) lazily populated by whichever repair phase runs first via `discovery ??= await discoverCandidates(...)`, then threaded through reconcileConnectorSummaryEvidence's new optional precomputedDiscovery parameter. Reduction was not attempted beyond that -- collapsing the lazy-init into a plain unconditional call before both phases would remove the fix's own benefit (a fully-starved page must still avoid paying for discovery at all), and extracting the one-line null-coalescing assignment into a helper would not lower the branching mass, only relocate it. Proven by test/connector-summary-evidence-sweep-discovery-dedup.test.ts's four tests using a direct testOnlyDiscoverCandidatesCallCounter (SQL query-count instrumentation does not reliably observe the repair loop's own prepared statements in this harness, confirmed via direct inline instrumentation showing 0 intercepted calls for a call that indisputably performed 5 real repairs): a page with candidates in both phases, a page with a candidate in only one phase, precomputedDiscovery genuinely skipping the second discoverCandidates call, and no regression for existing callers that omit it -- all four verified mutation-sensitive by reverting the fix and confirming the exact expected count regression (2 instead of 1). || REVISE (2026-08-01, recovery-gap-closure terminal fold gap, allowed_mass 108->124): live evidence (cin_bc1efca69a1c386d610f0924) proved a recovery-only run's durable detail-gap recoveries were permanently unreachable from this fold -- parseTerminalFactEvent gates entirely on collection_facts, which buildCollectionFacts returns null for on every recovery-only run by design (PR #322), so a mature covered=2/considered=4 stream stayed frozen at 2/4 forever even after every outstanding gap was durably recovered. The fix adds a SEPARATE, narrower fact channel rather than relaxing the existing one: parseTerminalEventPayload (extracted, mass-neutral raw-JSON-parse split out of parseTerminalFactEvent so both new and old parsers share it) and parseRecoveryGapClosureFactEvent (mirrors parseTerminalFactEvent's shape for the new recovery_gap_closure_facts block) are pure parse functions with no branching cost beyond the existing pattern they mirror. The real mass is applyRecoveryGapClosureFacts (+~10): unlike mergeEventStreamFacts (newest-attempt-wins replacement), this NARROWS an existing fact and every guard is a distinct correctness boundary that must reject silently rather than fabricate proof -- no stored fact yet (nothing to narrow), stored checkpoint not durable (committed/disabled) yet (no floor to narrow toward), no known considered denominator, no known prior covered/collected numerator, and a Math.min cap so an over-declared recovery count can never push covered past its own proven considered. Collapsing these into fewer branches was rejected: each is independently load-bearing (a bug in any one either fabricates a stream's first measurement or lets a gap-closure claim exceed what the last genuine measurement itself proved), which is exactly the provenance-falsification failure mode PR #322 already rejected once for this same recovery-only boundary. foldTerminalEventFacts (+~4) now parses both blocks independently (recovery-only run.completed carries recovery_gap_closure_facts but never collection_facts, so either-present must gate the row's attribution/generation-fencing/checkpoint logic, not collection_facts alone) and applies whichever parsed blocks are present through the same generation/checkpoint gate, unchanged. Proven load-bearing by connector-summary-stream-facts.test.ts's new recovery_gap_closure_facts tests: exact production-shape reproduction (mature 2/4 committed row + recovery-only 4/4 gap closure converges to 4/4, checkpoint advances, repeated fold is stable, a later failed/cancelled run cannot regress it), plus negative tests proving the block never originates a fresh fact, never narrows an unresolved (not_committed) stream, and never pushes covered past its own considered denominator."
  },
  "server/connector-summary-reconcile-observability.ts": {
    "allowed_mass": 15,
    "date": "2026-07-30",
    "reason": "Bounded evidence phase-progress fix (2026-07-30): the reconcile receipt serializes aggregate terminal-fold progress dimensions independently, preserving null for no participant/checkpoint while clamping numeric fields and retaining the existing allowlisted, payload-free telemetry boundary. The explicit null-versus-number branches distinguish an absent fold from checkpoint zero; no fold-duration field is emitted because bounded maintenance has one cooperative absolute deadline rather than a synthetic phase timeout. SQLite and real disposable-PostgreSQL starvation regressions assert the values, and the observability suite asserts the structured output."
  },
  "server/connector-summary-terminal-publisher.ts": {
    "allowed_mass": 5,
    "date": "2026-07-31",
    "reason": "New file (2026-07-31, fleet-summary-terminal-publisher): the one bounded maintenance authority that assembles a ConnectorListSummaryTerminalProjection for a swept page of connections and calls the already-fenced publishConnectorListSummaryTerminalProjection. publishConnectorListSummaryTerminalProjection/getConnectorListSummaryTerminalProjection[Batch] have existed since the terminal-gate revision, fully CAS-fenced and unit-tested, but had zero production callsites -- list_summary_projection_* sat permanently stale/unobserved. Mass is the minimum needed to stay bounded and honest: an early-return guard for an empty id set, the per-row current-evidence filter (evidenceRowIsFullyCurrent, mirroring the publisher's own currentEvidenceWhere predicate so this module never force-publishes a non-current row), an early-return when no instance rows resolve, and the per-row try/catch inside the publish loop so one row's fencing rejection or thrown error never blocks the rest of the batch. Collapsing the filter into the publisher's own fencing would silently waste an assembly pass on rows guaranteed to be rejected; collapsing the try/catch would let one race turn an otherwise-successful sweep round into a failed maintenance tick. Proven by test/connector-summary-terminal-publisher.test.ts (6 tests, SQLite + real PostgreSQL): successful ingest -> dirty -> sweep -> publish; a concurrent mutation between sweep and publish is fenced out and a later sweep retries and succeeds; a never-before-observed connection converges and publishes on its first sweep (startup/restart shape); a bounded sweep page publishes exactly its own page with one batched assembly (no N+1); and a static grep oracle proving no read-endpoint file calls the publisher."
  },
  "server/db.ts": {
    "allowed_mass": 173,
    "date": "2026-07-22",
    "reason": "The SQLite migration must inventory recognized enum shapes, preserve dependent objects, rebuild atomically, and fail closed before committing a foreign_key_check. These independent persistence safety branches are necessary to avoid lossy migration of historical schemas."
  },
  "server/index.ts": {
    "allowed_mass": 714,
    "date": "2026-07-27",
    "reason": "RI server entry root TypeScript migration (2026-07-27): server/index.js is the largest single file in the codebase (~6800 lines), containing startup orchestration, all route-adapter mounts, the scheduler manager factory, CLI entrypoint, and browser-surface lifecycle. The entire structural complexity is inherited from the pre-migration JS source; no new control-flow branches were introduced. The 25 type-contract fixes (exactOptionalPropertyTypes conditional spreads, useUnknownInCatchVariables guards, noUncheckedIndexedAccess bracket narrowing, and explicit typed parameters on implicit-any callbacks) each add at most one conditional branch; removing them would require suppressions or any-casts that violate the strict-plus baseline. Mass is the verbatim translation of the original JS complexity; it cannot be reduced without splitting this file into sub-modules, which is a separate refactor orthogonal to this migration slice."
  },
  "server/local-transformer-child.ts": {
    "allowed_mass": 1,
    "date": "2026-07-26",
    "reason": "Lane C3c TypeScript migration (2026-07-26): pump()'s `const job = queue.shift()` returns TransformerJob | undefined under noUncheckedIndexedAccess even though `queue.length > 0` in the enclosing while-condition already guarantees an element is present at this single-threaded call site. Added `if (!job) { break; }` (baseline 0 -> current 1) as the honest narrowing guard rather than a forbidden non-null assertion; the branch is unreachable in practice (queue is only ever read/written synchronously within this one module) but the type system cannot prove that from the API shape of Array.prototype.shift alone."
  },
  "server/manifest-resolution.ts": {
    "allowed_mass": 30,
    "date": "2026-07-28",
    "reason": "TypeScript migration closure (2026-07-28): manifest resolution adds 3 extra guards for StorageBinding | null | undefined -> StorageBinding | null | undefined alignment with exactOptionalPropertyTypes and SourceDescriptorOptions nativeManifest field. Each guard narrows a real nullable boundary; removing them would require a cast that violates the strict-plus baseline."
  },
  "server/owner-detail-gap-projection.ts": {
    "allowed_mass": 5,
    "date": "2026-08-02",
    "reason": "New file (2026-08-02, owner-only detail-gap projection): the bounded page must reject malformed limits and cursors, bind cursors to one connection, derive policy and lease state, and emit a strict redacted wire shape. These branches preserve pagination, exact-scope, and disclosure invariants; extracting them would split one projection contract without reducing a state distinction. Owner-route tests prove bounds, cursor reuse rejection, field allowlisting, and redaction."
  },
  "server/postgres-records.ts": {
    "allowed_mass": 204,
    "date": "2026-07-28",
    "reason": "Final TypeScript/PostgreSQL closure (2026-07-28): pg returns MIN/MAX aggregates over timestamptz columns as Date instances, while the typed query boundary also admits string-shaped rows from adapters and tests. computePostgresRecordWindow must distinguish both representations before normalizing them and reject invalid timestamps without emitting malformed window metadata. The eight-point increase over the unchanged 196 baseline is the explicit runtime discrimination that fixes the real PostgreSQL record-window parity failure; hiding it behind assertions or string coercion would erase the backend data-shape contract rather than reduce semantic complexity."
  },
  "server/postgres-storage.ts": {
    "allowed_mass": 117,
    "date": "2026-07-22",
    "reason": "The PostgreSQL migration serializes public legacy bootstraps before catalog discovery with bounded pg_try_advisory_lock polling, distinguishes exact direct legacy/current checks from unrelated compound checks, and performs transactional rollback. The polling/lock classification and fail-closed branches are required to avoid broad constraint deletion, concurrent startup failure, or concurrent-index deadlock."
  },
  "server/records.ts": {
    "allowed_mass": 456,
    "date": "2026-07-28",
    "reason": "TypeScript migration closure (2026-07-28): server/records.ts strict-plus type fixes add 7 conditional branches for null/undefined guards (string|null|undefined -> string narrowing), DevicePreparePlanEntry/DeviceRecordPlanEntry alignment, and exactOptionalPropertyTypes spreads. Each branch is an honest narrowing guard that expresses a real nullable boundary; the alternative is a cast or suppression that violates the strict-plus baseline."
  },
  "server/ref-control.ts": {
    "allowed_mass": 217,
    "date": "2026-07-30",
    "reason": "Fable ruling terminal-read-architecture-fable-0730.md §8 (2026-07-30, identity_inventory) + design doc add-source-perf-design-agy-0730.md R4/R5 (2026-07-30, retained_count_summary). The second increment adds the retained_count_summary named profile to the same listConnectorSummaryPage/getConnectorSummaryForRoute projection (one-projection-N-profiles, not a third projection): projectConnectorRetainedCountSummaryPage's own bounded dependency read (identity page + one evidence-row batch + one acquisition-batch-store batch, zero spine/browser-surface/runtime/schedule/run-history reads), its extracted deriveRetainedCountState helper (kept separate specifically to hold the per-row closure under the cognitive-complexity budget), a third profile branch and overload at each public entry point, and the new getConnectorSummaryForRoute overload. Removing the branch would either fold the retained-count path into the full projection (defeating the cost reduction the profile exists to prove) or duplicate the two public functions a second time."
  },
  "server/routes/ref-connectors.ts": {
    "allowed_mass": 33,
    "date": "2026-07-30",
    "reason": "Fable ruling terminal-read-architecture-fable-0730.md §8 (2026-07-30, identity_inventory) + design doc add-source-perf-design-agy-0730.md R4/R5 (2026-07-30, retained_count_summary + connector_id SET scope). The second increment adds a third accepted profile value (validated in the same one-place guard) and its matching branch/overload fork in sendConnectionScopedConnectorSummary's listConnectorSummaries closure, so the connection-scoped `?connection=` form can also serve retained_count_summary. The added branches are the profile parse/validate guard's extra arm and the closure's third call-site fork; both are the minimum surface for the route to expose a second additional named profile without a second route."
  },
  "server/routes/owner-connection-diagnostics.ts": {
    "allowed_mass": 30,
    "date": "2026-08-02",
    "reason": "Owner detail-gap observability (2026-08-02): the existing diagnostics adapter gains one exact-connection page route with strict query-shape validation, typed invalid-request mapping, operation-specific audit evidence, and the existing owner bearer and namespace guards. The added branches preserve the auth and error ordering at this HTTP boundary; moving them into the projection would hide transport semantics and weaken route-local reasoning. Focused route tests prove owner-only auth, exact scope, cursor/limit errors, and audit-safe behavior."
  },
  "server/routes/ref-device-exporters.ts": {
    "allowed_mass": 161,
    "date": "2026-07-25",
    "reason": "decouple-device-enrollment-from-ingest-writer-admission adds the idempotent re-enroll path (handleIdempotentReEnroll) that recovers a one-time device credential lost to a transport failure: it must enforce four independent safeguards (unexpired code, same bound device, device-not-revoked, same binding) before atomically rotating the credential, emit an audit receipt, and reject every other consumed-code replay — plus a typed-503 backpressure mapping (respondEnrollError). The enroll handler itself was decomposed first (performFirstEnrollment, handleIdempotentReEnroll, respondEnrollError all extracted, each within the per-function complexity budget); the residual file-level mass is the four safeguard branches, which are load-bearing correctness for a live credential-loss fix, not incidental style. D5/D6 (fix-enroll-pending-code-partial-write-idempotency / fix-enroll-stable-binding-identity-key) add a second typed-error branch to respondEnrollError (raw Postgres 23505 -> retryable 503, defense-in-depth alongside the existing connector_instance_busy mapping), a fault-injection hook (maybeEnrollPhaseFault) mirroring the file's existing deviceIngestPhaseFaultHook seam so the D5/D6 oracles can inject exact partial-write/race failure points deterministically, and an explicit consumed-code-replay rejection (respondIfConsumedCodeReplay) so a declined D2 resume can never fall through to performFirstEnrollment and create an orphaned, unclaimable device. The consumed-code handling was extracted into its own function specifically to keep the route handler's own complexity flat; the residual file-level mass is the extra typed-error branch and the fault-injection seam, both load-bearing for the live 500/23505 counterexamples this closes."
  },
  "server/scheduler-manager-factory.ts": {
    "allowed_mass": 76,
    "date": "2026-07-27",
    "reason": "TypeScript migration (2026-07-27): strict contracts replace implicit JavaScript surfaces for scheduler dependencies, and the original sequential refresh order is preserved with an explicit promise reduction rather than parallelizing manifest resolution and owner-token issuance. The four-point mass increase is the minimum control-flow cost of retaining that ordering while satisfying the no-await-in-loop rule; removing it would change refresh concurrency and behavior."
  },
  "server/source-descriptor.ts": {
    "allowed_mass": 3,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): descriptor construction continues to validate the binding kind and identifier before projecting a client-visible source. The small guard set is the existing null-on-invalid contract expressed honestly under strict typing."
  },
  "server/ssrf-guard.ts": {
    "allowed_mass": 29,
    "date": "2026-07-27",
    "reason": "P2 TypeScript migration (2026-07-27): separate Undici and Node HTTPS callback adapters share one bounded-address core but preserve their incompatible callback settlement contracts. The additional branches are the load-bearing error-without-socket and late-socket-destruction paths proven by ssrf-guard.test.js; collapsing them would either fabricate a socket or weaken single settlement."
  },
  "server/stores/browser-surface-replacement-ledger-store.ts": {
    "allowed_mass": 48,
    "date": "2026-07-30",
    "reason": "Reviewed replacement episode correction (2026-07-30): atomic correction must fail closed on a complete exact-scope immutable set, a named failed predecessor, unresolved membership, and no intervening competing starts, while retaining parity for SQLite and PostgreSQL transactions. The additional branches are the validation boundary and idempotent/reversible persistence checks; collapsing them into a generic override helper would weaken the intentionally stricter batch contract or hide backend transaction semantics. Proven by mutation-grade omission, mixed-scope, altered-fingerprint, resolved/unrelated member, partial-apply, retry, revoke, and non-target-isolation tests."
  },
  "server/stores/connector-detail-gap-store.js": {
    "allowed_mass": 29,
    "date": "2026-07-22",
    "reason": "The SQLite and Postgres implementations must each make the same run-and-lease-owned compare-and-set transitions for claim, explicit attempt, recovery, re-deferral, release, and expired-lease reclaim. The branches preserve the distinction between an untouched issuance and a real provider attempt across durable backend dialects; collapsing them behind a shallow helper would hide the SQL ownership predicates that prevent re-serve races."
  },
  "server/stores/connector-detail-gap-store.ts": {
    "allowed_mass": 84,
    "date": "2026-08-02",
    "reason": "Sticky-terminal metadata repair: each SQLite/PostgreSQL upsert conflict branch uses explicit terminal-only CASE preservation for reason, next_attempt_after, last_error_json, and last_run_id so pending rediscovery cannot erase the durable quarantine decision that the scoped repair primitive requires. The duplicated predicates are backend-specific SQL and preserve the existing recovered reopen CASE byte-for-byte; extracting them would hide the dialect-specific conflict semantics without reducing the required state distinctions. Focused SQLite and PostgreSQL regressions prove metadata survives rediscovery and terminal+quarantined repair still requeues. || Owner detail-gap observability (2026-08-02): adds the same exact-instance, all-status, `(created_at, gap_id)` keyset query to SQLite and PostgreSQL. The duplicated SQL is backend-specific bounded persistence logic, and the cursor predicates plus limit placement preserve parity and query bounds. SQLite conformance passes; the PostgreSQL parity test is gated by the repository's existing test URL. || gmail-too-large-settlement-0802 (2026-08-02): the exact-scope repair selectors and lease/status compare-and-set transitions intentionally mirror backend-specific SQL predicates for pending status, stored evidence class, connector instance, stream, and unleased ownership. The duplicated SQL is the durable ownership fence and preserves exact evidence while making repair idempotent; extracting it would hide dialect-specific conditions without reducing state distinctions. Proven by the exact-scope dry-run/apply/idempotence tests, four-row starvation test, and stale-lease CAS test."
  },
  "server/stores/connector-instance-credential-store.ts": {
    "allowed_mass": 7,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): encrypted credential persistence retains validation for credential kinds, ownership, status, and secret recovery. The checks protect the fail-closed store contract and both backend paths; replacing them with casts would weaken the typed storage boundary."
  },
  "server/stores/connector-instance-store.js": {
    "allowed_mass": 28,
    "date": "2026-07-25",
    "reason": "D8 (fix-enroll-connector-instance-pk-collision): a live counterexample proved connector_instances.upsert's INSERT ... ON CONFLICT(owner, connector, source_kind, source_binding_key) is not resilient to a PRIMARY KEY collision against a legacy row for the SAME logical binding whose source_binding_key was computed under an OLDER, larger sourceBinding shape (embedding per-enrollment device_id/source_instance_id) that predates deviceExporterSourceBindingIdentity's stable {kind, local_binding_name}-only shape. That legacy row's id predates makeConnectorInstanceId and coincides with what today's formula computes for this same binding under its current key. Reaching the INSERT already proves the named ON CONFLICT target did not match this binding under its CURRENT key, so a 23505 here means either (a) the colliding row is this same binding under a legacy key (a migration, not a collision), or (b) it is a genuinely unrelated row (must fail closed). Both backends (Postgres try/catch; SQLite equivalent catching SQLITE_CONSTRAINT_PRIMARYKEY) now look up the colliding row and migrate it in place -- UPDATE its source_binding_key/source_binding_json to the current shape and return its SAME connector_instance_id -- ONLY when isSameLogicalBindingUnderLegacyKey proves it (same owner/connector/source_kind, and the local_binding_name embedded in its OWN stored source_binding_json matches); any other collision re-throws the raw 23505 unmodified. Proven load-bearing by a mutation test: reverting the migration logic makes the Postgres D8 regression (device-enroll-postgres-admission-decoupling.test.js) fail deterministically 3/3 runs with the exact live 503 enrollment_identity_conflict symptom; a SQLite unit test (connector-instance-store.test.js) additionally proves the fail-closed path for an unrelated colliding row."
  },
  "server/stores/connector-maintenance-cursor-store.ts": {
    "allowed_mass": 5,
    "date": "2026-07-30",
    "reason": "P1 PostgreSQL-replica maintenance fence (2026-07-30): the singleton cursor must atomically acquire only an unleased/expired row, issue a monotonically increasing generation plus opaque lease token, and condition both commit and failure release on both values. The SQLite BEGIN IMMEDIATE and PostgreSQL conditional UPDATE forms intentionally expose their backend predicates rather than hiding them behind a shallow SQL abstraction. A real PostgreSQL dual-runner regression and SQLite parity prove that an expired stale incomplete owner cannot replace a later complete NULL cursor."
  },
  "server/stores/credential-encryption.ts": {
    "allowed_mass": 12,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): encryption retains its key-source, sealed-token shape, field-length, and authenticated-decrypt guards. The typed rethrows now preserve the original causal error on the same CredentialEncryptionError instance; these branches are required to remain secret-free and fail closed."
  },
  "server/stores/device-exporter-store.ts": {
    "allowed_mass": 48,
    "date": "2026-07-31",
    "reason": "decouple-device-enrollment-from-ingest-writer-admission adds rotateDeviceCredential to both the SQLite and Postgres store backends (the store is a two-backend union, so a new durable primitive necessarily lands twice). Each revokes all non-revoked credentials for the device and inserts exactly one fresh credential; the Postgres variant wraps revoke+insert in withPostgresTransaction so concurrent rotations serialize on the revoke's row locks and exactly one active credential remains. D6 (fix-enroll-stable-binding-identity-key, +1) adds resolveOrCreateEnrollmentDevice to both backends: atomically resolves an existing orphaned partial-write device for a (owner, connector, binding) or creates a fresh one, serialized on Postgres by a pg_advisory_xact_lock keyed on the binding (proven load-bearing by a mutation test: removing the lock makes four independent Postgres oracles fail deterministically) and on SQLite by the single-writer connection (no explicit lock needed, documented in the method itself). D7 (fix-enroll-source-kind-identity-gap, +2) qualifies advisoryEnrollmentBindingKey and both resolveOrCreateEnrollmentDevice orphan queries by sourceKind (an added parameter plus an added SQL predicate on each of the two backends' lookup queries), closing a live cross-kind adoption gap where a local_device orphan could be adopted by a browser_collector enrollment sharing the same owner/connector/binding. Proven load-bearing by a mutation test: removing the source_kind predicate from the Postgres orphan query makes the new D7 adversarial oracle fail deterministically (3/3 runs). This is the minimum surface for the credential-rotation, identity-resolution, and source-kind-isolation guarantees and cannot be reduced without dropping one backend or the qualifier. Scoped-ingest-outcome-summary fix (2026-07-30, +4): listSourceInstanceHeartbeatsByConnectionIds' LEFT JOIN subquery aggregated MAX(accepted_at) GROUP BY device_id, source_instance_id over the entire device_ingest_batch_outcomes table (filtered only by status='accepted'), so the connector-summary LIST read's cost grew with total fleet-wide ingest history instead of the requested page. Both the SQLite and Postgres branches (a two-backend union, so the fix lands twice) now add a device_id IN (SELECT device_id FROM device_source_instances WHERE connector_instance_id ...) predicate inside the subquery, scoping the aggregate to the requested connector_instance_id page. The predicate is deliberately derived through device_source_instances rather than filtering device_ingest_batch_outcomes.connector_instance_id directly: legacy rows written through recordBatchOutcome's insert-batch-outcome path can carry an empty-string connector_instance_id (normalizeOutcome's `?? \"\"` default), so filtering the aggregate by that column would silently drop real ingest history for those rows. Proven by N=0/1/25 SQLite tests asserting exact parity against the unscoped legacy aggregate, no off-page leakage from a larger unrelated fleet fixture, and an EXPLAIN QUERY PLAN assertion that the subquery does not fall back to scanning all of device_ingest_batch_outcomes. fix-fleet-local-collector-evidence-completeness independent-gate follow-up (2026-07-31, +8, both backends): replaces the two-step (revoke-before-consume) supersedePriorDevicesForBinding with consumeEnrollmentCodeAndSupersedePriorDevices, which folds the enrollment-code consume UPDATE and the prior-device revoke cascade into ONE transaction so a losing/failed replacement enrollment can never revoke the device it was trying to replace (the independent gate's blocking finding #1). The added branching is the direct, minimal expression of that atomicity requirement: check the consume UPDATE's row-affected count and return false immediately (no supersede) when this attempt did not win the race, check the caller-supplied isNewDevice flag and skip the read+revoke-loop when the resolved device was adopted (an orphan resume, never a genuinely new device), then the same find-superseded-rows-and-revoke-each-through-the-existing-cascade loop the old method already had. Tried collapsing the two guard checks into one compound condition; measured no mass reduction (the two conditions gate genuinely independent decisions — race outcome vs. adopted-vs-new — and Biome's cognitive-complexity scoring counts them identically either way). Proven load-bearing by a path-bound regression (device-exporter-routes.test.ts, \"a replacement enrollment that fails before completion never revokes the prior working device\") verified failing against the pre-fix two-step code (reverted to e43348b17's content, confirmed actual: 'revoked') and passing after."
  },
  "server/stores/provider-auth-run-credentials.ts": {
    "allowed_mass": 3,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): provider-auth run injection keeps its connector/source gate and validates recovered JSON bundles before mapping environment fields. The typed JSON parse rethrow preserves code, identity, and SyntaxError cause without a suppression."
  },
  "server/stores/run-history-backfill-stage.ts": {
    "allowed_mass": 53,
    "date": "2026-07-30",
    "reason": "New file (2026-07-30, run-history-backfill-list-cutover / terminal-read-architecture-fable-0730.md §9 R9.1-R9.3): the bounded, resumable spine-to-run_history backfill stage. Its mass is inherent dual-backend duplication across five independent concerns, each needed on both SQLite and PostgreSQL: candidate-run discovery by event_seq cursor, fold-data loading (batched per-backend terminal-data extraction so facts_json captures collection_facts/known_gaps/recovery_only/collection_rate from the terminal event's raw data rather than the fold summary, which does not carry those fields), legacy connector-wide singleton attribution (resolveLegacyConnectorWideInstanceId, reusing resolveActiveByConnector's own throws-on-ambiguous semantics), per-candidate insert (both backends' ON CONFLICT DO NOTHING so a concurrent live terminal write always wins), and fenced-lease cursor bookkeeping mirroring the evidence sweep's pattern, plus a bounded multi-round startup accelerator. Two extraction passes already applied (backfillOneCandidate, loadCandidateFoldData) to keep runRunHistoryBackfillRound's own complexity below 20; the residual mass is the five dual-backend concerns themselves — collapsing further would hide backend-specific SQL predicates or lose the terminal-data/fold-summary distinction a prior draft got wrong. Proven by test/run-history-backfill-cutover.test.ts (9 tests): candidate corpus, legacy singleton skip, idempotency, crash-resume, concurrent-write and concurrent-owner races, boundedness, zero-spine-events on the product route. LIVE CANARY REVISE (2026-07-30, +10 mass): run_id alone was proven NOT globally unique on a live instance (two distinct connections legitimately sharing one run_id). Identity moved to the composite (run_id, connector_instance_id) pair throughout: candidate discovery now GROUPs BY both columns with a NULL-aware exclusion predicate (SQLite's OR-both-NULL branch; Postgres's IS NOT DISTINCT FROM); every per-candidate insert's conflict target is now ON CONFLICT(run_id, connector_instance_id) DO NOTHING, not run_id alone; and loadCandidateFoldData filters each candidate's run_id-fetched event window down to its own connector_instance_id (the real column on Postgres rows, connectionIdFromEventData on SQLite's hydrated records) before folding with the genuinely unmodified summarizeEvents/summarizeRows — only the input window is scoped. Without this, two connections sharing a run_id would collide on insert or silently blend into one wrong row. Proven by test/run-history-duplicate-run-id-identity.test.ts's backfill test: two connections sharing a run_id discover as two separate candidates and fold into two separate rows, each reflecting only its own connection's event history."
  },
  "server/stores/run-history-writer.ts": {
    "allowed_mass": 19,
    "date": "2026-07-30",
    "reason": "New file (2026-07-30, Authority Slice A / generalize-run-history-write-authority): the kind-neutral run_history writer hooked into emitSpineEvent. Its branches are the identity guard (skip a run event missing run_id/connector_instance_id/connector_id rather than write a row with fabricated identity), the run.started-vs-terminal event-type branch, the finalize-then-fallback-insert branch (an UPDATE targeting status='running' that affects zero rows falls back to an ON CONFLICT DO NOTHING insert of the run already terminal, covering a lost/raced started write), and the small field-reassembly helpers (connectorErrorJsonFromTerminalData reassembles the executor's flattened connector-error fields; factsJsonFromTerminalData extracts a small bounded subset of SpineSummary-relevant fields present on the terminal event's own data). Each branch is load-bearing idempotency/identity-safety logic, not incidental structure; collapsing them would either silently duplicate a row under a race or write fabricated identity. Proven by test/run-history-writer-authority.test.ts (6 tests covering every branch: per-run-kind started->finalized transition, retried-started idempotency, retried-terminal idempotency, terminal-only fallback insert, scheduler-merge-not-duplicate, scheduler_managed reader scoping). REVISE (second gate pass, 2026-07-30, G1 closure): +6 mass for the new run.progress_reported branch on both backends (collectionRateMergeJson's undefined-guard against writing an empty merge on a progress event carrying no rate change, plus the SQLite/Postgres branch dispatch itself) that atomically merges collection_rate into a still-running row's facts_json, fenced by status='running' so a concurrent/later terminal write always wins. This closes a live G1 violation the gate reproduced against the sanctioned Postgres instance (a spine_events read fired on every connector-summary route for a currently-running connection); the branch is the load-bearing fence, not incidental structure. Proven by test/active-run-summary-zero-spine.test.ts (6 tests, dual-backend, zero skips): the merge lands correctly, a stale post-terminal progress event is a no-op, and zero spine_events statements survive for in-progress/terminal/no-run GETs on both SQLite and real Postgres."
  },
  "server/stores/static-secret-credential-probe.ts": {
    "allowed_mass": 10,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): the credential probe adapter retains capability gating and the provider-error versus generic-retryable mapping. The Error-property checks preserve the existing owner-safe failure surface when a dependency throws an unknown value."
  },
  "server/stores/static-secret-run-credentials.ts": {
    "allowed_mass": 6,
    "date": "2026-07-27",
    "reason": "Auth/store leaf TypeScript migration (2026-07-27): run credential injection keeps the static-secret connector gate and the browser-session exception for absent, revoked, or rejected optional login credentials. These branches preserve fail-closed recovery for all other paths without casting the recovered credential."
  },
  "server/transport.ts": {
    "allowed_mass": 77,
    "date": "2026-07-27",
    "reason": "P2 TypeScript migration (2026-07-27): the Fastify-to-Express shim now has explicit runtime-checked query-parser and route-manifest boundaries, plus response/header/middleware contracts. These branches preserve existing request validation ordering and Express chain semantics; removing them would move framework-specific uncertainty into route modules."
  },
  "server/web-push-notifications.ts": {
    "allowed_mass": 13,
    "date": "2026-08-01",
    "reason": "notification-narrow-0801: closes the UAT gate's P1 TTL finding (TTL computed from the interaction's original duration, not durable remaining lifetime at send time, so a delayed push could outlive its interaction) and lands the accepted RFC 8030 Urgency scope-narrowing (only response-required interaction kinds with a bounded timeout get `high`). fanoutPendingInteractionWebPushImpl gains one added branch: reading timeout_seconds off the interaction, computing the urgency+TTL decision via notification-policy.ts's projectInteractionTransportPriority/computeInteractionPushTtl, and returning a suppressed result outright when no useful time remains -- this fail-closed suppression (never send a push for an interaction already past its useful expiry) is the load-bearing behavior the gate required, not incidental structure; the actual urgency/TTL math lives in notification-policy.ts (pure, separately unit-tested with the gate's mandated 0/10/35/60/300/900/long/absent matrix) specifically so this file's added branch stays a single classify-then-decide call. Reduction was not attempted beyond that split, since folding the branch back into the existing early-returns above it would not lower complexity, only remove the one place callers need to see 'this interaction was already expired.' Proven by test/web-push-notifications.test.ts's new urgency/TTL/suppression fanout tests plus a real-socket assertion that the computed TTL/Urgency are forwarded to the literal web-push wire call, unchanged for every other fanout path (assistance/escalation/test), which keep the prior fixed-TTL/normal-urgency behavior via transportOptions defaulting to {}."
  },
  "runtime/connector-gap-bounding.ts": {
    "allowed_mass": 95,
    "date": "2026-08-01",
    "reason": "fix-recovery-gap-closure-terminal-fold-gap (2026-08-01): buildCollectionFacts is untouched -- recovery-only runs still cannot claim inventory (considered/checkpoint), by design (PR #322). The new buildRecoveryGapClosureFacts is a SEPARATE, narrower pure function (mass +2, 93->95): it groups durableDetailGaps entries with status===\"recovered\" by stream into a recovered_count map, then returns null when not recoveryOnly or nothing was recovered this run. The two early-return guards (not recoveryOnly; empty recovered-count map) are the load-bearing distinction between \"nothing to report\" and \"this run durably closed N gaps\" -- collapsing them into buildCollectionFacts itself was rejected because that function's null-unconditionally contract for recoveryOnly is a separately load-bearing invariant (connector-gap-bounding-recovery-only-facts.test.ts) that must not gain a conditional exception. Proven load-bearing by connector-gap-bounding-recovery-only-facts.test.ts's new buildRecoveryGapClosureFacts tests (recoveryOnly=false returns null even with recovered gaps; recoveryOnly=true groups per-stream counts correctly; the block never carries considered/covered/checkpoint fields)."
  },
  "runtime/scheduler/pre-run-gate.ts": {
    "allowed_mass": 16,
    "date": "2026-08-02",
    "reason": "fix-scheduler-needs-human-skip-record-suppression-regression (2026-08-02): 3cc82e6eb's notification-narrow-0801 reused the single notifiedNeedsHumanSkips Set for two DISTINCT one-shot gates -- whether gateNeedsHuman has already recorded the needs-human SKIP RECORD (the audit-trail entry onRunComplete observes) and whether it has already pushed the owner-facing ESCALATION notification -- and scheduler.ts's onInteractionWithNeedsHumanDedupe pre-armed that same Set from a scheduled interaction's onInteraction callback to suppress a duplicate escalation push. Because both gates shared one Set, the pre-arm also silently suppressed the transition's first, legitimate skip record -- reproduced directly: an automatic run that triggers an interaction never produced its expected needs_human_attention skip, so the scheduler test 'marks connector as needs-human when automatic run triggers interaction' hung inside its own 8s waitFor (Node's --test-timeout marks a timed-out test failed but does not force-exit the process, so the still-running waitFor loop kept the runner alive), which surfaced upstream as the aggregate scheduler.test.ts 120s outer timeout. Fix: split the two concerns into independent runtime sets (notifiedNeedsHumanSkips keeps gating the skip record exactly as already documented; the new notifiedNeedsHumanEscalations is scheduler.ts's dedicated pre-arm target for the push) and extract the escalation-push-once logic into pushNeedsHumanEscalationOnce so gateNeedsHuman's own branching stays flat rather than nesting the escalation gate inside the skip-record gate. Extraction was attempted and measured: it lowers the file from a nested 17 to 16, not further -- the residual mass is two independently load-bearing one-shot gates (skip-record dedup, escalation-push dedup) plus their coupled clear-on-recovery reset, which is the minimum structure that keeps the audit trail honest while still deduping the owner-facing push; collapsing them back into one Set is exactly the regression this fixes. Proven by scheduler.test.ts's 'scheduler marks connector as needs-human when automatic run triggers interaction' (now completes in ~0.3s instead of hanging past a 25s outer timeout, 3 consecutive full-suite runs of all 43 tests pass clean in ~15-17s each) and scheduler-push-escalation-l8.test.ts's existing 8 tests (including the two direct markNeedsHumanDedupeFromInteraction unit tests and the unrelated-connector-instance escalation-scoping regression), all unchanged and passing."
  }
}
