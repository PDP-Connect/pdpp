candidate_id: t1b-spine-correlations-annotators-0003
created: "2026-07-02T02:10:00.000Z"
repo_sha: "6e8d8559c36b94d41bc4c42a9a31342e60b3b334"
subsystem: lib/postgres-spine
files: [lib/postgres-spine.js, test/grant-package-postgres-path.test.js, test/ref-grant-packages.test.js]
symbols:
  - postgresListSpineCorrelations
  - file: lib/postgres-spine.js
    parent_fn: postgresListSpineCorrelations
    anchor: summaries.map grant_package_id annotation callback
    line: 739
  - file: lib/postgres-spine.js
    parent_fn: postgresListSpineCorrelations
    anchor: summaries.map client-metadata annotation callback
    line: 754
public_surface: []
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "postgresListSpineCorrelations enriches page summaries through two anonymous .map callbacks that CAPTURE their lookup index from the closure: :739 (cc 13) annotates grant summaries with grant_package_id via the captured packageByGrant Map (with a gid = s.grant_id || s.id fallback and a no-op pass-through when unmapped), :754 (cc 8) annotates grant/trace summaries with the registered-client object via the captured clientById Map. Each is a pure per-summary annotation policy hidden inside list traversal — the exact seam shape of the landed record-version-stats T1b (b2060f39a). Naming each with the captured index as an EXPLICIT parameter gives the two annotation policies (id-fallback rule, skip-when-absent rule, spread-not-mutate rule) single-entry surfaces; the enclosing fn reads as fetch -> summarize -> filter -> annotate -> paginate."
plan:
  transform_class: explicit-context-extraction
  instruction: "In lib/postgres-spine.js hoist the two callback bodies to named module-scope functions near postgresListSpineCorrelations: (1) annotateGrantPackageId(summary, packageByGrant) — body of the :739 callback, captured packageByGrant now an explicit param; (2) annotateClientMetadata(summary, clientById) — body of the :754 callback, captured clientById explicit. Callsites become summaries.map((s) => annotateGrantPackageId(s, packageByGrant)) and summaries.map((s) => annotateClientMetadata(s, clientById)). Branch bodies byte-identical: null-summary pass-through, gid fallback order (grant_id || id), packageId truthiness gate, spread-clone (never mutate the input summary), client_id string gate. The surrounding size>0 guards and await ordering stay in the enclosing fn untouched."
expected_quality_gain: medium
owner_attention_reduction: low
product_impact: low
priority:
  score: 132
  computed: "2026-07-02T02:10:00.000Z"
  inputs:
    mass: 11
    churn: 12
risk:
  blast_radius: local
  reversibility: trivial
  silent_wrongness_cost: medium
  property_at_risk: "spine correlation rows: grant_package_id present exactly when the grant maps to a package (gid fallback grant_id||id), client object present exactly when clientById has the summary's client_id; summaries never mutated in place"
authoring_evidence:
  - kind: inventory
    detail: "quality/inventory/callback-smells.json — lib/postgres-spine.js:739 cc 13 captures [packageByGrant]; :754 cc 8 captures [clientById]; both kind=iterator"
    result: "T1b class, immutable captures, zero awaits inside the callbacks"
  - kind: coverage-sweep
    detail: "grep -rln 'grant_package_id' test/; grep -rln 'listSpineCorrelations' test/"
    result: "test/grant-package-postgres-path.test.js:201 and test/ref-grant-packages.test.js:192 pin grant_package_id on spine rows; test/postgres-runtime-storage.test.js + test/ref-spine-correlations-list-operation.test.js exercise the correlations listing"
evidence_required:
  - rung: direct-test
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && sh -c 'set -e; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; rc=0; for f in grant-package-postgres-path ref-grant-packages postgres-runtime-storage ref-spine-correlations-list-operation; do db=pdpp_hone_t1b_sca3_$(echo $f | tr - _); dropdb --if-exists $db; createdb $db; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db node --test --test-force-exit test/$f.test.js || rc=1; dropdb --if-exists $db; done; exit $rc'"
    expect: "all pass with unchanged counts (2+6+14+14=36 across the four files), before AND after the edit; per-file ephemeral DBs + --test-force-exit replicate the scripts/run-tests.js isolation contract"
  - rung: exact-move
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/lib/postgres-spine.js"
    expect: the two callback bodies byte-identical modulo indentation and the explicit index params; only wrapping + naming changed
  - rung: typecheck
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit"
    expect: "0 errors"
not_allowed:
  - behavior-change
  - relocation-without-decomplecting
  - new-dependency
  - reordering-the-annotation-passes
  - touching-the-sqlite-spine-path
maker_tier: standard
judge_tier: standard
maker_provider: claude
judge_provider: codex
batch_key: preserve_refactor×pure_logic×lib/postgres-spine
touchset: [lib/postgres-spine.js]
estimates:
  tokens: 35000
  evidence_cost: medium
depends_on: []
unlocks: []
status: landed
outcome:
  commit: bb449c617b571330d455fdd4ed9fb2aba833de99
  skip_reason: null
  blocked_on: null
  judge_verdict: "codex PASS (confidence 0.94): The removed callback bodies and added helper bodies are equivalent modulo parameter naming and explicit map parameters: null pass-through, grant_id || id fallback, truthiness gates, and spread-clone behavior are preserved. Callsites keep the same annotation pass order and surrounding size guards, with no new dependency or public surface change. The evidence covers the required before/after test rung, typecheck, and exact diff inspection for this pure preserve_refactor."
  evidence_receipts:
    - "[baseline] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && sh -c 'set -e; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; rc=0; for f in grant-package-postgres-path ref-grant-packages postgres-runtime-storage ref-spine-correlations-list-operation; do db=pdpp_hone_t1b_sca3_$(echo $f | tr - _); dropdb --if-exists $db; createdb $db; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db node --test --test-force-exit test/$f.test.js || rc=1; dropdb --if-exists $db; done; exit $rc' -> exit 0 (14s) PASS; exit=0 djb2=271fa583 bytes=4241 receipt=quality/receipts/t1b-spine-correlations-annotators-0003/baseline-1-direct-test.txt"
    - "[baseline] exact-move: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/lib/postgres-spine.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-spine-correlations-annotators-0003/baseline-2-exact-move.txt"
    - "[baseline] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (5s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-spine-correlations-annotators-0003/baseline-3-typecheck.txt"
    - "[post] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && sh -c 'set -e; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; rc=0; for f in grant-package-postgres-path ref-grant-packages postgres-runtime-storage ref-spine-correlations-list-operation; do db=pdpp_hone_t1b_sca3_$(echo $f | tr - _); dropdb --if-exists $db; createdb $db; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db node --test --test-force-exit test/$f.test.js || rc=1; dropdb --if-exists $db; done; exit $rc' -> exit 0 (14s) PASS; exit=0 djb2=5981f5b1 bytes=4241 receipt=quality/receipts/t1b-spine-correlations-annotators-0003/post-1-direct-test.txt"
    - "[post] exact-move: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/lib/postgres-spine.js -> exit 0 (0s) PASS; exit=0 djb2=3a2ceec3 bytes=1626 receipt=quality/receipts/t1b-spine-correlations-annotators-0003/post-2-exact-move.txt"
    - "[post] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (5s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-spine-correlations-annotators-0003/post-3-typecheck.txt"
  tokens_actual: 42654
  lesson: null
resets:
  - at: "2026-07-02T03:20:05.322Z"
    from_status: blocked
    reason: blocked on shared-DB duplicate-index collision; fixed by cherry-pick e7ede6e50 (idempotent record-index bootstrap from main 2f122e547)
  - at: "2026-07-02T06:59:41.987Z"
    from_status: blocked
    reason: "repaired 2026-07-02 per hang-diagnosis: run-2 idle-hang (2700s fail-closed) = missing --test-force-exit (scheduler interval keeps process alive after runner finishes) + shared/dirty DB reddened grant-package tests + concurrent shared-DB schema bootstrap race; direct-test rung rewritten to per-file ephemeral DB + --test-force-exit (wrapper-equivalent); rewritten rung verified green verbatim 36/36 exit 0 in ~90s"
