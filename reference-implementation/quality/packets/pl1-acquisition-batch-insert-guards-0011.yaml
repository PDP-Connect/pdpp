candidate_id: pl1-acquisition-batch-insert-guards-0011
created: "2026-07-02T10:10:00.000Z"
repo_sha: "078bd4d00e1c06243d73fa07eada24678e14624b"
subsystem: server/stores/acquisition-batch-store
files: [server/stores/acquisition-batch-store.ts]
symbols: [normalizeInsert]
public_surface:
  - "acquisition batch insert validation errors: 'ownerSubjectId is required.', 'connectorId is required.', 'connectorInstanceId is required.', 'artifactSha256 is required for owner-artifact batches.'"
behavior_status: contract
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "normalizeInsert (cc23, scope-fn red_scan empty) validates an owner-artifact acquisition batch then builds the persisted row (including a content-addressed batchId default). It braids REQUIRED-FIELD VALIDATION (four presence guards, each with a distinct thrown Error) with ROW CONSTRUCTION (a large defaulted object literal). Extracting the guard cluster into a named pure assert separates validation from construction — a genuine concern split — leaving normalizeInsert a straight builder and removing the four throw branches from its head. Pure function, no I/O, no grant/consent."
plan:
  transform_class: extract-validation-guard
  instruction: "In server/stores/acquisition-batch-store.ts ONLY. Create ONE module-level (column-0) pure named function ABOVE normalizeInsert:\n\nfunction assertAcquisitionBatchInsert(record) — throws on invalid input, returns nothing. Contains VERBATIM the four guards currently at the top of normalizeInsert (L76-79): `if (!record.ownerSubjectId) throw new Error('ownerSubjectId is required.'); if (!record.connectorId) throw new Error('connectorId is required.'); if (!record.connectorInstanceId) throw new Error('connectorInstanceId is required.'); if (!record.artifactSha256) throw new Error('artifactSha256 is required for owner-artifact batches.');`.\n\nSEAM MECHANISM (explicit): assertAcquisitionBatchInsert takes ONE explicit parameter named `record` (== the caller's parameter identifier), so normalizeInsert delegates with a bare identifier. normalizeInsert's body becomes `assertAcquisitionBatchInsert(record); const now = record.now ?? new Date().toISOString(); return { ...the existing object literal unchanged... };`.\n\nHARD PRESERVE: the exact order of the four guards (ownerSubjectId, connectorId, connectorInstanceId, artifactSha256), every Error message byte-for-byte, the `const now = record.now ?? new Date().toISOString();` line staying in normalizeInsert AFTER the assert, and the ENTIRE returned object literal — especially the `batchId: record.batchId ?? makeAcquisitionBatchId(record.ownerSubjectId, record.connectorId, record.artifactSha256)` default, every `?? null`/`?? 0`/`?? 'owner_artifact'`/`?? 'validated'` default, the stringifyJson(record.mediaCoverage ?? null)/stringifyJson(record.warnings ?? [])/stringifyJson(record.receipt ?? null) calls, and createdAt/updatedAt = ?? now. NO new module file, NO behavior change."
expected_quality_gain: medium
owner_attention_reduction: low
product_impact: none
priority:
  score: 46
  computed: "2026-07-02T10:10:00.000Z"
  inputs:
    mass: 18
    churn: 6
risk:
  blast_radius: local
  reversibility: trivial
  silent_wrongness_cost: low
  property_at_risk: acquisition-batch insert validation — a dropped required-field guard would let a batch row persist without the artifactSha256 that anchors its content-addressed batchId
authoring_evidence:
  - kind: red-probe
    detail: "scope-fn --target on acquisition-batch-store.ts::normalizeInsert → red_scan [], cognitive_before 23; body has no grant/consent/bearer/token markers"
    result: pure validator+builder
  - kind: coverage-check
    detail: "normalizeInsert reached through the owner-artifact ingest path exercised by test/static-secret-setup-status-route.test.js (asserts committed acquisition-batch counts/method/accepted_count after ingest, and artifact_sha256 handling)"
    result: indirect but end-to-end route oracle exercises the batch insert
evidence_required:
  - rung: direct-test
    command: "cd \"$REPO_ROOT\" && sh -c 'export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; rc=0; db=pdpp_hone_pl1acqbatch; dropdb --if-exists $db; createdb $db; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/static-secret-setup-status-route.test.js || rc=1; dropdb --if-exists $db; echo \"rc=$rc\"; exit $rc'"
    expect: static-secret-setup-status-route suite green with unchanged pass count; ephemeral DB isolates the batch rows
    expect_check:
      type: exit_code
      value: 0
  - rung: typecheck
    command: "cd \"$REPO_ROOT\" && npx tsc --noEmit"
    expect: "0 errors"
    expect_check:
      type: exit_code
      value: 0
  - rung: complexity-remeasure
    command: "node \"$HONE_ROOT/collectors/scope-fn.mjs\" --repo \"$REPO_ROOT\" --target 'server/stores/acquisition-batch-store.ts::normalizeInsert'"
    expect: found=true; cognitive_before strictly below baseline 23 and below floor 18 (four-guard cluster removed)
    expect_check:
      type: scope_fn_lt
      value: 18
  - rung: file-excess-remeasure
    command: "node \"$HONE_ROOT/collectors/scope-fn.mjs\" --repo \"$REPO_ROOT\" --file 'server/stores/acquisition-batch-store.ts' --cog 5"
    expect: file_excess strictly below the pre-edit baseline (19); the only NEW flagged name permitted is assertAcquisitionBatchInsert
    expect_check:
      type: file_excess_lt
      value: 19
  - rung: seam-and-message-pin
    command: "cd \"$REPO_ROOT\" && node -e 'const fs=require(\"fs\");const s=fs.readFileSync(\"server/stores/acquisition-batch-store.ts\",\"utf8\");const w=s.replace(/\\s+/g,\" \");let rc=0;const msgs=[\"ownerSubjectId is required.\",\"connectorId is required.\",\"connectorInstanceId is required.\",\"artifactSha256 is required for owner-artifact batches.\"];for(const m of msgs){const ok=s.includes(m);console.log((ok?\"PASS \":\"FAIL \")+\"message preserved: \"+m);if(!ok)rc=1;}const batchIdDefault=/batchId: record\\.batchId \\?\\? makeAcquisitionBatchId\\(/.test(w);console.log((batchIdDefault?\"PASS \":\"FAIL \")+\"content-addressed batchId default preserved\");if(!batchIdDefault)rc=1;if(!s.includes(\"function assertAcquisitionBatchInsert\")){console.log(\"baseline: guard not extracted; vacuous PASS for seam\");process.exit(rc);}const seam=[[\"assertAcquisitionBatchInsert(record) declared\",/function assertAcquisitionBatchInsert\\( ?record,? ?\\)/],[\"delegated with bare identifier\",/assertAcquisitionBatchInsert\\( ?record ?\\)/]];for(const c of seam){const ok=c[1].test(w);console.log((ok?\"PASS \":\"FAIL \")+c[0]);if(!ok)rc=1;}process.exit(rc);'"
    expect: "exit 0, every line PASS. All four error messages + the content-addressed batchId default enforced at baseline+post. Post: the guard helper is declared with the single named param `record` and delegated with a bare identifier."
    expect_check:
      type: exit_code
      value: 0
not_allowed:
  - behavior-change
  - public-shape-change
  - error-message-change
  - batchid-default-change
  - guard-order-change
  - new-dependency
  - new-module-file
maker_tier: cheap
judge_tier: standard
maker_provider: null
judge_provider: null
batch_key: preserve_refactor×pure_logic×server/stores/acquisition-batch-store
touchset: [server/stores/acquisition-batch-store.ts]
estimates:
  tokens: 28000
  evidence_cost: low
depends_on: []
unlocks: []
status: skipped
outcome:
  commit: null
  skip_reason: "ALREADY DECOMPLECTED by the Jul-3 unification — closed as validated-non-defect after re-anchor + fresh measurement (2026-07-05). git-follow: server/stores/acquisition-batch-store.js was migrated .js->.ts in place. In the current .ts tree the exact concern-split this packet proposed (separate the four required-field guards from normalizeInsert's row construction) is ALREADY LANDED: the guards are extracted into a module-level pure function assertRequiredInsertFields(record) that throws the same four messages byte-for-byte and returns the destructured validated fields, and normalizeInsert now opens `const { ownerSubjectId, connectorId, connectorInstanceId, artifactSha256 } = assertRequiredInsertFields(record);` (server/stores/acquisition-batch-store.ts:249,271). All four messages verified literally present ('ownerSubjectId is required.', 'connectorId is required.', 'connectorInstanceId is required.', 'artifactSha256 is required for owner-artifact batches.') and the content-addressed batchId default is preserved. FRESH MEASUREMENT (scope-fn ground truth): normalizeInsert cognitive_before=19 (was baseline 23); the residual cc19 is the large defaulted object literal the packet EXPLICITLY forbade touching (not_allowed: behavior-change/batchid-default-change), not the guard cluster — the guard cluster is gone. The packet's transform (extract the guards) has no remaining work; the only extractable helper name differs (assertRequiredInsertFields vs the packet's assertAcquisitionBatchInsert). Closed honestly rather than re-worked."
  blocked_on: null
  judge_verdict: null
  evidence_receipts: []
  tokens_actual: null
  lesson: "the unification landed the validation/construction concern-split via assertRequiredInsertFields before this packet ran; re-measure the target BEFORE re-working a preserve_refactor packet after a heavy upstream refactor — the guard cluster was already extracted, so the honest disposition is skipped/validated-non-defect."
resets:
  - at: "2026-07-02T22:07:57.330Z"
    from_status: reverted
    reason: "re-route: extraction class -> codex maker/claude judge (tonight forced claude-maker = wrong pairing, all reverted)"
  - at: "2026-07-05T00:00:00.000Z"
    from_status: pending
    reason: "re-anchor .js->.ts after Jul-3 unification; fresh measurement shows the guard cluster already extracted into assertRequiredInsertFields -> closed skipped/validated-non-defect"
