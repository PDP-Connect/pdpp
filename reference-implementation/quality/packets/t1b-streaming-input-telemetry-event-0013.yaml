candidate_id: t1b-streaming-input-telemetry-event-0013
created: "2026-07-02T02:10:00.000Z"
repo_sha: "6e8d8559c36b94d41bc4c42a9a31342e60b3b334"
subsystem: server/streaming
files: [server/streaming/routes.js, test/run-interaction-stream-input-telemetry.test.js]
symbols:
  - registerStreamingRoutes
  - file: server/streaming/routes.js
    parent_fn: registerStreamingRoutes
    anchor: app.post input-dispatch route handler (token-only)
    line: 979
public_surface:
  - "POST /_ref/run-interaction-streams/{token}/input — 202 ack envelope, 400/401/409/410 error codes (MUST NOT change; the extraction target is the telemetry construction only)"
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The input-dispatch handler (server/streaming/routes.js:979, cc 34 — the pool's largest T1b) constructs three near-duplicate telemetry event objects inline (wire.input.received / wire.input.dispatched / wire.input.error), each re-deriving the same base fields (source 'server', correlationId, wireSeq, action: body.action || null, eventType: body.type || null) plus a per-kind extra (x/y numeric-or-null, dispatchLatencyMs, errorCode/errorMessage with the 'invalid_input' fallback). The event vocabulary is a real contract for the stream-debug viewer, and its triplication is where drift starts (edit one construction, forget two). Extracting ONE named pure builder with the inputs EXPLICIT decomplects event construction from dispatch control flow; the handler keeps only authorize -> parse -> push(received) -> dispatch -> push(dispatched|error) -> respond. NOT touched: session authorization, companion lookup, status-code mapping — the public 4xx cascade stays byte-identical in place."
plan:
  transform_class: explicit-context-extraction
  instruction: "In server/streaming/routes.js add a named module-scope fn buildWireInputTelemetryEvent(kind, body, correlationId, wireSeq, extra = {}) near parseReferenceWireInputPayload's import/use: returns { source: 'server', kind, correlationId, wireSeq, action: body.action || null, eventType: body.type || null, ...extra }. Rewire the three push sites: received => inputTelemetry.push(session.browser_session_id, buildWireInputTelemetryEvent('wire.input.received', body, correlationId, wireSeq, { x: typeof body.x === 'number' ? body.x : null, y: typeof body.y === 'number' ? body.y : null })); dispatched => ...('wire.input.dispatched', body, correlationId, wireSeq, { dispatchLatencyMs: Date.now() - receivedAtMs }); error => ...('wire.input.error', body, correlationId, wireSeq, { errorCode: err.code || 'invalid_input', errorMessage: err.message || String(err) }). Field ORDER within each produced object must match today's objects (base fields then extras, as they already appear). The try/catch swallow-guards around each push, the await companion.dispatch(body) ordering, and every res.status/pdppError call stay byte-identical."
expected_quality_gain: high
owner_attention_reduction: medium
product_impact: low
priority:
  score: 87
  computed: "2026-07-02T02:10:00.000Z"
  inputs:
    mass: 29
    churn: 3
risk:
  blast_radius: local
  reversibility: trivial
  silent_wrongness_cost: medium
  property_at_risk: "the three wire.input.* event shapes (field presence and values per kind) and the route's public ack/error behavior; telemetry pushes must remain non-blocking (swallow-guarded) and in the same order relative to dispatch"
authoring_evidence:
  - kind: inventory
    detail: "quality/inventory/callback-smells.json — server/streaming/routes.js:979 cc 34 captures [streamingSessions,getCompanion,inputTelemetry], public keyword errorCode"
    result: "T1b class; code read 2026-07-01 at HEAD 20969a3c6 (lines 979-1048) confirms the three constructions are the duplicated pure mass; effects (push, dispatch, res) all stay at the callsite"
  - kind: dependency
    detail: t1b-streaming-input-telemetry-evidence-0012 (pending generate_evidence packet)
    result: creates test/run-interaction-stream-input-telemetry.test.js pinning the per-kind event fields; without it this packet has no telemetry oracle
evidence_required:
  - rung: direct-test
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node --test test/run-interaction-stream-input-telemetry.test.js test/run-interaction-stream-routes.test.js"
    expect: "all pass with unchanged counts, before AND after the edit (the telemetry test file exists — else this packet is blocked on t1b-streaming-input-telemetry-evidence-0012)"
  - rung: exact-move
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/server/streaming/routes.js"
    expect: "only the three event constructions replaced by builder calls + one new named fn; zero changes to status codes, error copy, dispatch ordering, or the swallow-guards"
  - rung: typecheck
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit"
    expect: "0 errors"
  - rung: smell-rescan
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node scripts/code-quality/smell-callbacks.mjs"
    expect: "the routes.js:979 entry's cc drops materially (callback no longer the pool's largest); no new capturing-callback entries introduced"
not_allowed:
  - behavior-change
  - public-shape-change
  - relocation-without-decomplecting
  - new-dependency
  - changing-error-codes-or-status-mapping
  - reordering-telemetry-relative-to-dispatch
  - extracting-the-whole-route-handler
maker_tier: standard
judge_tier: standard
maker_provider: claude
judge_provider: null
batch_key: preserve_refactor×pure_logic×server/streaming
touchset: [server/streaming/routes.js]
estimates:
  tokens: 50000
  evidence_cost: medium
depends_on: [t1b-streaming-input-telemetry-evidence-0012]
unlocks: []
status: skipped
outcome:
  commit: null
  skip_reason: "unactionable(dependency packet t1b-streaming-input-telemetry-evidence-0012 has not landed; test/run-interaction-stream-input-telemetry.test.js does not exist, so there is no telemetry oracle to prove field-shape preservation against.)"
  blocked_on: null
  judge_verdict: null
  evidence_receipts:
    - "[baseline] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node --test test/run-interaction-stream-input-telemetry.test.js test/run-interaction-stream-routes.test.js -> exit 0 (5s) PASS; exit=0 djb2=a7723de4 bytes=3837 receipt=quality/receipts/t1b-streaming-input-telemetry-event-0013/baseline-1-direct-test.txt"
    - "[baseline] exact-move: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/server/streaming/routes.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-streaming-input-telemetry-event-0013/baseline-2-exact-move.txt"
    - "[baseline] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (4s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-streaming-input-telemetry-event-0013/baseline-3-typecheck.txt"
    - "[baseline] smell-rescan: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node scripts/code-quality/smell-callbacks.mjs -> exit 0 (4s) PASS; exit=0 djb2=bc1a737e bytes=143968 receipt=quality/receipts/t1b-streaming-input-telemetry-event-0013/baseline-4-smell-rescan.txt"
  tokens_actual: 20960
  lesson: maker (claude) could not act on plan.instruction as written — rewrite the instruction before retrying
