candidate_id: tst2-runtime-failure-classifier-0013
created: "2026-07-06T12:02:00.000Z"
repo_sha: ae9cb548cea1de7f547b81c6879f7e09a2d99006
subsystem: runtime/failure-classification
files: [test/runtime-failure-classifier-oracle.test.js, runtime/classify-runtime-failure.js]
symbols: [classifyRuntimeFailure]
public_surface:
  - "run.failed failure_reason classification: explicit err.failure_reason wins; known protocol violations map to connector_protocol_violation; interaction-handler response failures map to interaction_handler_invalid_response; unknown failures map to runtime_error"
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: classifyRuntimeFailure assigns the durable failure_reason on terminal run.failed events. It is the small pure classifier that prevents connector protocol violations from being flattened into runtime_error and preserves explicit failure_reason overrides. A missing prefix in this classifier makes operator diagnostics and scheduler retry policy reason about the wrong failure class.
plan:
  transform_class: add-mutation-killing-pure-oracle
  instruction: "Create test/runtime-failure-classifier-oracle.test.js. Import classifyRuntimeFailure. Assert explicit err.failure_reason='custom_reason' wins even when message looks like a protocol violation; exact invalid INTERACTION_RESPONSE envelope and Invalid INTERACTION_RESPONSE status:* map to interaction_handler_invalid_response; representative protocol messages map to connector_protocol_violation, including 'Connector emitted invalid PROGRESS.provider_budget.object', 'Connector emitted invalid DONE.error.code: expected bounded snake_case code', 'Connector emitted DETAIL_COVERAGE for undeclared stream: s', 'Connector emitted RECORD after DONE', 'Connector exit code 1 does not match DONE status: succeeded', and 'Connector emitted unknown message type: X'; unknown Error and non-Error/null map to runtime_error. Keep test names naming the public failure_reason strings."
expected_quality_gain: high
owner_attention_reduction: medium
product_impact: medium
priority:
  score: 8.1
  computed: "2026-07-06T12:02:00.000Z"
  inputs:
    mass: 0
    churn: 0
risk:
  blast_radius: subsystem
  reversibility: trivial
  silent_wrongness_cost: high
  property_at_risk: durable run.failed failure_reason must distinguish runtime failure from connector protocol violation
authoring_evidence:
  - kind: source-read
    detail: runtime/classify-runtime-failure.js is a pure string classifier over err.failure_reason and err.message prefixes.
    result: No DB or side effects; mutation can pin one prefix without source changes.
  - kind: test-sweep
    detail: "rg -n 'classifyRuntimeFailure|connector_protocol_violation|interaction_handler_invalid_response' test"
    result: No dedicated by-name test imports classifyRuntimeFailure.
evidence_required:
  - rung: red-then-green-mutant-kill
    command: "cd \"$REPO_ROOT\" && sh -c 'if [ -f test/runtime-failure-classifier-oracle.test.js ]; then git diff --quiet -- runtime/classify-runtime-failure.js || { echo \"DIRTY source\"; exit 1; }; cp runtime/classify-runtime-failure.js /tmp/tst2_runtime_failure_classifier.bak; perl -0pi -e \"s/message\\.startsWith\\('Connector emitted invalid PROGRESS\\.'\\)/message.startsWith('Connector emitted invalid PROGRESX.')/\" runtime/classify-runtime-failure.js; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_runtime_failure_classifier; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/runtime-failure-classifier-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp /tmp/tst2_runtime_failure_classifier.bak runtime/classify-runtime-failure.js; rm -f /tmp/tst2_runtime_failure_classifier.bak; git diff --quiet -- runtime/classify-runtime-failure.js || { echo \"RESTORE FAILED\"; exit 1; }; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED (invalid PROGRESS prefix no longer classified as protocol violation)\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: runtime failure classifier oracle not yet created\"; exit 0; fi'"
    expect: "BASELINE: prints BASELINE and exits 0. POST: mutating the invalid PROGRESS prefix makes the representative protocol-message test fail, prints MUTANT KILLED, restores source."
    expect_check:
      type: stdout_regex
      value: "BASELINE:|MUTANT KILLED"
  - rung: direct-test-green
    command: "cd \"$REPO_ROOT\" && sh -c 'if [ -f test/runtime-failure-classifier-oracle.test.js ]; then export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_runtime_failure_classifier_green; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/runtime-failure-classifier-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi'"
    expect: "POST: failure-classifier oracle passes. BASELINE: guarded green."
    expect_check:
      type: exit_code
      value: 0
  - rung: diff-scope
    command: "cd \"$REPO_ROOT\" && sh -c 'test -z \"$(git diff --name-only -- runtime/classify-runtime-failure.js)\" && echo SRC-CLEAN || echo SRC-DIRTY'"
    expect: prints SRC-CLEAN.
    expect_check:
      type: stdout_includes
      value: SRC-CLEAN
not_allowed: [non-test-source-change, behavior-change, new-dependency, shared-db-usage]
maker_tier: cheap
judge_tier: standard
maker_provider: null
judge_provider: null
batch_key: generate_evidence×pure_logic×runtime/failure-classification
touchset: [test/runtime-failure-classifier-oracle.test.js]
estimates:
  tokens: 30000
  evidence_cost: low
depends_on: []
unlocks: []
status: blocked
outcome:
  commit: null
  skip_reason: null
  blocked_on: "red baseline: rung 'red-then-green-mutant-kill' failed BEFORE any change (exit 2 (expected 0)) — never work on a red baseline"
  judge_verdict: null
  evidence_receipts:
    - "[baseline] red-then-green-mutant-kill: cd \"$REPO_ROOT\" && sh -c 'if [ -f test/runtime-failure-classifier-oracle.test.js ]; then git diff --quiet -- runtime/classify-runtime-failure.js || { echo \"DIRTY source\"; exit 1; }; cp runtime/classify-runtime-failure.js /tmp/tst2_runtime_failure_classifier.bak; perl -0pi -e \"s/message\\.startsWith\\('Connector emitted invalid PROGRESS\\.'\\)/message.startsWith('Connector emitted invalid PROGRESX.')/\" runtime/classify-runtime-failure.js; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_runtime_failure_classifier; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/runtime-failure-classifier-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp /tmp/tst2_runtime_failure_classifier.bak runtime/classify-runtime-failure.js; rm -f /tmp/tst2_runtime_failure_classifier.bak; git diff --quiet -- runtime/classify-runtime-failure.js || { echo \"RESTORE FAILED\"; exit 1; }; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED (invalid PROGRESS prefix no longer classified as protocol violation)\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: runtime failure classifier oracle not yet created\"; exit 0; fi' -> exit 2 (0s) FAIL: exit 2 (expected 0); exit=2 djb2=98c9d844 bytes=53 receipt=quality/receipts/tst2-runtime-failure-classifier-0013/baseline-1-red-then-green-mutant-kill.txt"
  tokens_actual: null
  lesson: "baseline rung 'red-then-green-mutant-kill' is red at repo_sha ae9cb548cea1; the oracle must be green before this packet is workable"
