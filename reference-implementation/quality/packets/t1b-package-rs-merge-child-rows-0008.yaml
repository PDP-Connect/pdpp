candidate_id: t1b-package-rs-merge-child-rows-0008
created: "2026-07-02T02:10:00.000Z"
repo_sha: "6e8d8559c36b94d41bc4c42a9a31342e60b3b334"
subsystem: server/package-rs-client
files: [server/package-rs-client.js, test/package-rs-client.test.js]
symbols:
  - mergeListEnvelopes
  - mergeEventSubListEnvelopes
  - file: server/package-rs-client.js
    parent_fn: mergeListEnvelopes
    anchor: results.forEach per-child merge callback
    line: 644
  - file: server/package-rs-client.js
    parent_fn: mergeEventSubListEnvelopes
    anchor: results.forEach per-child merge callback
    line: 831
public_surface: []
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The grant-package fan-out client merges child-RS list envelopes in two sibling functions whose per-child policy is an anonymous forEach callback (cc 10 each) CAPTURING the accumulators (merged, warnings, and in mergeListEnvelopes also the meta object it mutates): a failed child becomes a source_unavailable warning built from a connection_id||source.id||grant_id identity fallback, a successful child contributes its rows each tagged with memberSourceTag. The two callbacks are near-verbatim duplicates of one another — the per-child classification (warning-or-rows) is a pure decision braided into accumulation twice. Extracting ONE named pure step — child result in, {rows} or {warning} out — decomplects decision from accumulation, deduplicates the identity-fallback rule, and leaves each merge fn's distinct meta/partial handling exactly where it differs."
plan:
  transform_class: explicit-context-extraction
  instruction: "In server/package-rs-client.js add a named module fn childListContribution(result, child) -> { rows: array } | { warning: object } placed near memberSourceTag: failed result => { warning: { code: 'source_unavailable', message: `Source ${child.member.connection_id || child.member.source?.id || child.member.grant_id} returned ${result.status}`, source: memberSourceTag(child.member) } }; ok result => { rows: (Array.isArray(result.body?.data) ? result.body.data : (Array.isArray(result.body) ? result.body : [])).map((row) => ({ ...row, source: memberSourceTag(child.member) })) }. Rewire BOTH loops to call it: mergeListEnvelopes keeps its own accumulation (warning => meta.package.partial = true + warnings.push; rows => appended per-row: for (const row of rows) merged.push(row)); mergeEventSubListEnvelopes keeps its own (warning => warnings.push; rows => appended per-row: for (const row of rows) merged.push(row); partial derived from warnings.length at envelope build, as today). String templates and fallback order byte-identical. Do NOT touch mergeSearchEnvelopes (its per-child policy differs: scan totals/has_more) and do NOT change either fn's envelope/meta assembly. SPREAD-PUSH MANDATE — repaired 2026-07-02 per run-4: preserve the per-row append semantics VERBATIM — the for..of one-row-at-a-time push above is the documented equivalent of the original forEach per-row append; NEVER spread-push a data-sized array (merged.push(...rows) is FORBIDDEN: it can throw at the JS argument-count limit on sufficiently large valid arrays and differs from a per-row loop on sparse arrays — an observable boundary behavior change; run-4's judge REJECTed this packet (confidence 0.84) for exactly this)."
expected_quality_gain: medium
owner_attention_reduction: low
product_impact: low
priority:
  score: 90
  computed: "2026-07-02T02:10:00.000Z"
  inputs:
    mass: 10
    churn: 9
risk:
  blast_radius: local
  reversibility: trivial
  silent_wrongness_cost: medium
  property_at_risk: "fan-out merge results: row order (child order then row order), per-row source tag, source_unavailable warning message with the exact identity fallback chain, and each fn's partial-flag semantics"
authoring_evidence:
  - kind: inventory
    detail: "quality/inventory/callback-smells.json — server/package-rs-client.js:644 cc 10 captures [children,meta,warnings,merged]; :831 cc 10 captures [children,warnings,merged]; both module_refs [memberSourceTag]"
    result: "T1b class; the two callbacks are near-verbatim duplicates (read 2026-07-01 at HEAD 20969a3c6, lines 635-670 and 823-858)"
  - kind: coverage-sweep
    detail: "test/package-rs-client.test.js (37 tests) exercises /v1/streams fan-out incl. connection_id narrowing (:382-:410) and event-subscription routing (:688+)"
    result: route-level direct coverage over both merge paths
evidence_required:
  - rung: direct-test
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node --test test/package-rs-client.test.js"
    expect: "all 37 tests pass with unchanged counts, before AND after the edit"
  - rung: exact-move
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/server/package-rs-client.js"
    expect: warning template and rows-extraction expressions byte-identical inside the new fn; both loop rewires are accumulation-only; mergeSearchEnvelopes untouched
  - rung: typecheck
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit"
    expect: "0 errors"
not_allowed:
  - behavior-change
  - relocation-without-decomplecting
  - new-dependency
  - touching-mergeSearchEnvelopes
  - changing-warning-copy-or-identity-fallback-order
  - "changing-either-fn's-meta/partial-assembly"
  - spread-push-of-data-arrays
maker_tier: standard
judge_tier: strong
maker_provider: claude
judge_provider: codex
batch_key: preserve_refactor×pure_logic×server/package-rs-client
touchset: [server/package-rs-client.js]
estimates:
  tokens: 45000
  evidence_cost: low
depends_on: []
unlocks: []
status: reverted
outcome:
  commit: null
  skip_reason: null
  blocked_on: null
  judge_verdict: "codex REJECT (confidence 0.9): The diff avoids the prior forbidden spread-push and preserves the warning copy, identity fallback, and partial/meta assembly, but the successful row path is still not behavior-identical. The original loop iterated rows with for...of and pushed `{ ...row, source }` for each iterated slot; the new helper uses `rows.map(...)` first, which skips sparse-array holes and can later push `undefined` where the old code would push a source-tagged object. Because this is a preserve_refactor packet with behavior-change forbidden, the passing route tests and typecheck are insufficient."
  evidence_receipts:
    - "[baseline] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node --test test/package-rs-client.test.js -> exit 0 (0s) PASS; exit=0 djb2=b5854dcc bytes=3451 receipt=quality/receipts/t1b-package-rs-merge-child-rows-0008/baseline-1-direct-test.txt"
    - "[baseline] exact-move: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/server/package-rs-client.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-package-rs-merge-child-rows-0008/baseline-2-exact-move.txt"
    - "[baseline] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (5s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-package-rs-merge-child-rows-0008/baseline-3-typecheck.txt"
    - "[post] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node --test test/package-rs-client.test.js -> exit 0 (0s) PASS; exit=0 djb2=3a81e730 bytes=3451 receipt=quality/receipts/t1b-package-rs-merge-child-rows-0008/post-1-direct-test.txt"
    - "[post] exact-move: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/server/package-rs-client.js -> exit 0 (0s) PASS; exit=0 djb2=c6f8d1be bytes=2621 receipt=quality/receipts/t1b-package-rs-merge-child-rows-0008/post-2-exact-move.txt"
    - "[post] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (5s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/t1b-package-rs-merge-child-rows-0008/post-3-typecheck.txt"
  tokens_actual: 79532
  lesson: "judge rejected: The diff avoids the prior forbidden spread-push and preserves the warning copy, identity fallback, and partial/meta assembly, but the successful row path is still not behavior-identical. The original loop iterated rows with for...of and pus…"
resets:
  - at: "2026-07-02T08:17:22.020Z"
    from_status: reverted
    reason: "spread-push mandate folded in per run-4 REJECT (JS argument-limit/sparse-array divergence); per-row append made explicit, not_allowed += spread-push-of-data-arrays"
