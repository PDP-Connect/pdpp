candidate_id: tst2-cli-parse-args-terminator-0034
created: "2026-07-06T23:54:00.000Z"
repo_sha: current-tree-no-git-sandbox
subsystem: packages/cli/args
files: ["../packages/cli/test/parse-args-terminator-oracle.test.js", "../packages/cli/src/ref/args.js"]
symbols: [parseArgs, requirePositional]
public_surface:
  - CLI shared argument parser treats -- as a terminator and preserves following dash-prefixed values as positionals
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The published CLI shares parseArgs across ref and read commands. Losing POSIX-style -- handling makes legitimate dash-prefixed record ids, paths, and query text unreachable."
plan:
  transform_class: add-seeded-mutation-oracle
  instruction: "Create packages/cli/test/parse-args-terminator-oracle.test.js. Import parseArgs and requirePositional from '../src/ref/args.js' plus PdppUsageError. Assert parseArgs(['--format','json','--','--literal','value']) returns flags.format 'json' and positionals ['--literal','value']; assert requirePositional([],0,'stream') throws PdppUsageError with exitCode 64. Touch no source."
expected_quality_gain: medium
owner_attention_reduction: medium
product_impact: medium
risk:
  blast_radius: subsystem
  reversibility: trivial
  silent_wrongness_cost: medium
  property_at_risk: dash-prefixed user values after -- must not be reinterpreted as flags
authoring_evidence:
  - kind: coverage-sweep
    detail: "rg -n 'parseArgs|-- literal|terminator|requirePositional' packages/cli/test"
    result: no direct shared parser terminator oracle found
  - kind: code-read
    detail: packages/cli/src/ref/args.js parseArgs pushes argv.slice(i + 1) when arg is --
    result: pure parser seam
evidence_required:
  - rung: red-then-green-mutant-kill
    command: "cd \"$REPO_ROOT/../packages/cli\" && if [ -f test/parse-args-terminator-oracle.test.js ]; then src=src/ref/args.js; bak=$REPO_ROOT/.pdpp_hone_tst2_0034_cli_args.bak; cp \"$src\" \"$bak\"; perl -0pi -e \"s/positionals\\\\.push\\\\(\\\\.\\\\.\\\\.argv\\\\.slice\\\\(i \\\\+ 1\\\\)\\\\);/positionals.push(...argv.slice(i));/\" \"$src\"; if cmp -s \"$src\" \"$bak\"; then cp \"$bak\" \"$src\"; rm -f \"$bak\"; echo \"MUTATION NOT APPLIED\"; exit 1; fi; rc=0; timeout 300 node --test --test-force-exit test/parse-args-terminator-oracle.test.js 2>&1 || rc=1; cp \"$bak\" \"$src\"; cmp -s \"$src\" \"$bak\" || { echo \"RESTORE FAILED\"; rm -f \"$bak\"; exit 1; }; rm -f \"$bak\"; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: parse args oracle not yet created\"; exit 0; fi"
    expect: BASELINE guarded green while absent. POST including the -- terminator in positionals must fail the new parser oracle and print MUTANT KILLED after source restore.
    expect_check:
      type: stdout_regex
      value: "BASELINE:|MUTANT KILLED"
  - rung: direct-test-green
    command: "cd \"$REPO_ROOT/../packages/cli\" && if [ -f test/parse-args-terminator-oracle.test.js ]; then rc=0; timeout 300 node --test --test-force-exit test/parse-args-terminator-oracle.test.js 2>&1 || rc=1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi"
    expect: "POST: the parseArgs oracle passes. BASELINE: guarded green while absent."
    expect_check:
      type: exit_code
      value: 0
not_allowed: [non-test-source-change, production-source-change, behavior-change, new-dependency, git-command]
maker_tier: cheap
judge_tier: standard
maker_provider: codex
judge_provider: null
batch_key: generate_evidence×pure_logic×packages/cli/args
touchset: ["../packages/cli/test/parse-args-terminator-oracle.test.js"]
estimates:
  tokens: 24000
  evidence_cost: low
depends_on: []
unlocks: []
status: skipped
outcome:
  commit: null
  skip_reason: unactionable(requested exitCode 64 contradicts current PdppUsageError exitCode 2 while production-source-change is forbidden)
  blocked_on: null
  judge_verdict: null
  evidence_receipts:
    - "[baseline] red-then-green-mutant-kill: cd \"$REPO_ROOT/../packages/cli\" && if [ -f test/parse-args-terminator-oracle.test.js ]; then src=src/ref/args.js; bak=$REPO_ROOT/.pdpp_hone_tst2_0034_cli_args.bak; cp \"$src\" \"$bak\"; perl -0pi -e \"s/positionals\\\\.push\\\\(\\\\.\\\\.\\\\.argv\\\\.slice\\\\(i \\\\+ 1\\\\)\\\\);/positionals.push(...argv.slice(i));/\" \"$src\"; if cmp -s \"$src\" \"$bak\"; then cp \"$bak\" \"$src\"; rm -f \"$bak\"; echo \"MUTATION NOT APPLIED\"; exit 1; fi; rc=0; timeout 300 node --test --test-force-exit test/parse-args-terminator-oracle.test.js 2>&1 || rc=1; cp \"$bak\" \"$src\"; cmp -s \"$src\" \"$bak\" || { echo \"RESTORE FAILED\"; rm -f \"$bak\"; exit 1; }; rm -f \"$bak\"; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: parse args oracle not yet created\"; exit 0; fi -> exit 0 (0s) PASS; exit=0 djb2=328d96e5 bytes=44 receipt=quality/receipts/tst2-cli-parse-args-terminator-0034/baseline-1-red-then-green-mutant-kill.txt"
    - "[baseline] direct-test-green: cd \"$REPO_ROOT/../packages/cli\" && if [ -f test/parse-args-terminator-oracle.test.js ]; then rc=0; timeout 300 node --test --test-force-exit test/parse-args-terminator-oracle.test.js 2>&1 || rc=1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi -> exit 0 (0s) PASS; exit=0 djb2=caf12c67 bytes=31 receipt=quality/receipts/tst2-cli-parse-args-terminator-0034/baseline-2-direct-test-green.txt"
  tokens_actual: 37352
  lesson: maker (codex) could not act on plan.instruction as written — rewrite the instruction before retrying
routing_class: certified-mechanical
