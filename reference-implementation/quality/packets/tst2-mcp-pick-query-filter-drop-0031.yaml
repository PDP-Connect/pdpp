candidate_id: tst2-mcp-pick-query-filter-drop-0031
created: "2026-07-06T23:51:00.000Z"
repo_sha: current-tree-no-git-sandbox
subsystem: packages/mcp-server/query-affordances
files:
  - "../packages/mcp-server/test/pick-query-filter-drop-oracle.test.js"
  - "../packages/mcp-server/src/tools.js"
symbols: [pickQuery]
public_surface:
  - MCP query_records/fetch argument forwarding must never leak raw filter or expand_limit objects as flat query params
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "Typed filters and expand limits are encoded through dedicated bracket-query builders. A raw pickQuery leak would silently send filter=[object Object] or expand_limit=[object Object], creating false-negative reads that look successful to MCP clients."
plan:
  transform_class: add-seeded-mutation-oracle
  instruction: "Create packages/mcp-server/test/pick-query-filter-drop-oracle.test.js. Import { __internal } from '../src/tools.js' and call pickQuery with args containing stream, filter:{sent_at:{gte:'2026-01-01'}}, expand_limit:{line_items:2}, cursor:'cur_1', limit:5, connection_id:'cin_a'; supportedKeys must include every key. Assert the returned object keeps cursor, limit, connection_id but omits stream, filter, and expand_limit. Touch no source."
expected_quality_gain: high
owner_attention_reduction: medium
product_impact: medium
risk:
  blast_radius: subsystem
  reversibility: trivial
  silent_wrongness_cost: high
  property_at_risk: raw nested query objects must not be forwarded as no-op flat params
authoring_evidence:
  - kind: coverage-sweep
    detail: "rg -n 'pickQuery|filter drop|expand_limit.*pickQuery|raw filter' packages/mcp-server/test"
    result: no direct __internal.pickQuery oracle found; broader integration tests cover selected routes only
  - kind: code-read
    detail: "packages/mcp-server/src/tools.js pickQuery skips stream, filter, and expand_limit before forwarding supported scalar args"
    result: pure unit seam exported through __internal
evidence_required:
  - rung: red-then-green-mutant-kill
    command: "cd \"$REPO_ROOT/../packages/mcp-server\" && if [ -f test/pick-query-filter-drop-oracle.test.js ]; then src=src/tools.js; bak=$REPO_ROOT/.pdpp_hone_tst2_0031_mcp_tools.bak; cp \"$src\" \"$bak\"; perl -0pi -e \"s/if \\\\(key === \\\\x27filter\\\\x27\\\\) continue;/if (false && key === \\\\x27filter\\\\x27) continue;/\" \"$src\"; if cmp -s \"$src\" \"$bak\"; then cp \"$bak\" \"$src\"; rm -f \"$bak\"; echo \"MUTATION NOT APPLIED\"; exit 1; fi; rc=0; timeout 300 node --test --test-force-exit test/pick-query-filter-drop-oracle.test.js 2>&1 || rc=1; cp \"$bak\" \"$src\"; cmp -s \"$src\" \"$bak\" || { echo \"RESTORE FAILED\"; rm -f \"$bak\"; exit 1; }; rm -f \"$bak\"; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: pickQuery oracle not yet created\"; exit 0; fi"
    expect: BASELINE guarded green while absent. POST disabling the filter skip must leak filter into the picked object and make the oracle print MUTANT KILLED after byte-clean restore.
    expect_check:
      type: stdout_regex
      value: "BASELINE:|MUTANT KILLED"
  - rung: direct-test-green
    command: "cd \"$REPO_ROOT/../packages/mcp-server\" && if [ -f test/pick-query-filter-drop-oracle.test.js ]; then rc=0; timeout 300 node --test --test-force-exit test/pick-query-filter-drop-oracle.test.js 2>&1 || rc=1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi"
    expect: "POST: the pure pickQuery oracle passes. BASELINE: guarded green while absent."
    expect_check:
      type: exit_code
      value: 0
not_allowed: [non-test-source-change, production-source-change, behavior-change, new-dependency, git-command]
maker_tier: cheap
judge_tier: standard
maker_provider: codex
judge_provider: claude
batch_key: generate_evidence×pure_logic×packages/mcp-server/query-affordances
touchset: ["../packages/mcp-server/test/pick-query-filter-drop-oracle.test.js"]
estimates:
  tokens: 28000
  evidence_cost: low
depends_on: []
unlocks: []
status: landed
outcome:
  commit: "43a721e991c7d1f3e3329604091d224a3d1bb2d5"
  skip_reason: null
  blocked_on: null
  judge_verdict: "claude PASS (confidence 0.88): The diff adds exactly one test file matching the plan's instruction verbatim (same args, same supportedKeys construction, same assertions), touches no production source, and violates none of the not_allowed constraints. The mutant-kill evidence is genuine: baseline correctly no-ops absent the test, the mutant (disabling the filter-skip) makes the oracle fail with a diff showing leaked filter object, and the source is restored before the green re-run passes cleanly — this is real evidence the test's assertions are load-bearing rather than tautological."
  evidence_receipts:
    - "[baseline] red-then-green-mutant-kill: cd \"$REPO_ROOT/../packages/mcp-server\" && if [ -f test/pick-query-filter-drop-oracle.test.js ]; then src=src/tools.js; bak=$REPO_ROOT/.pdpp_hone_tst2_0031_mcp_tools.bak; cp \"$src\" \"$bak\"; perl -0pi -e \"s/if \\\\(key === \\\\x27filter\\\\x27\\\\) continue;/if (false && key === \\\\x27filter\\\\x27) continue;/\" \"$src\"; if cmp -s \"$src\" \"$bak\"; then cp \"$bak\" \"$src\"; rm -f \"$bak\"; echo \"MUTATION NOT APPLIED\"; exit 1; fi; rc=0; timeout 300 node --test --test-force-exit test/pick-query-filter-drop-oracle.test.js 2>&1 || rc=1; cp \"$bak\" \"$src\"; cmp -s \"$src\" \"$bak\" || { echo \"RESTORE FAILED\"; rm -f \"$bak\"; exit 1; }; rm -f \"$bak\"; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: pickQuery oracle not yet created\"; exit 0; fi -> exit 0 (0s, timeout 1800s) PASS; exit=0 timeout=1800s djb2=de01f47a bytes=43 receipt=quality/receipts/tst2-mcp-pick-query-filter-drop-0031/baseline-1-red-then-green-mutant-kill.txt"
    - "[baseline] direct-test-green: cd \"$REPO_ROOT/../packages/mcp-server\" && if [ -f test/pick-query-filter-drop-oracle.test.js ]; then rc=0; timeout 300 node --test --test-force-exit test/pick-query-filter-drop-oracle.test.js 2>&1 || rc=1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi -> exit 0 (0s, timeout 1800s) PASS; exit=0 timeout=1800s djb2=caf12c67 bytes=31 receipt=quality/receipts/tst2-mcp-pick-query-filter-drop-0031/baseline-2-direct-test-green.txt"
    - "[post] red-then-green-mutant-kill: cd \"$REPO_ROOT/../packages/mcp-server\" && if [ -f test/pick-query-filter-drop-oracle.test.js ]; then src=src/tools.js; bak=$REPO_ROOT/.pdpp_hone_tst2_0031_mcp_tools.bak; cp \"$src\" \"$bak\"; perl -0pi -e \"s/if \\\\(key === \\\\x27filter\\\\x27\\\\) continue;/if (false && key === \\\\x27filter\\\\x27) continue;/\" \"$src\"; if cmp -s \"$src\" \"$bak\"; then cp \"$bak\" \"$src\"; rm -f \"$bak\"; echo \"MUTATION NOT APPLIED\"; exit 1; fi; rc=0; timeout 300 node --test --test-force-exit test/pick-query-filter-drop-oracle.test.js 2>&1 || rc=1; cp \"$bak\" \"$src\"; cmp -s \"$src\" \"$bak\" || { echo \"RESTORE FAILED\"; rm -f \"$bak\"; exit 1; }; rm -f \"$bak\"; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: pickQuery oracle not yet created\"; exit 0; fi -> exit 0 (0s, timeout 1800s) PASS; exit=0 timeout=1800s djb2=9f71f154 bytes=1324 receipt=quality/receipts/tst2-mcp-pick-query-filter-drop-0031/post-1-red-then-green-mutant-kill.txt"
    - "[post] direct-test-green: cd \"$REPO_ROOT/../packages/mcp-server\" && if [ -f test/pick-query-filter-drop-oracle.test.js ]; then rc=0; timeout 300 node --test --test-force-exit test/pick-query-filter-drop-oracle.test.js 2>&1 || rc=1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi -> exit 0 (0s, timeout 1800s) PASS; exit=0 timeout=1800s djb2=62bf417c bytes=196 receipt=quality/receipts/tst2-mcp-pick-query-filter-drop-0031/post-2-direct-test-green.txt"
  tokens_actual: 53769
  lesson: null
routing_class: certified-mechanical
