created: "2026-07-02T09:53:40.301Z"
repo_sha: a86ed53d654801067446ce2db1315e8115c60420
subsystem: runtime
behavior_status: likely_intended
ownership: OWN
action: generate_evidence
proof_class: judgment_first
execution_gate: autonomous
expected_quality_gain: high
owner_attention_reduction: low
product_impact: low
priority:
  score: 94
  computed: "2026-07-02T09:53:40.301Z"
  inputs:
    mass: 189
    churn: 63
not_allowed:
  - behavior-change
  - implementation-change
  - new-dependency
  - wall-clock-sleep-as-sync-primitive
  - pinning-cross-stream-global-order
  - new-helper-files-outside-touchset
  - existence-assertions-where-order-is-the-contract
maker_tier: standard
judge_tier: strong
maker_provider: codex
judge_provider: claude
batch_key: generate_evidence×judgment_first×runtime
estimates:
  tokens: 55000
  evidence_cost: medium
depends_on: []
status: landed
outcome:
  commit: e2ec7647d6b4480315e0b161b17d3e76a6a37875
  skip_reason: null
  blocked_on: null
  judge_verdict: "claude PASS (confidence 0.78): The diff is test-only (matches touchset), leaves runtime/index.js at byte-identity per baseline/post receipts, and typechecks clean. The named test 'INTERACTION arm: resolveByRequestId completes before the assistance resolution event and stdin response' exists verbatim, asserts strict log-index order (not existence) via a synchronous shared sequence log with held barriers for recordInteractionRequest/resolveByRequestId, and the red-then-green rung's post receipt shows the seeded await-drop mutation flips the timeout arm's ordering assertion to failing before restore — satisfying the failing_test_named check pattern present in the output. No sleeps, no cross-stream global-order pinning, no new files outside the touchset, and no new dependencies were introduced."
  evidence_receipts:
    - "[baseline] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-interaction-timeout.test.js ]; then node --test test/runtime-msg-order-interaction-timeout.test.js test/runtime-handle-msg-dispatch.test.js; else node --test test/runtime-handle-msg-dispatch.test.js; fi -> exit 0 (1s) PASS; exit=0 djb2=6d74c561 bytes=877 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/baseline-1-direct-test.txt"
    - "[baseline] red-then-green-interaction-resolve-order: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-interaction-timeout.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/await attentionWriter.resolveByRequestId(msg.request_id, responseStatus);/attentionWriter.resolveByRequestId(msg.request_id, responseStatus);/' runtime/index.js || exit 1; rc=0; node --test test/runtime-msg-order-interaction-timeout.test.js || rc=1; git checkout -- runtime/index.js; [ \"$rc\" -eq 1 ]; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/baseline-2-red-then-green-interaction-resolve-order.txt"
    - "[baseline] byte-identity: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/baseline-3-byte-identity.txt"
    - "[baseline] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (4s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/baseline-4-typecheck.txt"
    - "[post] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-interaction-timeout.test.js ]; then node --test test/runtime-msg-order-interaction-timeout.test.js test/runtime-handle-msg-dispatch.test.js; else node --test test/runtime-handle-msg-dispatch.test.js; fi -> exit 0 (1s) PASS; exit=0 djb2=25d2aef6 bytes=1120 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/post-1-direct-test.txt"
    - "[post] red-then-green-interaction-resolve-order: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-interaction-timeout.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/await attentionWriter.resolveByRequestId(msg.request_id, responseStatus);/attentionWriter.resolveByRequestId(msg.request_id, responseStatus);/' runtime/index.js || exit 1; rc=0; node --test test/runtime-msg-order-interaction-timeout.test.js || rc=1; git checkout -- runtime/index.js; [ \"$rc\" -eq 1 ]; else echo oracle-not-yet-authored; fi -> exit 0 (1s) PASS; exit=0 djb2=31df5d8c bytes=3372 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/post-2-red-then-green-interaction-resolve-order.txt"
    - "[post] byte-identity: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/post-3-byte-identity.txt"
    - "[post] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (4s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-interaction-timeout-0002d/post-4-typecheck.txt"
  tokens_actual: 223580
  lesson: null
candidate_id: hm-await-order-interaction-timeout-0002d
files:
  - runtime/index.js
  - test/runtime-handle-msg-dispatch.test.js
  - test/runtime-msg-order-interaction-timeout.test.js
symbols:
  - runConnector
  - file: runtime/index.js
    parent_fn: handleMsg
    anchor: "case 'INTERACTION' — awaited chain: run.interaction_required (:3021) → run.assistance_requested (:3042) → attentionWriter.recordInteractionRequest (:3061) → Promise.race(response|timeout|violation) (:3085) → run.interaction_completed (:3099) → attentionWriter.resolveByRequestId (:3124) → assistance resolution event → known-gap (non-success) → stdin response write"
    line: 3005
public_surface:
  - "spine event_types run.interaction_required, run.assistance_requested, run.interaction_completed, run.assistance_resolved family (timeline projection order)"
  - "connector stdin protocol: the INTERACTION_RESPONSE line is written only after the attention store and spine resolution are complete"
why_this_matters: "ONE contract, decomposed out of hm-awaited-order-oracle-0002 (4 failed attempts; superseded): the INTERACTION arm's awaited lifecycle chain (runtime/index.js:3005-3173). The dashboard's attention projection and the connector's stdin protocol both depend on this order; the run-5 judge additionally REVISEd that the timeout arm omitted the assistance-resolution/known-gap order BEFORE the stdin write — that demand is folded in here as a first-class assertion. Extraction packet hm-interaction-lifecycle-extract-0009 cites this ordering as its oracle. Unlike the original packet's claim that no single-line mutation covers INTERACTION, the await-drop on resolveByRequestId(msg.request_id, responseStatus) (:3124) is a verified-unique sed target whose kill is deterministic under the held-fake-store harness."
plan:
  transform_class: direct-unit-oracle-generation
  instruction: "Add test/runtime-msg-order-interaction-timeout.test.js pinning EXACTLY ONE contract: the INTERACTION lifecycle order, exercised in a success arm and a timeout arm. Copy the landed hm-0001 harness (test/runtime-handle-msg-dispatch.test.js, status landed, all pins green): inline writeStub for the temp-file fake connector (:103-140), readline flowing-mode stdin via createInterface({ input: process.stdin, crlfDelay: Infinity }) (:125), idle setInterval keep-alive tail (:116), injectable onInteraction passed to runConnector (:189). SELF-CONTAINED SINGLE FILE: ALL harness code (stub writer, RS stub with the shared sequence log, sentinel plumbing) lives INSIDE the one touchset test file — creating ANY other file is a touchset violation and the engine reverts ALL work fail-closed (run-3 lesson). SHARED MONOTONIC SEQUENCE LOG doctrine (the run-5 judge's core demand): one plain array; EVERY mark is appended SYNCHRONOUSLY at the moment of observation (the spine sink pushes at emit-invocation, the RS stub pushes 'request' at receipt and 'completed' when the response is released and fully written, connector-side observations are written synchronously with writeFileSync and folded into the log) — async marks weaken the log and were REVISEd in run-5. Assertions compare LOG INDEX ORDER, never existence (run-2/run-5 refusal class: the run-5 STATE mutant SURVIVED an existence-shaped oracle) and never wall-clock margins. HOLD-the-boundary: the RS stub HOLDS a response until the test releases it through an explicit barrier promise — a hold is not a sleep; green-path ordering is enforced by the runtime's await, never by hold length. INTERACTION stdin plumbing (the run-3 lesson): hm-0001's stub reads only the START line via rl.once('line') and closes the interface, which PAUSES stdin — that gap caused run-3's two red tests; here the stub MUST keep the readline interface OPEN with rl.on('line', ...) and echo each observed stdin line synchronously (writeFileSync to a sentinel) so the test can log 'stdin:response-observed' and place it in the shared sequence order. Inject a fake attention store via opts.connectorAttentionStore (merged into createAttentionWriter at runtime/index.js:2238-2239) whose recordInteractionRequest and resolveByRequestId push ':called' synchronously and ':completed' when the test releases their HELD promise (barrier, not timer); inject the spine sink to push event_types synchronously at emit. SUCCESS ARM — onInteraction resolves after a tick with {type:'INTERACTION_RESPONSE', status:'completed'} (injectable onInteraction, hm-0001 :189); assert the full index order: run.interaction_required → run.assistance_requested → recordInteractionRequest:completed → run.interaction_completed → resolveByRequestId:completed → assistance resolution event → stdin:response-observed. TIMEOUT ARM — msg.timeout_seconds: 1 with onInteraction never resolving (the 1s timer is the CONTRACT under test, not a sync primitive): assert responseStatus 'timeout' round-trips to the connector's stdin, and that the assistance-resolution event AND the interaction_required known-gap surfacing are ordered BEFORE 'stdin:response-observed' in the same log (the run-5 judge's explicit demand). MANDATORY named test (exactly): 'INTERACTION arm: resolveByRequestId completes before the assistance resolution event and stdin response' carrying the resolveByRequestId:completed → assistance-resolution-event → stdin order assertions (either arm may host it; it must be the one the seeded mutant flips). Kill mechanics: under the seeded mutation (await dropped from resolveByRequestId at :3124) the runtime's continuation emits the assistance resolution event synchronously before the held resolveByRequestId promise can complete — index order inverts deterministically in-process, no OS race. PROCESS REQUIREMENT: iterate node --test on your file DURING authoring and only conclude when it is green against UNMUTATED code (this oracle CHARACTERIZES current behavior); then verify the kill yourself: seed the packet's sed mutation locally, confirm your named test (and ONLY an order assertion) goes red, restore runtime/index.js to byte-identity before handing back (the byte-identity rung enforces it). Never hand back a red file, and never hand back a file whose named test survives the seeded mutant — the red-then-green rung exits nonzero and the packet fails. Do NOT leave runtime/index.js modified; the engine's rung seeds and restores it itself."
risk:
  blast_radius: local
  reversibility: trivial
  silent_wrongness_cost: medium
  property_at_risk: "a fake attention store that resolves synchronously (or logs only ':called') cannot distinguish awaited from un-awaited completion — the mutant survives (the run-5 STATE survival class transplanted to INTERACTION); the held-promise barrier with synchronous ':completed' marks is load-bearing"
authoring_evidence:
  - kind: code-read
    detail: "runtime/index.js case 'INTERACTION' re-read 2026-07-02 at a86ed53d6: :3021 interaction_required emit, :3042 assistance_requested emit, :3061 await recordInteractionRequest, :3085 Promise.race, :3099 interaction_completed emit, :3124 await resolveByRequestId(msg.request_id, responseStatus); attention-store injection seam :2238-2239 (opts.connectorAttentionStore → createAttentionWriter)"
    result: awaited chain and injection seam confirmed at current line numbers
  - kind: grep-unique
    detail: "grep -cF 'await attentionWriter.resolveByRequestId(msg.request_id, responseStatus);' runtime/index.js (2026-07-02, a86ed53d6)"
    result: "1 — the red-then-green sed target is verified unique (the other resolveByRequestId call at :2257 takes different arguments)"
  - kind: baseline-run
    detail: "node --test test/runtime-handle-msg-dispatch.test.js test/runtime-pipe-resilience.test.js test/event-spine.test.js (2026-07-02, repo_sha a86ed53d6)"
    result: "71/71 pass, 0 fail, 0 skipped — harness-pattern suites verified green on the current tree"
  - kind: fold-back
    detail: "hm-awaited-order-oracle-0002 judge history distilled 2026-07-02 (4 attempts, ~$22, lesson \"packet bar not reachable by this maker\"): run-2 judge refused existence-shaped assertions for DONE flush-before-EOF and INTERACTION attention/stdin ordering and refused the MISSING RECORD batch-boundary contract; run-3 ended skipped(touchset-violation: probe-driver.mjs/probe-stdin.mjs outside touchset); run-5 codex REVISE 0.98 then 0.88: (i) the seeded STATE flush-order mutant SURVIVED the oracle (receipt post-r1-2-red-then-green-state-flush-order.txt: exit 0, all tests green under the dropped-await mutation — the oracle asserted existence, not order), (ii) the rung exit code was not probative because the trailing git checkout masked node --test failures, (iii) the timeout INTERACTION ordering omitted the assistance-resolution/known-gap order before the stdin write, (iv) an async stdin mark weakened the shared monotonic log"
    result: "this decomposition pins ONE contract per packet; every red-then-green rung captures rc BEFORE restore and exits 0 ONLY when the seeded run failed, with the named failure enforced deterministically via expect_check failing_test_named; all order assertions read ONE shared monotonic sequence log with SYNCHRONOUS marks"
evidence_required:
  - rung: direct-test
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-interaction-timeout.test.js ]; then node --test test/runtime-msg-order-interaction-timeout.test.js test/runtime-handle-msg-dispatch.test.js; else node --test test/runtime-handle-msg-dispatch.test.js; fi"
    expect: "baseline (new file absent): landed hm-0001 dispatch-guards oracle (7 tests, incl. the INTERACTION plumbing this packet extends) green; post: the new single-contract oracle green alongside it, 0 fail, no new skips"
    expect_check:
      type: exit_code
      value: 0
  - rung: red-then-green-interaction-resolve-order
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-interaction-timeout.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/await attentionWriter.resolveByRequestId(msg.request_id, responseStatus);/attentionWriter.resolveByRequestId(msg.request_id, responseStatus);/' runtime/index.js || exit 1; rc=0; node --test test/runtime-msg-order-interaction-timeout.test.js || rc=1; git checkout -- runtime/index.js; [ \"$rc\" -eq 1 ]; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent, exit 0). Post: the rung seeds the dropped-await mutation (runtime/index.js:3124; sed target verified unique 2026-07-02), runs the oracle capturing rc BEFORE restoring the file, restores, and exits 0 ONLY IF the seeded run FAILED — failing output visible in the receipt, and the engine requires the failure line to name 'INTERACTION arm: resolveByRequestId completes before the assistance resolution event and stdin response'"
    expect_check:
      type: failing_test_named
      value: resolveByRequestId completes before the assistance resolution
  - rung: byte-identity
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js"
    expect: empty — this packet changes tests only (runtime/index.js is not in the touchset); also proves the red-then-green rung restored its seed
  - rung: typecheck
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit"
    expect: "0 errors"
    expect_check:
      type: exit_code
      value: 0
touchset: [test/runtime-msg-order-interaction-timeout.test.js]
unlocks: [hm-interaction-lifecycle-extract-0009, hm-dispatch-table-coherence-0011]
resets:
  - at: "2026-07-02T13:44:58.691Z"
    from_status: skipped
    reason: "retry cross-pair: run-7 SKIP claimed 'no spine-sink injection seam in runConnector' but read-only verification shows plan cites the EXISTING injectable onInteraction (runtime/index.js:1967) and the spine sink is realizable inside the single touchset test file (module mock or RS-stub receipt marks) without implementation-change; maker-drift skip, not packet defect"
