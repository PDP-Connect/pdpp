candidate_id: hm-dispatch-guards-oracle-0001
created: "2026-07-02T03:38:00.000Z"
repo_sha: e7ede6e50ea9b2f3d60ba908862a846c833d7a5c
subsystem: runtime
files: [runtime/index.js, test/event-spine.test.js, test/runtime-cancel-run.test.js]
symbols:
  - runConnector
  - file: runtime/index.js
    parent_fn: runConnector
    anchor: async function handleMsg(msg) — switch (msg.type) dispatch frame
    line: 2928
public_surface:
  - "run failure messages (wire-REACHABLE guards, redesigned 2026-07-02 per run-2): 'Connector emitted unknown message type', 'Connector emitted <type> after DONE', 'Connector emitted <type> while waiting for INTERACTION_RESPONSE' (the rl-interceptor, runtime/index.js:3579 — the reachable concurrency guard), 'Connector emitted duplicate ASSISTANCE.assistance_request_id', 'Connector emitted ASSISTANCE_STATUS for unknown assistance_request_id'"
  - spine event_type run.detail_gap_terminal (timeline projection)
  - "documented-not-pinned: 'Connector emitted INTERACTION while already waiting' (runtime/index.js:3013) — wire-UNREACHABLE defensive depth; keep-or-remove queued for owner ratification (docs/research/b-contract-ratification-queue.md row 5)"
behavior_status: likely_intended
ownership: OWN
action: generate_evidence
proof_class: judgment_first
execution_gate: autonomous
why_this_matters: "handleMsg (runtime/index.js:2928, cc 194 — the largest single complexity mass in the owned core, agenda item rt-index-handlemsg-a2-decomplect) is about to be decomplected into named per-family handlers. Its dispatch FRAME has behaviors no test pins by name: (1) unknown msg.type throws 'Connector emitted unknown message type: X' and fails the run; (2) any message after DONE throws 'Connector emitted <type> after DONE' (the doneMessage guard prologue — the 'after DONE' matches in existing tests pin checkpoint persistence, not this guard string); (3) duplicate ASSISTANCE.assistance_request_id throws; (4) ASSISTANCE_STATUS with an unknown assistance_request_id throws; (5) run.detail_gap_terminal (the §10-A terminal-gap transition, handleMsg:3347) is emitted by no pinned path. REDESIGNED 2026-07-02 per run-2: the original packet also demanded pinning the handleMsg-internal concurrency guard 'Connector emitted INTERACTION while already waiting' (:3013), but the empirical consensus (run-1 maker pinned the interceptor string instead; run-2 maker returned no oracle for it; static analysis of the serial pump + the rl.on('line') interceptor) is that :3013 is wire-UNREACHABLE — a second INTERACTION arriving while one is pending is consumed by failPendingInteraction at :3577/:3579 with 'Connector emitted INTERACTION while waiting for INTERACTION_RESPONSE' (which also calls terminateChild, :2859), and the serial pump never dispatches a queued second INTERACTION while pendingInteraction is set. The packet therefore pins the REACHABLE frame (including the interceptor guard in the focused dispatch oracle — it is already pinned by collection-profile/cli/event-spine, which corroborates the routing) and DOCUMENTS the :3013 dead guard for owner ratification instead of chasing it. Restructuring the switch without these pins risks silently changing protocol-violation semantics."
plan:
  transform_class: direct-unit-oracle-generation
  instruction: "REDESIGNED 2026-07-02 per run-2 (this supersedes run-1's R1, which demanded the :3013 guard be reached from the wire): EMPIRICAL FINDING — the handleMsg guard 'Connector emitted INTERACTION while already waiting' (runtime/index.js:3013; also referenced by classifyRuntimeFailure :1054) is wire-UNREACHABLE. Provenance: run-1's maker could only trigger the rl-interceptor string; run-2's maker returned no oracle for :3013 after the reachability demand; static analysis concurs — a second INTERACTION line ARRIVING while one is pending is consumed by failPendingInteraction in rl.on('line') (:3577/:3579, message 'Connector emitted INTERACTION while waiting for INTERACTION_RESPONSE', and failPendingInteraction itself calls terminateChild at :2859), and the serial pump (processNext :2863-2926 awaits handleMsg inline; the INTERACTION arm awaits its race and clears pendingInteraction before returning) never dispatches a queued second INTERACTION while pendingInteraction is set. Do NOT attempt to reach :3013 from the wire; do NOT present the :3577 string as the :3013 guard (they are distinct surfaces); do NOT remove or edit the :3013 guard (keep-or-remove is queued for the owner: docs/research/b-contract-ratification-queue.md row 5 — human decision needed). The dead-guard-inventory rung documents this finding into the receipts. --- TASK: Add test/runtime-handle-msg-dispatch.test.js driving the REAL dispatch path: import { runConnector } from '../runtime/index.js', follow the fake-connector harness style of test/runtime-cancel-run.test.js (temp connector script written with writeFileSync emitting JSONL on stdout; stub RS http server via node:http createServer capturing ingest POSTs) and the spine read-back style of test/event-spine.test.js where a persisted spine assertion is needed. Pin by name: (1) connector emits {type:'BOGUS'} → runConnector outcome is a failed run whose failure reason/message contains 'Connector emitted unknown message type: BOGUS'; (2) connector emits DONE then a RECORD → failure contains 'Connector emitted RECORD after DONE'; also DONE then malformed JSON line → 'Connector emitted invalid JSONL after DONE'; (3) connector emits INTERACTION, then a second INTERACTION while the first is unresolved (onInteraction returns a never-resolving promise, second message sent immediately) → failure contains 'Connector emitted INTERACTION while waiting for INTERACTION_RESPONSE' (the wire-REACHABLE interceptor guard; already pinned by collection-profile/cli/event-spine — corroborating the routing — and pinned here so the dispatch-frame oracle is self-contained) AND the child process is terminated (assert the child exits — e.g. the connector script would have written a sentinel file after a delay and does not; terminateChild fires inside failPendingInteraction); (4) two ASSISTANCE messages with the same assistance_request_id → 'Connector emitted duplicate ASSISTANCE.assistance_request_id'; (5) ASSISTANCE_STATUS with request id never opened → 'Connector emitted ASSISTANCE_STATUS for unknown assistance_request_id'; (6) DETAIL_GAP whose last_error is non-transient (http_status 404) against an injected opts.detailGapStore fake whose upsertPendingGap/maybeTerminateGap path exhausts the recovery budget → a run.detail_gap_terminal spine event is emitted carrying gap_id and terminal_reason (use the full in-memory server harness from test/event-spine.test.js for spine read-back, or capture via the injected store + spine sink the way event-spine asserts other run.* events). Assert named fields and exact guard strings, not deep-equal blobs. PROCESS (non-negotiable): iterate node --test test/runtime-handle-msg-dispatch.test.js DURING authoring until green on unmutated code; an EMPTY or vacuous oracle file is an automatic REVISE — run-2 handed back an empty file and was refused twice at confidence 0.99; the direct-test rung must show all six named pins passing. Seeded-regression discrimination is ENGINE-RUN (makers cannot write receipts — quality/ is forbidden to them): six red-then-green-* rungs each seed one guard mutation in runtime/index.js via sed, run this oracle expecting the matching test to FAIL, capture that failing run into receipts, and restore. Author each test to pin the exact string/event so its mutation reds it. Do NOT mutate runtime/index.js yourself."
expected_quality_gain: medium
owner_attention_reduction: low
product_impact: low
priority:
  score: 95
  computed: "2026-07-02T03:38:00.000Z"
  inputs:
    mass: 189
    churn: 63
risk:
  blast_radius: local
  reversibility: trivial
  silent_wrongness_cost: medium
  property_at_risk: "pinning incidental trivia (exact stderr formatting, event counts across unrelated arms) instead of the guard contract would freeze accidents; missing the terminateChild assertion in (3) would let a later extraction drop child termination on protocol violation undetected"
authoring_evidence:
  - kind: sensor
    detail: "node ~/code/minnows/tools/hone/collectors/scope-fn.mjs --repo <repo> --target 'runtime/index.js::handleMsg'"
    result: "found=true, cognitive_before=194, line=2928, caller_count=1, red_scan=[consent]"
  - kind: coverage-sweep
    detail: "grep -rl over test/ for guard strings: 'unknown message type', 'while already waiting', 'duplicate ASSISTANCE', 'unknown assistance_request_id', 'run.detail_gap_terminal'"
    result: "all five → NONE (zero matching test files); by contrast 'outside declared resources'/'outside START.scope'/'undeclared stream'/'invalid JSONL' ARE pinned (collection-profile.test.js, event-spine.test.js, cli.test.js). Re-swept 2026-07-02: the rl-interceptor string 'while waiting for INTERACTION_RESPONSE' IS already pinned (collection-profile.test.js:5254, event-spine.test.js:3358, cli.test.js:4328) — corroborating that the wire concurrency path terminates at the interceptor, never at :3013"
  - kind: unreachability-analysis
    detail: "redesigned 2026-07-02 per run-2 — runtime/index.js routing: rl.on('line') (:3574+) fails a pending interaction via failPendingInteraction (:2853-2861, which also calls terminateChild) on ANY line arriving while pendingInteraction is set, including a second INTERACTION; processNext (:2863-2926) awaits handleMsg inline and the INTERACTION arm clears pendingInteraction before returning, so no dispatch-time path reaches the :3013 guard"
    result: "the :3013 guard ('Connector emitted INTERACTION while already waiting', also referenced by classifyRuntimeFailure :1054) is wire-UNREACHABLE; provenance: run-1 maker pinned :3577 instead, run-2 maker returned an empty oracle after the reachability demand, static analysis 2026-07-02 — three independent signals"
  - kind: human-decisions-needed
    detail: "keep-or-remove the wire-unreachable :3013 guard 'Connector emitted INTERACTION while already waiting' — appended 2026-07-02 as row 5 of docs/research/b-contract-ratification-queue.md for the owner's ratification queue"
    result: OPEN — awaiting owner decision; this packet documents the finding (dead-guard-inventory rung) and preserves the guard verbatim; neither maker nor judge may resolve it
  - kind: baseline-run
    detail: "node --test test/event-spine.test.js test/runtime-cancel-run.test.js (2026-07-02, dirty tree)"
    result: "event-spine 46/46 pass, runtime-cancel-run 2/2 pass; harness patterns confirmed reusable (runConnector imported directly, opts.detailGapStore injectable at runtime/index.js:1886)"
evidence_required:
  - rung: direct-test
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && sh -c 'if [ -f test/runtime-handle-msg-dispatch.test.js ]; then node --test test/runtime-handle-msg-dispatch.test.js test/runtime-cancel-run.test.js test/event-spine.test.js; else node --test test/runtime-cancel-run.test.js test/event-spine.test.js; fi'"
    expect: "baseline (new file absent): existing suites green (runtime-cancel-run 2/2, event-spine 46/46); post: new oracle file green alongside them with ALL SIX named pins present and passing (an empty or vacuous oracle file is an automatic REVISE — run-2's failure mode), 0 fail, no new skips"
    expect_check:
      type: exit_code
      value: 0
  - rung: red-then-green-unknown-type
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/unknown message type: ${msg.type}/unknown message kind: ${msg.type}/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent). Post (restructured 2026-07-02 per run-2 — engine-run mutation, df-0004 pattern): seeded run FAILS naming the unknown-message-type test — the mutation rewrites the throw string at runtime/index.js:3568 (sed target verified unique 2026-07-02: grep -cF 'unknown message type: ${msg.type}' runtime/index.js == 1; the classifier prefix at :1079 does not match the target); guard confirmed the file was clean before seeding; checkout restores it — this receipt IS the failing seeded run run-1 and run-2 were refused without"
  - rung: red-then-green-after-done
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/} after DONE/} following DONE/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent). Post: seeded run FAILS naming the after-DONE guard test — the mutation rewrites the generic doneMessage-prologue throw at runtime/index.js:2935 (sed target verified unique 2026-07-02: grep -c '} after DONE' runtime/index.js == 1; the invalid-JSONL-after-DONE string at :2933 is untouched, so exactly the 'Connector emitted RECORD after DONE' pin goes red); checkout restores it"
  - rung: red-then-green-interceptor-guard
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/emitted ${msg.type} while waiting for INTERACTION_RESPONSE/emitted ${msg.type} while awaiting INTERACTION_RESPONSE/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent). Post: seeded run FAILS naming the second-INTERACTION interceptor test — the mutation rewrites the rl-interceptor guard string at runtime/index.js:3579 (sed target verified unique 2026-07-02: grep -cF 'emitted ${msg.type} while waiting for INTERACTION_RESPONSE' runtime/index.js == 1; the invalid-JSONL interceptor line at :3583 does not match), proving the oracle pins the wire-REACHABLE concurrency guard; checkout restores it"
  - rung: red-then-green-duplicate-assistance
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/duplicate ASSISTANCE.assistance_request_id/duplicated ASSISTANCE.assistance_request_id/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent). Post: seeded run FAILS naming the duplicate-ASSISTANCE test — the mutation rewrites the throw at runtime/index.js:3180 (sed target verified unique 2026-07-02: grep -cF 'duplicate ASSISTANCE.assistance_request_id' runtime/index.js == 1); checkout restores it"
  - rung: red-then-green-unknown-assistance-status
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/ASSISTANCE_STATUS for unknown assistance_request_id/ASSISTANCE_STATUS for unmatched assistance_request_id/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent). Post: seeded run FAILS naming the unknown-ASSISTANCE_STATUS test — the mutation rewrites the throw at runtime/index.js:3215 (sed target verified unique 2026-07-02: grep -cF 'ASSISTANCE_STATUS for unknown assistance_request_id' runtime/index.js == 1); checkout restores it"
  - rung: red-then-green-detail-gap-terminal
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/run.detail_gap_terminal/run.detail_gap_terminal_mutated/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent). Post: seeded run FAILS naming the run.detail_gap_terminal spine test — the mutation rewrites the event_type at its single emit site runtime/index.js:3347 (sed target verified unique 2026-07-02: grep -c 'run.detail_gap_terminal' runtime/index.js == 1); checkout restores it"
  - rung: dead-guard-inventory
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && grep -n \"Connector emitted INTERACTION while already waiting\" runtime/index.js && echo \"FINDING 2026-07-02: the :3013 guard is wire-UNREACHABLE — a second INTERACTION arriving while one is pending is consumed by failPendingInteraction in rl.on('line') at :3577/:3579 before dispatch, and the serial pump (processNext awaits handleMsg inline; the INTERACTION arm clears pendingInteraction before returning) never dispatches a queued second INTERACTION while pendingInteraction is set. Provenance: run-1 maker could only trigger the interceptor string; run-2 maker returned no oracle for :3013; static analysis 2026-07-02. Guard kept verbatim as defensive depth pending owner keep-or-remove ratification: docs/research/b-contract-ratification-queue.md row 5.\""
    expect: "REQUIRED at baseline AND post (redesigned 2026-07-02 per run-2): grep lists BOTH sites — :1054 (classifyRuntimeFailure reference) and :3013 (the throw) — proving the guard still exists untouched, then the FINDING block prints verbatim; this receipt IS the unreachability documentation the redesign mandates (human decision needed: keep-or-remove is the owner's, queued as ratification row 5 — this packet must not resolve it)"
    expect_check:
      type: stdout_includes
      value: wire-UNREACHABLE
  - rung: byte-identity
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js"
    expect: "empty — this packet changes tests only (valid pre-commit: runtime/index.js is NOT in the touchset, so it must match HEAD); also proves every red-then-green rung restored its seed (restructured 2026-07-02 per run-2)"
  - rung: typecheck
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit"
    expect: "0 errors"
    expect_check:
      type: exit_code
      value: 0
not_allowed:
  - behavior-change
  - implementation-change
  - new-dependency
  - snapshot-testing-the-entire-spine
  - pinning-cross-arm-event-counts
  - attempting-to-reach-the-3013-guard-from-the-wire
  - presenting-the-3577-interceptor-string-as-the-3013-guard
  - removing-or-editing-the-3013-dead-guard-before-owner-ratification
  - empty-or-vacuous-oracle-file
maker_tier: standard
judge_tier: strong
maker_provider: claude
judge_provider: codex
batch_key: generate_evidence×judgment_first×runtime
touchset: [test/runtime-handle-msg-dispatch.test.js]
estimates:
  tokens: 80000
  evidence_cost: medium
depends_on: []
unlocks:
  - hm-reporting-family-extract-0003
  - hm-record-scope-extract-0004
  - hm-state-checkpoint-extract-0005
  - hm-assistance-family-extract-0006
  - hm-detail-page-coverage-extract-0007
  - hm-detail-gap-ledger-extract-0008
  - hm-interaction-lifecycle-extract-0009
  - hm-done-terminal-extract-0010
status: landed
outcome:
  commit: "1a9bc8d0e2c1303772b4a8c306d78e52dd72106e"
  skip_reason: null
  blocked_on: null
  judge_verdict: "codex PASS (confidence 0.94): The added test file implements the required direct oracle against the real runConnector path and covers all six named pins, including the reachable INTERACTION interceptor plus child-termination sentinel and the run.detail_gap_terminal event fields. The diff does not modify runtime/index.js, does not attempt to reach or reclassify the wire-unreachable :3013 guard, and the dead-guard receipt shows both guard references remain intact. The visible seeded-regression receipts demonstrate exact-string/event sensitivity, and typecheck plus byte-identity evidence satisfy the remaining packet gates."
  evidence_receipts:
    - "[baseline] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && sh -c 'if [ -f test/runtime-handle-msg-dispatch.test.js ]; then node --test test/runtime-handle-msg-dispatch.test.js test/runtime-cancel-run.test.js test/event-spine.test.js; else node --test test/runtime-cancel-run.test.js test/event-spine.test.js; fi' -> exit 0 (8s) PASS; exit=0 djb2=bacce723 bytes=11116 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-1-direct-test.txt"
    - "[baseline] red-then-green-unknown-type: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/unknown message type: ${msg.type}/unknown message kind: ${msg.type}/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-2-red-then-green-unknown-type.txt"
    - "[baseline] red-then-green-after-done: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/} after DONE/} following DONE/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-3-red-then-green-after-done.txt"
    - "[baseline] red-then-green-interceptor-guard: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/emitted ${msg.type} while waiting for INTERACTION_RESPONSE/emitted ${msg.type} while awaiting INTERACTION_RESPONSE/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-4-red-then-green-interceptor-guard.txt"
    - "[baseline] red-then-green-duplicate-assistance: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/duplicate ASSISTANCE.assistance_request_id/duplicated ASSISTANCE.assistance_request_id/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-5-red-then-green-duplicate-assistance.txt"
    - "[baseline] red-then-green-unknown-assistance-status: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/ASSISTANCE_STATUS for unknown assistance_request_id/ASSISTANCE_STATUS for unmatched assistance_request_id/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-6-red-then-green-unknown-assistance-status.txt"
    - "[baseline] red-then-green-detail-gap-terminal: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/run.detail_gap_terminal/run.detail_gap_terminal_mutated/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-7-red-then-green-detail-gap-terminal.txt"
    - "[baseline] dead-guard-inventory: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && grep -n \"Connector emitted INTERACTION while already waiting\" runtime/index.js && echo \"FINDING 2026-07-02: the :3013 guard is wire-UNREACHABLE — a second INTERACTION arriving while one is pending is consumed by failPendingInteraction in rl.on('line') at :3577/:3579 before dispatch, and the serial pump (processNext awaits handleMsg inline; the INTERACTION arm clears pendingInteraction before returning) never dispatches a queued second INTERACTION while pendingInteraction is set. Provenance: run-1 maker could only trigger the interceptor string; run-2 maker returned no oracle for :3013; static analysis 2026-07-02. Guard kept verbatim as defensive depth pending owner keep-or-remove ratification: docs/research/b-contract-ratification-queue.md row 5.\" -> exit 0 (0s) PASS; exit=0 djb2=680f9df8 bytes=833 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-8-dead-guard-inventory.txt"
    - "[baseline] byte-identity: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-9-byte-identity.txt"
    - "[baseline] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (5s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/baseline-10-typecheck.txt"
    - "[post] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && sh -c 'if [ -f test/runtime-handle-msg-dispatch.test.js ]; then node --test test/runtime-handle-msg-dispatch.test.js test/runtime-cancel-run.test.js test/event-spine.test.js; else node --test test/runtime-cancel-run.test.js test/event-spine.test.js; fi' -> exit 0 (8s) PASS; exit=0 djb2=64707acb bytes=11890 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-1-direct-test.txt"
    - "[post] red-then-green-unknown-type: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/unknown message type: ${msg.type}/unknown message kind: ${msg.type}/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (1s) PASS; exit=0 djb2=5186a6fb bytes=1888 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-2-red-then-green-unknown-type.txt"
    - "[post] red-then-green-after-done: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/} after DONE/} following DONE/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (1s) PASS; exit=0 djb2=503a33de bytes=1878 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-3-red-then-green-after-done.txt"
    - "[post] red-then-green-interceptor-guard: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/emitted ${msg.type} while waiting for INTERACTION_RESPONSE/emitted ${msg.type} while awaiting INTERACTION_RESPONSE/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (1s) PASS; exit=0 djb2=c419ef4f bytes=1924 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-4-red-then-green-interceptor-guard.txt"
    - "[post] red-then-green-duplicate-assistance: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/duplicate ASSISTANCE.assistance_request_id/duplicated ASSISTANCE.assistance_request_id/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (1s) PASS; exit=0 djb2=65bac00b bytes=2008 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-5-red-then-green-duplicate-assistance.txt"
    - "[post] red-then-green-unknown-assistance-status: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/ASSISTANCE_STATUS for unknown assistance_request_id/ASSISTANCE_STATUS for unmatched assistance_request_id/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (1s) PASS; exit=0 djb2=2f8bc3b8 bytes=2081 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-6-red-then-green-unknown-assistance-status.txt"
    - "[post] red-then-green-detail-gap-terminal: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-handle-msg-dispatch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/run.detail_gap_terminal/run.detail_gap_terminal_mutated/' runtime/index.js && node --test test/runtime-handle-msg-dispatch.test.js; git checkout -- runtime/index.js; else echo oracle-not-yet-authored; fi -> exit 0 (1s) PASS; exit=0 djb2=5c1b1000 bytes=1753 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-7-red-then-green-detail-gap-terminal.txt"
    - "[post] dead-guard-inventory: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && grep -n \"Connector emitted INTERACTION while already waiting\" runtime/index.js && echo \"FINDING 2026-07-02: the :3013 guard is wire-UNREACHABLE — a second INTERACTION arriving while one is pending is consumed by failPendingInteraction in rl.on('line') at :3577/:3579 before dispatch, and the serial pump (processNext awaits handleMsg inline; the INTERACTION arm clears pendingInteraction before returning) never dispatches a queued second INTERACTION while pendingInteraction is set. Provenance: run-1 maker could only trigger the interceptor string; run-2 maker returned no oracle for :3013; static analysis 2026-07-02. Guard kept verbatim as defensive depth pending owner keep-or-remove ratification: docs/research/b-contract-ratification-queue.md row 5.\" -> exit 0 (0s) PASS; exit=0 djb2=680f9df8 bytes=833 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-8-dead-guard-inventory.txt"
    - "[post] byte-identity: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-9-byte-identity.txt"
    - "[post] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (5s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-dispatch-guards-oracle-0001/post-10-typecheck.txt"
  tokens_actual: 100481
  lesson: null
resets:
  - at: "2026-07-02T04:44:43.517Z"
    from_status: reverted
    reason: "repaired per run-1 codex REVISE x2: instruction now leads with the judge's non-negotiables — pin the exact handleMsg guard string 'Connector emitted INTERACTION while already waiting' (runtime/index.js:3013, NOT the rl-interceptor string at :3577) and paste a genuinely FAILING seeded-regression run into receipts"
  - at: "2026-07-02T06:58:39.914Z"
    from_status: reverted
    reason: "restructured: evidence moved into engine-run mutation rungs per run-2 structural lesson (df-0004 pattern)"
