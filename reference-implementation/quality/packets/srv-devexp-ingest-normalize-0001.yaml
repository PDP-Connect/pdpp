# hone candidate packet (hand-authored schema-validation exercise, 2026-07-01)
# Container upgraded v1.0 -> v1.1 on 2026-07-01 (post-landing): model_tier split into maker_tier/
# judge_tier, prose evidence translated to literal {rung, command, expect}, plan/proof_class/
# execution_gate/estimates added, folded scalars flattened (hone's yaml.mjs rejects '>' folds, so
# the landed packet was machine-unreadable). OUTCOME CONTENT PRESERVED VERBATIM — terminal packets
# are immutable memory; only the serialization container changed.
candidate_id: srv-devexp-ingest-normalize-0001
created: "2026-07-01T00:00:00.000Z"
repo_sha: b6899084b0973a0a312c59f75bbadb0beac44404
subsystem: server/routes/device-exporters
files:
  - reference-implementation/server/routes/ref-device-exporters.ts
symbols:
  - normalizeDeviceIngestRecords
  - "anonymous (record, index) => {...} .map callback at reference-implementation/server/routes/ref-device-exporters.ts:571 inside normalizeDeviceIngestRecords (fn starts line 564)"
public_surface:
  - "POST /_ref/device-exporters/:device_id/ingest-batches — error envelope code=invalid_request, param=records, and accepted-record projection must be byte-preserved"
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: exact_move
execution_gate: autonomous
why_this_matters: "The per-record ingest validator/normalizer (cc 11 by smell-callbacks 2026-07-01 scan) lives as an anonymous .map callback braided into collection traversal inside normalizeDeviceIngestRecords. The record-level policy (must be object; record_key required as string|array with r.record_key ?? r.key fallback; stream must be non-empty string; emitted_at passthrough only when string; data defaulted to {}) is unnamed and unaddressable, so it cannot be referenced by tests, docs, or future packets."
plan:
  transform_class: hoist-to-named-module-fn
  instruction: "This is the whole change: hoist the callback body BYTE-IDENTICAL to a named module-scope function normalizeDeviceIngestRecord(record: unknown, index: number) placed directly above normalizeDeviceIngestRecords; the callsite becomes .map(normalizeDeviceIngestRecord). The callback's only free variables are the module-scope helpers requireNonEmptyString (line 339) and optionalObject (line 335), so a module-scope hoist keeps them in scope — this is an exact move, not a rewrite. Do NOT dedupe the three repeated invalid_request Error constructions in this packet (that is a behavior-adjacent follow-up; byte-identity is the proof class here)."
expected_quality_gain: medium
owner_attention_reduction: low
product_impact: low
risk:
  blast_radius: local
  reversibility: branch-revert
  silent_wrongness_cost: medium
  property_at_risk: "device ingest accept/reject semantics: which record shapes are accepted, the exact invalid_request error code/param/message strings, and the normalized {stream, key, emitted_at, data} projection"
authoring_evidence:
  - kind: inventory
    detail: "smell-callbacks 2026-07-01 scan — anonymous .map callback at ref-device-exporters.ts:571, cc 11, inside normalizeDeviceIngestRecords"
    result: "packetized as the schema-validation execution exercise"
evidence_required:
  - rung: direct-test-baseline
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node --test test/device-exporter-routes.test.js"
    expect: "all tests pass BEFORE the edit (direct route-level coverage of ingest-batches, incl. 201 accepted_record_count and invalid_request paths); paste pass/fail counts"
  - rung: exact-move
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/server/routes/ref-device-exporters.ts"
    expect: "the moved function body is byte-identical to the original callback body (whitespace-normalized; only the wrapper line changes); callsite is .map(normalizeDeviceIngestRecord)"
  - rung: typecheck
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit"
    expect: "0 errors"
  - rung: smell-rescan
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node scripts/code-quality/smell-callbacks.mjs --root=$PWD"
    expect: "no anonymous callback entry with cc>=6 reported for server/routes/ref-device-exporters.ts at/near line 571"
  - rung: direct-test
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node --test test/device-exporter-routes.test.js"
    expect: "AFTER the edit: same pass count as baseline"
not_allowed:
  - behavior-change
  - public-shape-change
  - error-message-or-error-code-change
  - new-dependency
  - moving-code-to-a-new-file
  - touching-any-other-route-or-function-in-the-file
  - deduplicating-the-error-construction (out of scope for this packet)
maker_tier: cheap
judge_tier: standard
maker_provider: claude
judge_provider: codex
batch_key: "preserve_refactor×exact_move×server/routes"
touchset:
  - reference-implementation/server/routes/ref-device-exporters.ts
estimates:
  tokens: 30000
  evidence_cost: low
depends_on: []
unlocks: []
status: landed
outcome:
  commit: 405ff7cdacc801892568bb1fda92602070c66505
  skip_reason: null
  blocked_on: null
  judge_verdict: "codex (gpt-5.5), read-only, given diff + this packet. Q1 behavior-preserving: 'PASS. The extracted function body is identical in validation, error construction, fallback key logic, stream trimming via requireNonEmptyString, emitted_at handling, and data defaulting. .map passes a third array argument, but JavaScript ignores it because the function has no rest/arguments usage and does not depend on this. TypeScript inference is also stable.' Q2 genuine decomplect: 'PASS. The collection traversal remains in normalizeDeviceIngestRecords, while the per-record accept/reject and projection policy is now first-class ... It names the record-level invariant that was previously hidden inside traversal.' VERDICT: PASS"
  evidence_receipts:
    - "BASELINE node --test test/device-exporter-routes.test.js -> 'tests 19 / pass 19 / fail 0 / duration_ms 4658.329275'"
    - "npx tsc --noEmit -> exit 0, no output (receipt: 'TSC: 0 errors')"
    - "smell re-scan: node scripts/code-quality/smell-callbacks.mjs --root=$PWD -> ref-device-exporters.ts entries no longer include any callback in parent_fn=normalizeDeviceIngestRecords; the former line-571 cc-11 iterator entry is absent (remaining entries are the out-of-scope route-handler T2 callbacks at lines 1054-1885)"
    - "exact-move check (node script over git show HEAD vs worktree): 'orig body lines: 20 | new body lines: 20; byte-identical raw: false; identical modulo leading indentation: true'; callsite '.map(normalizeDeviceIngestRecord)'"
    - "AFTER node --test test/device-exporter-routes.test.js -> 'tests 19 / pass 19 / fail 0 / duration_ms 4154.327892'"
    - "git diff --stat (pre-commit) -> '.../server/routes/ref-device-exporters.ts | 46 +++--- ; 1 file changed, 24 insertions(+), 22 deletions(-)' (touchset respected)"
  tokens_actual: 30000
  lesson: "(1) smell-callbacks counts MODULE-SCOPE helpers as captured_vars, so this T1b was really a T1a hoist — the detector's capture list needs a scope-aware filter, and packets derived from it inherit the misclassification. (2) The 'exact move' proof class needs a normalized definition: a hoist out of a callback always changes one indentation level, so 'byte-identical' must mean whitespace-normalized body hash or every executor will improvise. (3) Packet self-sufficiency held EXCEPT the smell re-scan step: the packet said 'same invocation as the 2026-07-01 scan' instead of the literal command; the executor had to read the instrument's usage header (--root arg) — evidence_required entries must be literal runnable commands and instrument scripts should be listed in files[] or a tools field. tokens_actual is an estimate (~30k for the execution phase); no meter existed in this hand run."
