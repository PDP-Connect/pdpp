candidate_id: tst2-source-kind-resolution-0015
created: "2026-07-06T12:04:00.000Z"
repo_sha: ae9cb548cea1de7f547b81c6879f7e09a2d99006
subsystem: server/connector-source-kind
files: [test/source-kind-resolution-oracle.test.js, server/routes/connector-source-kind.ts]
symbols: [sourceKindFromManifestBindings, resolveEnrolledSourceKind, SourceKindResolutionError]
public_surface:
  - "reference enrollment source_kind resolver: filesystem binding -> local_device; browser binding -> browser_collector; filesystem wins when both are declared; explicit contradictory source_kind rejects with invalid_request/param=source_kind"
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The enrollment source-kind resolver decides whether a connector instance is device-backed or browser-collector-backed from manifest bindings. Existing edge tests cover malformed bindings, but the public contract also needs the precedence and rejection behavior pinned: filesystem wins over browser, and a caller-supplied contradictory source_kind must fail with typed invalid_request rather than defaulting."
plan:
  transform_class: add-mutation-killing-pure-oracle
  instruction: "Create test/source-kind-resolution-oracle.test.js. Import SourceKindResolutionError, sourceKindFromManifestBindings, resolveEnrolledSourceKind from ../server/routes/connector-source-kind.ts. Assert sourceKindFromManifestBindings returns local_device for {bindings:{filesystem:{}}}, browser_collector for {bindings:{browser:{}}}, and local_device when both filesystem and browser keys exist. Assert resolveEnrolledSourceKind returns the derived value when requestedSourceKind is null/undefined or matches. Assert it throws SourceKindResolutionError with code invalid_request and param source_kind when requestedSourceKind contradicts the manifest, and param connector_id when no manifest binding resolves."
expected_quality_gain: high
owner_attention_reduction: medium
product_impact: medium
priority:
  score: 7.9
  computed: "2026-07-06T12:04:00.000Z"
  inputs:
    mass: 0
    churn: 0
risk:
  blast_radius: subsystem
  reversibility: trivial
  silent_wrongness_cost: high
  property_at_risk: connector enrollment must not silently put a filesystem-capable connector into the browser_collector source-kind lane or accept contradictory caller input
authoring_evidence:
  - kind: source-read
    detail: server/routes/connector-source-kind.ts declares filesystem-over-browser precedence and SourceKindResolutionError code/param fields.
    result: Pure resolver with typed public error vocabulary.
  - kind: test-sweep
    detail: "rg -n 'sourceKindFromManifestBindings|resolveEnrolledSourceKind' test"
    result: Existing edge test covers non-object bindings; this packet targets precedence and typed contradiction behavior.
evidence_required:
  - rung: red-then-green-mutant-kill
    command: "cd \"$REPO_ROOT\" && sh -c 'if [ -f test/source-kind-resolution-oracle.test.js ]; then git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"DIRTY source\"; exit 1; }; cp server/routes/connector-source-kind.ts /tmp/tst2_source_kind_resolution.bak; perl -0pi -e \"s/if \\(Object\\.hasOwn\\(bindings, \\\"filesystem\\\"\\)\\) \\{\\n    return \\\"local_device\\\";\\n  \\}\\n  if \\(Object\\.hasOwn\\(bindings, \\\"browser\\\"\\)\\) \\{\\n    return \\\"browser_collector\\\";\\n  \\}/if (Object.hasOwn(bindings, \\\"browser\\\")) {\\n    return \\\"browser_collector\\\";\\n  }\\n  if (Object.hasOwn(bindings, \\\"filesystem\\\")) {\\n    return \\\"local_device\\\";\\n  }/\" server/routes/connector-source-kind.ts; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_source_kind_resolution; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/source-kind-resolution-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp /tmp/tst2_source_kind_resolution.bak server/routes/connector-source-kind.ts; rm -f /tmp/tst2_source_kind_resolution.bak; git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"RESTORE FAILED\"; exit 1; }; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED (browser binding incorrectly wins over filesystem)\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: source kind resolution oracle not yet created\"; exit 0; fi'"
    expect: "BASELINE: prints BASELINE and exits 0. POST: reversing binding precedence makes the both-bindings assertion fail, prints MUTANT KILLED, restores source."
    expect_check:
      type: stdout_regex
      value: "BASELINE:|MUTANT KILLED"
  - rung: direct-test-green
    command: "cd \"$REPO_ROOT\" && sh -c 'if [ -f test/source-kind-resolution-oracle.test.js ]; then export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_source_kind_resolution_green; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/source-kind-resolution-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi'"
    expect: "POST: source-kind resolver oracle passes. BASELINE: guarded green."
    expect_check:
      type: exit_code
      value: 0
  - rung: diff-scope
    command: "cd \"$REPO_ROOT\" && sh -c 'test -z \"$(git diff --name-only -- server/routes/connector-source-kind.ts)\" && echo SRC-CLEAN || echo SRC-DIRTY'"
    expect: prints SRC-CLEAN.
    expect_check:
      type: stdout_includes
      value: SRC-CLEAN
not_allowed: [non-test-source-change, behavior-change, new-dependency, shared-db-usage]
maker_tier: cheap
judge_tier: standard
maker_provider: codex
judge_provider: claude
batch_key: generate_evidence×pure_logic×server/connector-source-kind
touchset: [test/source-kind-resolution-oracle.test.js]
estimates:
  tokens: 32000
  evidence_cost: low
depends_on: []
unlocks: []
status: landed
outcome:
  commit: c3e3d941d54e4b819bc76bb325cb894c9bd1f8b4
  skip_reason: null
  blocked_on: null
  judge_verdict: "claude PASS (confidence 0.85): The diff adds exactly one test file with no source changes (diff-scope gate confirms SRC-CLEAN pre- and post-diff, satisfying the non-test-source-change and behavior-change prohibitions). The mutation-kill evidence is genuine: baseline correctly no-ops before the oracle exists, and post-diff the precedence-swap mutant causes the both-bindings assertion to fail with a clear actual/expected mismatch (browser_collector vs local_device), correctly reported as MUTANT KILLED, with the source restored afterward. All four packet-specified assertions (filesystem/browser/both precedence, pass-through on null/undefined/matching request, typed invalid_request/source_kind rejection on contradiction, invalid_request/connector_id rejection on unresolved binding) are present in the diff and match the plan instruction line-by-line."
  evidence_receipts:
    - "[baseline] red-then-green-mutant-kill: cd \"$REPO_ROOT\" && sh -c 'if [ -f test/source-kind-resolution-oracle.test.js ]; then git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"DIRTY source\"; exit 1; }; cp server/routes/connector-source-kind.ts /tmp/tst2_source_kind_resolution.bak; perl -0pi -e \"s/if \\(Object\\.hasOwn\\(bindings, \\\"filesystem\\\"\\)\\) \\{\\n    return \\\"local_device\\\";\\n  \\}\\n  if \\(Object\\.hasOwn\\(bindings, \\\"browser\\\"\\)\\) \\{\\n    return \\\"browser_collector\\\";\\n  \\}/if (Object.hasOwn(bindings, \\\"browser\\\")) {\\n    return \\\"browser_collector\\\";\\n  }\\n  if (Object.hasOwn(bindings, \\\"filesystem\\\")) {\\n    return \\\"local_device\\\";\\n  }/\" server/routes/connector-source-kind.ts; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_source_kind_resolution; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/source-kind-resolution-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp /tmp/tst2_source_kind_resolution.bak server/routes/connector-source-kind.ts; rm -f /tmp/tst2_source_kind_resolution.bak; git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"RESTORE FAILED\"; exit 1; }; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED (browser binding incorrectly wins over filesystem)\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: source kind resolution oracle not yet created\"; exit 0; fi' -> exit 0 (0s) PASS; exit=0 djb2=1ad10328 bytes=56 receipt=quality/receipts/tst2-source-kind-resolution-0015/baseline-1-red-then-green-mutant-kill.txt"
    - "[baseline] direct-test-green: cd \"$REPO_ROOT\" && sh -c 'if [ -f test/source-kind-resolution-oracle.test.js ]; then export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_source_kind_resolution_green; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/source-kind-resolution-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi' -> exit 0 (0s) PASS; exit=0 djb2=caf12c67 bytes=31 receipt=quality/receipts/tst2-source-kind-resolution-0015/baseline-2-direct-test-green.txt"
    - "[baseline] diff-scope: cd \"$REPO_ROOT\" && sh -c 'test -z \"$(git diff --name-only -- server/routes/connector-source-kind.ts)\" && echo SRC-CLEAN || echo SRC-DIRTY' -> exit 0 (0s) PASS; exit=0 djb2=d3fe6147 bytes=10 receipt=quality/receipts/tst2-source-kind-resolution-0015/baseline-3-diff-scope.txt"
    - "[post] red-then-green-mutant-kill: cd \"$REPO_ROOT\" && sh -c 'if [ -f test/source-kind-resolution-oracle.test.js ]; then git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"DIRTY source\"; exit 1; }; cp server/routes/connector-source-kind.ts /tmp/tst2_source_kind_resolution.bak; perl -0pi -e \"s/if \\(Object\\.hasOwn\\(bindings, \\\"filesystem\\\"\\)\\) \\{\\n    return \\\"local_device\\\";\\n  \\}\\n  if \\(Object\\.hasOwn\\(bindings, \\\"browser\\\"\\)\\) \\{\\n    return \\\"browser_collector\\\";\\n  \\}/if (Object.hasOwn(bindings, \\\"browser\\\")) {\\n    return \\\"browser_collector\\\";\\n  }\\n  if (Object.hasOwn(bindings, \\\"filesystem\\\")) {\\n    return \\\"local_device\\\";\\n  }/\" server/routes/connector-source-kind.ts; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_source_kind_resolution; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/source-kind-resolution-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp /tmp/tst2_source_kind_resolution.bak server/routes/connector-source-kind.ts; rm -f /tmp/tst2_source_kind_resolution.bak; git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"RESTORE FAILED\"; exit 1; }; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED (browser binding incorrectly wins over filesystem)\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: source kind resolution oracle not yet created\"; exit 0; fi' -> exit 0 (0s) FAIL: output does not contain 'BASELINE'; exit=0 djb2=a898fd73 bytes=1441 receipt=quality/receipts/tst2-source-kind-resolution-0015/post-1-red-then-green-mutant-kill.txt"
    - "[post-r1] red-then-green-mutant-kill: cd \"$REPO_ROOT\" && sh -c 'if [ -f test/source-kind-resolution-oracle.test.js ]; then git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"DIRTY source\"; exit 1; }; cp server/routes/connector-source-kind.ts /tmp/tst2_source_kind_resolution.bak; perl -0pi -e \"s/if \\(Object\\.hasOwn\\(bindings, \\\"filesystem\\\"\\)\\) \\{\\n    return \\\"local_device\\\";\\n  \\}\\n  if \\(Object\\.hasOwn\\(bindings, \\\"browser\\\"\\)\\) \\{\\n    return \\\"browser_collector\\\";\\n  \\}/if (Object.hasOwn(bindings, \\\"browser\\\")) {\\n    return \\\"browser_collector\\\";\\n  }\\n  if (Object.hasOwn(bindings, \\\"filesystem\\\")) {\\n    return \\\"local_device\\\";\\n  }/\" server/routes/connector-source-kind.ts; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_source_kind_resolution; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/source-kind-resolution-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp /tmp/tst2_source_kind_resolution.bak server/routes/connector-source-kind.ts; rm -f /tmp/tst2_source_kind_resolution.bak; git diff --quiet -- server/routes/connector-source-kind.ts || { echo \"RESTORE FAILED\"; exit 1; }; if [ $rc -eq 1 ]; then echo \"MUTANT KILLED (browser binding incorrectly wins over filesystem)\"; exit 0; else echo \"MUTANT SURVIVED\"; exit 1; fi; else echo \"BASELINE: source kind resolution oracle not yet created\"; exit 0; fi' -> exit 0 (0s) PASS; exit=0 djb2=2e2be1d2 bytes=1461 receipt=quality/receipts/tst2-source-kind-resolution-0015/post-r1-1-red-then-green-mutant-kill.txt"
    - "[post-r1] direct-test-green: cd \"$REPO_ROOT\" && sh -c 'if [ -f test/source-kind-resolution-oracle.test.js ]; then export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_source_kind_resolution_green; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/source-kind-resolution-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; exit $rc; else echo \"BASELINE: test not yet created\"; exit 0; fi' -> exit 0 (0s) PASS; exit=0 djb2=1af0f6ac bytes=507 receipt=quality/receipts/tst2-source-kind-resolution-0015/post-r1-2-direct-test-green.txt"
    - "[post-r1] diff-scope: cd \"$REPO_ROOT\" && sh -c 'test -z \"$(git diff --name-only -- server/routes/connector-source-kind.ts)\" && echo SRC-CLEAN || echo SRC-DIRTY' -> exit 0 (0s) PASS; exit=0 djb2=d3fe6147 bytes=10 receipt=quality/receipts/tst2-source-kind-resolution-0015/post-r1-3-diff-scope.txt"
  tokens_actual: 90349
  lesson: landed after 1 revision cycle(s) — first attempt did not clear the gate
