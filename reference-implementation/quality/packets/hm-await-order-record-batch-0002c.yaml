created: "2026-07-02T09:53:40.301Z"
repo_sha: a86ed53d654801067446ce2db1315e8115c60420
subsystem: runtime
behavior_status: likely_intended
ownership: OWN
action: generate_evidence
proof_class: judgment_first
execution_gate: autonomous
expected_quality_gain: high
owner_attention_reduction: low
product_impact: low
priority:
  score: 94
  computed: "2026-07-02T09:53:40.301Z"
  inputs:
    mass: 189
    churn: 63
not_allowed:
  - behavior-change
  - implementation-change
  - new-dependency
  - wall-clock-sleep-as-sync-primitive
  - pinning-cross-stream-global-order
  - new-helper-files-outside-touchset
  - existence-assertions-where-order-is-the-contract
maker_tier: standard
judge_tier: strong
maker_provider: codex
judge_provider: claude
batch_key: generate_evidence×judgment_first×runtime
estimates:
  tokens: 35000
  evidence_cost: low
depends_on: []
status: landed
outcome:
  commit: "660f068fa85f1ae2456327dff748b965714dc113"
  skip_reason: null
  blocked_on: null
  judge_verdict: "claude PASS (confidence 0.9): This retry replaces the prior run's setTimeout-poll wall-clock-sleep with a genuine explicit-barrier promise: the RS stub resolves firstRequestObserved synchronously inside the request handler and holds the response on an awaited firstResponseReleased promise that the test controls directly — no timers, no polling, anywhere in the diff. The order assertion compares indices in a shared monotonic log with synchronous push points, and the count assertion [2,1] plus the red-then-green rung (mutant flips >= to >, named test fails deterministically with actual:[3]) demonstrate a real, timing-independent kill. Touchset is respected (runtime/index.js byte-identity empty), no new deps, no cross-stream ordering pinned — the not_allowed violation that sank run-7 is genuinely absent here, not relabeled."
  evidence_receipts:
    - "[baseline] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-record-batch.test.js ]; then node --test test/runtime-msg-order-record-batch.test.js test/runtime-handle-msg-dispatch.test.js; else node --test test/runtime-handle-msg-dispatch.test.js; fi -> exit 0 (1s) PASS; exit=0 djb2=a22cd59e bytes=877 receipt=quality/receipts/hm-await-order-record-batch-0002c/baseline-1-direct-test.txt"
    - "[baseline] red-then-green-record-batch-boundary: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-record-batch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/length >= BATCH_SIZE/length > BATCH_SIZE/' runtime/index.js || exit 1; rc=0; node --test test/runtime-msg-order-record-batch.test.js || rc=1; git checkout -- runtime/index.js; [ \"$rc\" -eq 1 ]; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=82b862cb bytes=24 receipt=quality/receipts/hm-await-order-record-batch-0002c/baseline-2-red-then-green-record-batch-boundary.txt"
    - "[baseline] byte-identity: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-record-batch-0002c/baseline-3-byte-identity.txt"
    - "[baseline] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (4s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-record-batch-0002c/baseline-4-typecheck.txt"
    - "[post] direct-test: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-record-batch.test.js ]; then node --test test/runtime-msg-order-record-batch.test.js test/runtime-handle-msg-dispatch.test.js; else node --test test/runtime-handle-msg-dispatch.test.js; fi -> exit 0 (1s) PASS; exit=0 djb2=1de9c7ab bytes=976 receipt=quality/receipts/hm-await-order-record-batch-0002c/post-1-direct-test.txt"
    - "[post] red-then-green-record-batch-boundary: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-record-batch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/length >= BATCH_SIZE/length > BATCH_SIZE/' runtime/index.js || exit 1; rc=0; node --test test/runtime-msg-order-record-batch.test.js || rc=1; git checkout -- runtime/index.js; [ \"$rc\" -eq 1 ]; else echo oracle-not-yet-authored; fi -> exit 0 (0s) PASS; exit=0 djb2=57404beb bytes=1012 receipt=quality/receipts/hm-await-order-record-batch-0002c/post-2-red-then-green-record-batch-boundary.txt"
    - "[post] byte-identity: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js -> exit 0 (0s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-record-batch-0002c/post-3-byte-identity.txt"
    - "[post] typecheck: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit -> exit 0 (4s) PASS; exit=0 djb2=00001505 bytes=0 receipt=quality/receipts/hm-await-order-record-batch-0002c/post-4-typecheck.txt"
  tokens_actual: 94781
  lesson: null
candidate_id: hm-await-order-record-batch-0002c
files:
  - runtime/index.js
  - test/runtime-handle-msg-dispatch.test.js
  - test/runtime-msg-order-record-batch.test.js
symbols:
  - runConnector
  - file: runtime/index.js
    parent_fn: handleMsg
    anchor: "case 'RECORD' — recordBatch[stream].length >= BATCH_SIZE boundary flush at :2970-2972 (await flushBatch(stream)); BATCH_SIZE read in-process from PDPP_RUNTIME_BATCH_SIZE at :2385"
    line: 2970
public_surface:
  - "ingest POST batching: the RECORD arm flushes at the PDPP_RUNTIME_BATCH_SIZE boundary and awaits the POST before processing the next message"
why_this_matters: "ONE contract, decomposed out of hm-awaited-order-oracle-0002 (4 failed attempts; superseded): the RECORD arm flushes at the PDPP_RUNTIME_BATCH_SIZE boundary (:2970) with the ingest POST awaited before the next message is processed. This was contract (4) of the original packet — the one whose complete ABSENCE the run-2 judge named as a refusal ground. Extraction packet hm-record-scope-extract-0004 cites the RECORD arm's batch/flush behavior as its ordering oracle. The kill is pure count+order arithmetic — the most deterministic of the four decomposed contracts."
plan:
  transform_class: direct-unit-oracle-generation
  instruction: "Add test/runtime-msg-order-record-batch.test.js pinning EXACTLY ONE contract: with PDPP_RUNTIME_BATCH_SIZE=2, three RECORDs for stream A produce exactly 2 ingest POSTs (2 records, then 1) and the first POST is awaited before the third RECORD is processed. BATCH_SIZE is read IN-PROCESS (runtime/index.js:2385), so set process.env.PDPP_RUNTIME_BATCH_SIZE='2' in the test BEFORE calling runConnector and restore it after (t.after). Copy the landed hm-0001 harness (test/runtime-handle-msg-dispatch.test.js, status landed, all pins green): inline writeStub for the temp-file fake connector (:103-140), readline flowing-mode stdin via createInterface({ input: process.stdin, crlfDelay: Infinity }) (:125), idle setInterval keep-alive tail (:116), injectable onInteraction passed to runConnector (:189). SELF-CONTAINED SINGLE FILE: ALL harness code (stub writer, RS stub with the shared sequence log, sentinel plumbing) lives INSIDE the one touchset test file — creating ANY other file is a touchset violation and the engine reverts ALL work fail-closed (run-3 lesson). SHARED MONOTONIC SEQUENCE LOG doctrine (the run-5 judge's core demand): one plain array; EVERY mark is appended SYNCHRONOUSLY at the moment of observation (the spine sink pushes at emit-invocation, the RS stub pushes 'request' at receipt and 'completed' when the response is released and fully written, connector-side observations are written synchronously with writeFileSync and folded into the log) — async marks weaken the log and were REVISEd in run-5. Assertions compare LOG INDEX ORDER, never existence (run-2/run-5 refusal class: the run-5 STATE mutant SURVIVED an existence-shaped oracle) and never wall-clock margins. HOLD-the-boundary: the RS stub HOLDS a response until the test releases it through an explicit barrier promise — a hold is not a sleep; green-path ordering is enforced by the runtime's await, never by hold length. Scenario: connector stub emits 3 RECORDs for stream A then DONE{status:'succeeded'}; the RS stub records each ingest POST body (record count per POST) and its sequence marks; HOLD the FIRST ingest response and release it after observing its 'ingest:request' mark — with the await in place, the second POST's request cannot be logged before the first POST's 'ingest:completed'. MANDATORY named test (exactly): 'RECORD arm: PDPP_RUNTIME_BATCH_SIZE=2 splits 3 records into exactly 2+1 ingest POSTs' asserting (i) exactly 2 ingest POSTs for stream A with record counts [2, 1], and (ii) index(ingest:completed #1) < index(ingest:request #2). Kill mechanics: the seeded mutation tightens 'length >= BATCH_SIZE' to 'length > BATCH_SIZE' (:2970), so the boundary flush at 2 never fires and the POST split collapses (single flush of 3 at the boundary-crossing or DONE flushAll) — the count assertion [2, 1] goes red deterministically with zero timing involved. PROCESS REQUIREMENT: iterate node --test on your file DURING authoring and only conclude when it is green against UNMUTATED code (this oracle CHARACTERIZES current behavior); then verify the kill yourself: seed the packet's sed mutation locally, confirm your named test (and ONLY an order assertion) goes red, restore runtime/index.js to byte-identity before handing back (the byte-identity rung enforces it). Never hand back a red file, and never hand back a file whose named test survives the seeded mutant — the red-then-green rung exits nonzero and the packet fails. Do NOT leave runtime/index.js modified; the engine's rung seeds and restores it itself."
risk:
  blast_radius: local
  reversibility: trivial
  silent_wrongness_cost: low
  property_at_risk: "forgetting that BATCH_SIZE is captured in-process (env must be set before runConnector, not on a child) yields a vacuously-green oracle against the default 500; count assertions must be per-stream so cross-stream global order stays unpinned"
authoring_evidence:
  - kind: code-read
    detail: "runtime/index.js case 'RECORD' re-read 2026-07-02 at a86ed53d6: :2965-2972 recordBatch push + boundary flush 'if (recordBatch[stream].length >= BATCH_SIZE) { await flushBatch(stream); }'; :2385 BATCH_SIZE = Number(process.env.PDPP_RUNTIME_BATCH_SIZE) || 500"
    result: contract confirmed at current line numbers; env is read in-process
  - kind: grep-unique
    detail: "grep -cF 'length >= BATCH_SIZE' runtime/index.js (2026-07-02, a86ed53d6)"
    result: "1 — the red-then-green sed target is verified unique"
  - kind: baseline-run
    detail: "node --test test/runtime-handle-msg-dispatch.test.js test/runtime-pipe-resilience.test.js test/event-spine.test.js (2026-07-02, repo_sha a86ed53d6)"
    result: "71/71 pass, 0 fail, 0 skipped — harness-pattern suites verified green on the current tree"
  - kind: fold-back
    detail: "hm-awaited-order-oracle-0002 judge history distilled 2026-07-02 (4 attempts, ~$22, lesson \"packet bar not reachable by this maker\"): run-2 judge refused existence-shaped assertions for DONE flush-before-EOF and INTERACTION attention/stdin ordering and refused the MISSING RECORD batch-boundary contract; run-3 ended skipped(touchset-violation: probe-driver.mjs/probe-stdin.mjs outside touchset); run-5 codex REVISE 0.98 then 0.88: (i) the seeded STATE flush-order mutant SURVIVED the oracle (receipt post-r1-2-red-then-green-state-flush-order.txt: exit 0, all tests green under the dropped-await mutation — the oracle asserted existence, not order), (ii) the rung exit code was not probative because the trailing git checkout masked node --test failures, (iii) the timeout INTERACTION ordering omitted the assistance-resolution/known-gap order before the stdin write, (iv) an async stdin mark weakened the shared monotonic log"
    result: "this decomposition pins ONE contract per packet; every red-then-green rung captures rc BEFORE restore and exits 0 ONLY when the seeded run failed, with the named failure enforced deterministically via expect_check failing_test_named; all order assertions read ONE shared monotonic sequence log with SYNCHRONOUS marks"
evidence_required:
  - rung: direct-test
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-record-batch.test.js ]; then node --test test/runtime-msg-order-record-batch.test.js test/runtime-handle-msg-dispatch.test.js; else node --test test/runtime-handle-msg-dispatch.test.js; fi"
    expect: "baseline (new file absent): landed hm-0001 dispatch-guards oracle (7 tests) green; post: the new single-contract oracle green alongside it, 0 fail, no new skips"
    expect_check:
      type: exit_code
      value: 0
  - rung: red-then-green-record-batch-boundary
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && if [ -f test/runtime-msg-order-record-batch.test.js ]; then git diff --quiet -- runtime/index.js && sed -i 's/length >= BATCH_SIZE/length > BATCH_SIZE/' runtime/index.js || exit 1; rc=0; node --test test/runtime-msg-order-record-batch.test.js || rc=1; git checkout -- runtime/index.js; [ \"$rc\" -eq 1 ]; else echo oracle-not-yet-authored; fi"
    expect: "baseline: oracle-not-yet-authored (file absent, exit 0). Post: the rung seeds the boundary-tightening mutation (runtime/index.js:2970; sed target verified unique 2026-07-02), runs the oracle capturing rc BEFORE restoring the file, restores, and exits 0 ONLY IF the seeded run FAILED — failing output visible in the receipt, and the engine requires the failure line to name 'RECORD arm: PDPP_RUNTIME_BATCH_SIZE=2 splits 3 records into exactly 2+1 ingest POSTs'"
    expect_check:
      type: failing_test_named
      value: splits 3 records into exactly 2+1 ingest POSTs
  - rung: byte-identity
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -- reference-implementation/runtime/index.js"
    expect: empty — this packet changes tests only (runtime/index.js is not in the touchset); also proves the red-then-green rung restored its seed
  - rung: typecheck
    command: "cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && npx tsc --noEmit"
    expect: "0 errors"
    expect_check:
      type: exit_code
      value: 0
touchset: [test/runtime-msg-order-record-batch.test.js]
unlocks: [hm-record-scope-extract-0004, hm-dispatch-table-coherence-0011]
resets:
  - at: "2026-07-02T13:44:58.660Z"
    from_status: reverted
    reason: "retry cross-pair: run-7 REJECT was a crisp not_allowed violation (setTimeout poll = wall-clock-sleep-as-sync-primitive); fix is mechanical (explicit barrier promise per packet doctrine) and threads via retry-context; codex maker proven on mandate-heavy retries"
