# hone work tst-record-index-repair-0009 — baseline rung 'direct-test-green'
# command: cd "$REPO_ROOT" && sh -c 'if [ -f test/postgres-record-index-repair-oracle.test.js ]; then export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst0009_repair_g; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/postgres-record-index-repair-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; exit $rc; else echo "BASELINE: test not yet created"; exit 0; fi'
# expect: POST: the repair oracle passes (obsolete index repaired to connector_instance_id,stream,version), no unexpected skips. BASELINE: prints 'BASELINE:' exit 0.
# exit: 0  duration: 0s  verdict: PASS

BASELINE: test not yet created
