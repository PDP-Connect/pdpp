# hone work tst2-dataset-summary-reconcile-window-0029 — maker brief digest (attempt 1)
# sha256(full brief)=c45b597bc36b516065500b0bf16f27383d5834136527de7ede57fc2367922cc7 bytes=5945
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): test/dataset-summary-reconcile-window-oracle.test.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: non-test-source-change, behavior-change, new-dependency, shared-db-usage, git-command.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== WORK PACKET (YAML) ==
candidate_id: tst2-dataset-summary-reconcile-window-0029
created: "2026-07-06T23:16:00.000Z"
repo_sha: current-tree-no-git-sandbox
subsystem: server/dataset-summary-read-model
files: [test/dataset-summary-reconcile-window-oracle.test.js, server/dataset-summary-read-model.js]
symbols: [reconcileDirtyDatasetSummaryRecordTimeBounds]
public_surface:
  - "dataset summary dirty record-time reconcile bounded window and residual reporting"
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: effectful
execution_gate: autonomous
why_this_matters: "The reconcile pass is a projection read-model contract: a bounded pass must report residual work and leave unprocessed rows dirty. rg found no by-name reconcile-window oracle."
plan:
  transform_class: add-seeded-mutation-oracle
  instruction: "Create test/dataset-summary-reconcile-window-oracle.test.js. Seed more dirty stream projection rows than the reconcile batch allows, run reconcileDirtyDatasetSummaryRecordTimeBounds, and assert processed/deferred/residual counts plus that unprocessed rows remain dirty while processed rows are clean only when their computed_at guard matched."
expected_quality_gain: high
owner_attention_reduction: medium
product_impact: medium
risk:
  blast_radius: subsystem
  reversibility: trivial
  silent_wrongness_cost: medium
  property_at_risk: "bounded projection reconcile must not silently claim all dirty time bounds were repaired"
authoring_evidence:
  - kind: coverage-sweep
    detail: "rg -n 'dataset-summary-reconcile-window|reconcileDirtyDatasetSummaryRecordTimeBounds.*residual|dirty record-time.*residual' test"
    result: "no exact reconcile-window oracle"
evidence_required:
  - rung: red-then-green-mutant-kill
    command: 'cd "$REPO_ROOT" && if [ -f test/dataset-summary-reconcile-window-oracle.test.js ]; then src=server/dataset-summary-read-model.js; bak=/tmp/tst2_0029_dataset_reconcile.bak; cp "$src" "$bak"; perl -0pi -e "s/const residual = scanned\\.length > MAX_RECONCILE_BATCH;/const residual = false;/" "$src"; if cmp -s "$src" "$bak"; then cp "$bak" "$src"; rm -f "$bak"; echo "MUTATION NOT APPLIED"; exit 1; fi; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_0029; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/dataset-summary-reconcile-window-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev