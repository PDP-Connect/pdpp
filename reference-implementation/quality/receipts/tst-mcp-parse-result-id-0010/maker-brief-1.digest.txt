# hone work tst-mcp-parse-result-id-0010 — maker brief digest (attempt 1)
# sha256(full brief)=437e9d5751b495e0dfc0c4e4695e350ab5f66d2924da552718ef9230dde17299 bytes=9725
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Path-coordinate note: Codex is running from the git root so its workspace-write sandbox covers the whole repository. The touchset in this brief has been rewritten to git-root-relative paths; follow the Binding rules touchset over any stale relative path prose in plan.instruction.
Binding rules:
- Modify ONLY these files (the touchset): packages/mcp-server/test/parse-record-result-id-oracle.test.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: non-test-source-change, behavior-change, new-dependency, git-command.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #3, from skipped: repair plan.instruction canonical URI envelope and reopen packet). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- lesson: repaired 2026-07-06: canonical pdpp://record case now includes v:1/kind:'record' envelope; evidence rungs no longer use git
== WORK PACKET (YAML) ==
candidate_id: tst-mcp-parse-result-id-0010
created: "2026-07-02T10:45:00.000Z"
subsystem: mcp-server/id-grammar
repo_sha: "078bd4d00e1c06243d73fa07eada24678e14624b"
files:
  - "../packages/mcp-server/test/parse-record-result-id-oracle.test.js"
  - "../packages/mcp-server/src/tools.js"
symbols: [parseRecordResultId, parseStreamRecordId]
public_surface:
  - "MCP fetch / read_record_field id grammar: self-contained `connection_id/stream:record_id` (connectionId populated); legacy `stream:record_id` (connectionId null); `pdpp://record/{base64url-JSON}` canonical URI (decoded to connection/stream/record); malformed ids rejected"
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "parseRecordResultId (packages/mcp-server/src/tools.js:2662, exported via __internal) is the id-grammar heart shared by fetch AND read_record_field: it parses the three public id forms that the content ladder promises are interchangeable self-contained handles — self-contained `connection_id/stream:record_id`, legacy `stream:record_id`, and the canonical `pdpp://record/{base64url-JSON}` URI the model sees in ladders/resource templates (the comment states 'a visible handle is never a dead fetch/read_record_field id'). self-contained-result-id.test.js exercises the grammar only through the end-to-end journey; the grammar itself is not unit-pinned by name. A mutation to the connection separator handling, the pdpp:// prefix branch, or the stream:record split would break a whole class of visible handles into dead ids with no failing unit test. This packet pins the grammar directly via __internal.parseRecordResultId — pure, no DB."
plan:
  transform_class: add-id-grammar-oracle
  instruction: "Create packages/mcp-server/test/parse-record-result-id-oracle.test.js (NEW file; touch no