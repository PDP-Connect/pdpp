# hone work hm-dispatch-table-coherence-0011 — maker brief digest (attempt 3)
# sha256(full brief)=28a5ea65df472d129928df89bb93497e87b3fc6f467c72b35cee28da9f3c2a53 bytes=21809
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): runtime/index.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, dispatch-semantics-change, handler-signature-redesign, new-dependency, new-module-file, callback-ratchet-baseline-update.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #1, from blocked: run-8 rung repair: job-1 red baseline had two causes, neither code — (a) the authored file list named runtime-handle-msg-order.test.js which never existed (stale filename from the pre-decomposition 0002 design; superseded by landed 0002a-d runtime-msg-order-* files), (b) a zero-CPU startup wedge on connector-detail-gap-store held the rung 33 min until operator-killed. Repaired list uses the four real runtime-msg-order-* files + timeout 300 per file; full 14-file ladder verified green by hand (7/1/1/1/2/124/46/12/18/2/26/2/4/15) before this reset). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- lesson: baseline rung 'direct-test-full-runtime' is red at repo_sha e7ede6e50ea9; the oracle must be green before this packet is workable
- blocked_on: red baseline: rung 'direct-test-full-runtime' failed BEFORE any change (exit 1 (expected 0)) — never work on a red baseline
== WORK PACKET (YAML) ==
candidate_id: hm-dispatch-table-coherence-0011
created: "2026-07-02T03:38:00.000Z"
repo_sha: e7ede6e50ea9b2f3d60ba908862a846c833d7a5c
subsystem: runtime
files: [runtime/index.js]
symbols:
  - file: runtime/index.js
    parent_fn: runConnector
    anchor: async function handleMsg(msg) — residual dispatch frame after family extractions
    line: 2928
public_surface: []
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: effectful
execution_gate: autonomous
why_this_matters: "Campaign closer for agenda item rt-index-handlemsg-a2-decomplect (handleMsg cc 194 → thin dispatcher). The eight family packets each extracted one message family; what remains must be verified AS A WHOLE against the agenda's acceptance criteria: (1) dispatch split into named handlers with explicit context and NO captured-mutable shared cells anywhere in the handler set; (2) ordering behavior proven preserved (the 0001/0002 characterization suites green on the final shape); (3) enclosing cc materially reduced — not relocated (whole-file excess strictly below the 713 campaign baseline with the reduction attributable to decomplection: explicit state records, un-braided families, named seams). This packet also sweeps integration residue that per-family packets could not see: duplicated msgCtx construction, leftover dead let-cells, inconsistent ctx member naming across the eight landings."
plan:
  transform_class: residual-dispatch-coherence
  