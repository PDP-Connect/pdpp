# hone work tst-record-index-idempotency-0008 — maker brief digest (attempt 1)
# sha256(full brief)=5adaaa6ea2b24119dae65615ffd2d04280cc31d1f31e94c9e4de94607a16a3ec bytes=12032
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): test/postgres-record-index-idempotency-oracle.test.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: non-test-source-change, behavior-change, new-dependency, shared-db-usage.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #1, from blocked: fix mutant-kill baseline expect_check (blocked → executable)). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- lesson: baseline rung 'red-then-green-mutant-kill' is red at repo_sha 078bd4d00e1c; the oracle must be green before this packet is workable
- blocked_on: red baseline: rung 'red-then-green-mutant-kill' failed BEFORE any change (expect_check[stdout_includes]: stdout does not contain 'MUTANT KILLED') — never work on a red baseline
== WORK PACKET (YAML) ==
candidate_id: tst-record-index-idempotency-0008
created: "2026-07-02T10:35:00.000Z"
subsystem: server/postgres-storage
repo_sha: "078bd4d00e1c06243d73fa07eada24678e14624b"
files: [test/postgres-record-index-idempotency-oracle.test.js, server/postgres-storage.js]
symbols: [ensurePostgresIndexDefinition, initPostgresStorage]
public_surface:
  - "storage bootstrap idempotency contract (main commit 2f122e547): initPostgresStorage MUST NOT drop/recreate a matching hot record index on every boot — a matching same-name index keeps its identity (oid stable) across restarts"
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: effectful
execution_gate: autonomous
why_this_matters: "Commit 2f122e547 ('fix(postgres): make record index bootstrap idempotent') fixed a real production defect: the bootstrap was dropping/recreating hot record/search indexes on EVERY boot, causing lock churn and rebuild cost. The fix gates recreation behind `if (matchesExpected) return;` in ensurePostgresIndexDefinition (postgres-storage.js:2221) under an advisory lock. The commit shipped test/postgres-record-index-bootstrap.test.js, but that test has NO demonstrated mutation-kill evidence — it is a plausibly-passing regression test whose ability to actually CATCH a re-regression is unproven. This packet adds a SHARP oid-stability oracle (a new file, disjoint touchset) and PROVES via the mutation ladder that removing the idempotency gate makes it go red — turning the commit's contract into pinned-and-mutation-verified coverage. The idempotency invariant is high-value: a silent regression reintroduces boot-time index churn on the live 18GB Postgres."
plan:
  transform_class: add-idempotency-oid-stability-oracle
  instruction: "Create test/postgres-record-index-idempotency-oracle.test.js (NEW file; touch no source). Follow the harness of test/postgres-record-index-bootstrap.test.js EXACTLY (guard on PDPP_TEST_POSTGRES_URL; with