# hone work t1b-transport-shim-negotiation-0002 — maker brief digest (attempt 1)
# sha256(full brief)=1eb0ea2c3dedc7df6fc40154eb67ecb7b763e8ec7dadecbdd3a2233f0b61165a bytes=6805
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): server/transport.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, relocation-without-decomplecting, new-dependency, changing-matchesMediaType, exporting-the-new-functions, touching-req.get/req.hostname/res-shim.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== WORK PACKET (YAML) ==
# hone candidate packet (hand-authored T1b batch, 2026-07-01)
candidate_id: t1b-transport-shim-negotiation-0002
created: "2026-07-02T02:10:00.000Z"
repo_sha: "6e8d8559c36b94d41bc4c42a9a31342e60b3b334"
subsystem: server/transport
files:
  - server/transport.js
  - test/fastify-transport.test.js
symbols:
  - expressShim
  - matchesMediaType
  - file: server/transport.js
    parent_fn: expressShim
    anchor: "req.is = (types) => negotiation shim"
    line: 323
  - file: server/transport.js
    parent_fn: expressShim
    anchor: "req.accepts = (types) => negotiation shim"
    line: 337
public_surface: []
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The express-compat content-negotiation policy lives as two inline arrows assigned inside expressShim, each CAPTURING req from the shim's scope: req.is (:323, cc 14) decides content-type matching (parameter stripping, array-vs-single candidates, matchesMediaType delegation) and req.accepts (:337, cc 21) decides accept-header matching (short/long-form expansion, '*/*' first-candidate rule). The negotiation policy — pure string-matching over two header values — is braided with the shim-wiring concern (when to install a polyfill on req). Hidden state: both callbacks read req.headers through the capture, so the policy has no single-entry testable surface; the follow-up oracle (t1b-transport-shim-negotiation-evidence-0001) pins it only route-level. Making the header values EXPLICIT parameters of two named module functions gives the negotiation contract an addressable surface and shrinks expressShim to wiring."
plan:
  transform_class: explicit-context-extraction
  instruction: "In server/transport.js hoist the two callback BODIES to named module-scope functions near matchesMediaType: (1) matchedContentType(contentTypeHeader, types) — body of the req.is arrow with `req.headers?.['content-type']` replaced by the explicit contentTypeHeader param; (2) acceptedType(acceptHeader, types) — body of the req.accepts arrow with `req.headers?.accept` replaced by the explicit acceptHeader param. Rewire inside expressShim: req.is = (types) => matchedContentType(req.headers?.['content-type'], types); req.accepts = (types) => acceptedType(req.headers?.accept, types). Branch bodies otherwise byte-identical (String() coercions, toLowerCase placement, candidate iteration order, false returns). Keep both functions module-private; move the explanatory comments above their new owners. Do not touch req.ge