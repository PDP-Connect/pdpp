# hone work t1b-streaming-input-telemetry-event-0013 — maker brief digest (attempt 1)
# sha256(full brief)=904e72de0fea3e82f83365eb9187e7527819f58d7373680f3fc814370ec4841d bytes=7781
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): server/streaming/routes.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, relocation-without-decomplecting, new-dependency, changing-error-codes-or-status-mapping, reordering-telemetry-relative-to-dispatch, extracting-the-whole-route-handler.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== WORK PACKET (YAML) ==
# hone candidate packet (hand-authored T1b batch, 2026-07-01)
# Evidence-gated on t1b-streaming-input-telemetry-evidence-0012.
candidate_id: t1b-streaming-input-telemetry-event-0013
created: "2026-07-02T02:10:00.000Z"
repo_sha: "6e8d8559c36b94d41bc4c42a9a31342e60b3b334"
subsystem: server/streaming
files:
  - server/streaming/routes.js
  - test/run-interaction-stream-input-telemetry.test.js
symbols:
  - registerStreamingRoutes
  - file: server/streaming/routes.js
    parent_fn: registerStreamingRoutes
    anchor: "app.post input-dispatch route handler (token-only)"
    line: 979
public_surface:
  - "POST /_ref/run-interaction-streams/{token}/input — 202 ack envelope, 400/401/409/410 error codes (MUST NOT change; the extraction target is the telemetry construction only)"
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The input-dispatch handler (server/streaming/routes.js:979, cc 34 — the pool's largest T1b) constructs three near-duplicate telemetry event objects inline (wire.input.received / wire.input.dispatched / wire.input.error), each re-deriving the same base fields (source 'server', correlationId, wireSeq, action: body.action || null, eventType: body.type || null) plus a per-kind extra (x/y numeric-or-null, dispatchLatencyMs, errorCode/errorMessage with the 'invalid_input' fallback). The event vocabulary is a real contract for the stream-debug viewer, and its triplication is where drift starts (edit one construction, forget two). Extracting ONE named pure builder with the inputs EXPLICIT decomplects event construction from dispatch control flow; the handler keeps only authorize -> parse -> push(received) -> dispatch -> push(dispatched|error) -> respond. NOT touched: session authorization, companion lookup, status-code mapping — the public 4xx cascade stays byte-identical in place."
plan:
  transform_class: explicit-context-extraction
  instruction: "In server/streaming/routes.js add a named module-scope fn buildWireInputTelemetryEvent(kind, body, correlationId, wireSeq, extra = {}) near parseReferenceWireInputPayload's import/use: returns { source: 'server', kind, correlationId, wireSeq, action: body.action || null, eventType: body.type || null, ...extra }. Rewire the three push sites: received => inputTelemetry.push(session.browser_session_id, buildWireInputTelemetryEvent('wire.input.received', body, correlationId, wireSeq, { x: typeof body.x === 'number' ? body.x 