# hone work tst-mcp-read-selector-guards-0001 — baseline rung 'direct-test-green'
# command: cd "$REPO_ROOT/../packages/mcp-server" && sh -c 'if [ -f test/read-record-field-selector-contract.test.js ]; then rc=0; timeout 300 node --test --test-force-exit test/read-record-field-selector-contract.test.js || rc=1; exit $rc; else echo "BASELINE: test not yet created"; exit 0; fi'
# expect: POST: the new test passes fully with source restored, no skips — proves the guards behave as pinned. BASELINE: prints 'BASELINE:' and exits 0.
# exit: 0  duration: 0s  verdict: PASS

BASELINE: test not yet created
