# hone work tst2-mcp-pick-query-filter-drop-0031 — maker brief digest (attempt 1)
# sha256(full brief)=b86c0dea18351d80e3f830f5612c83ba5dcd08418fa0ce02e00d74b85a6a532d bytes=6136
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Path-coordinate note: Codex is running from the git root so its workspace-write sandbox covers the whole repository. The touchset in this brief has been rewritten to git-root-relative paths; follow the Binding rules touchset over any stale relative path prose in plan.instruction.
Binding rules:
- Modify ONLY these files (the touchset): packages/mcp-server/test/pick-query-filter-drop-oracle.test.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: non-test-source-change, production-source-change, behavior-change, new-dependency, git-command.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== WORK PACKET (YAML) ==
candidate_id: tst2-mcp-pick-query-filter-drop-0031
created: "2026-07-06T23:51:00.000Z"
repo_sha: current-tree-no-git-sandbox
subsystem: packages/mcp-server/query-affordances
files:
  - "../packages/mcp-server/test/pick-query-filter-drop-oracle.test.js"
  - "../packages/mcp-server/src/tools.js"
symbols: [pickQuery]
public_surface:
  - MCP query_records/fetch argument forwarding must never leak raw filter or expand_limit objects as flat query params
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "Typed filters and expand limits are encoded through dedicated bracket-query builders. A raw pickQuery leak would silently send filter=[object Object] or expand_limit=[object Object], creating false-negative reads that look successful to MCP clients."
plan:
  transform_class: add-seeded-mutation-oracle
  instruction: "Create packages/mcp-server/test/pick-query-filter-drop-oracle.test.js. Import { __internal } from '../src/tools.js' and call pickQuery with args containing stream, filter:{sent_at:{gte:'2026-01-01'}}, expand_limit:{line_items:2}, cursor:'cur_1', limit:5, connection_id:'cin_a'; supportedKeys must include every key. Assert the returned object keeps cursor, limit, connection_id but omits stream, filter, and expand_limit. Touch no source."
expected_quality_gain: high
owner_attention_reduction: medium
product_impact: medium
risk:
  blast_radius: subsystem
  reversibility: trivial
  silent_wrongness_cost: high
  property_at_risk: raw nested query objects must not be forwarded as no-op flat params
authoring_evidence:
  - kind: coverage-sweep
    detail: "rg -n 'pickQuery|filter drop|expand_limit.*pickQuery|raw filter' packages/mcp-server/test"
    result: no direct __internal.pickQuery oracle found; broader integration tests cover selected routes only
  - kind: code-read
    detail: "packages/mcp-server/src/tools.js pickQuery skips stream, filter, and expand_limit before forwarding supported scalar args"
    result: pure unit seam exported through __internal
evidence_required:
  - rung: red-then-green-mutant-kill
    command: "cd \"$REPO_ROOT/../packages/mcp-server\" && if [ -f test/pick-query-filter-drop-oracle.test.js ]; then src=src/tools.js; bak=$REPO_ROOT/.pdpp_hone_tst2_003