# hone work t1b-package-rs-merge-child-rows-0008 — maker brief digest (attempt 1)
# sha256(full brief)=46ce24965653032c2b69afca353aa98cdfb9f9e4fd9ced98827d94b50c621e38 bytes=11361
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): server/package-rs-client.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, relocation-without-decomplecting, new-dependency, touching-mergeSearchEnvelopes, changing-warning-copy-or-identity-fallback-order, changing-either-fn's-meta/partial-assembly, spread-push-of-data-arrays.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #1, from reverted: spread-push mandate folded in per run-4 REJECT (JS argument-limit/sparse-array divergence); per-row append made explicit, not_allowed += spread-push-of-data-arrays). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- judge_verdict: codex REJECT (confidence 0.84): The warning path and partial/meta assembly are preserved, but the successful row path is not behavior-identical. The original loop appended one tagged row at a time; the new code builds a mapped array and then calls merged.push(...rows), which can fail for sufficiently large valid dense arrays due to JS argument limits and differs for sparse arrays. A preserve_refactor packet forbids that kind of boundary behavior change, and the supplied tests/typecheck do not cover it.
- lesson: judge rejected: The warning path and partial/meta assembly are preserved, but the successful row path is not behavior-identical. The original loop appended one tagged row at a time; the new code builds a mapped array and then calls merged.push(...rows), wh…
== WORK PACKET (YAML) ==
candidate_id: t1b-package-rs-merge-child-rows-0008
created: "2026-07-02T02:10:00.000Z"
repo_sha: "6e8d8559c36b94d41bc4c42a9a31342e60b3b334"
subsystem: server/package-rs-client
files: [server/package-rs-client.js, test/package-rs-client.test.js]
symbols:
  - mergeListEnvelopes
  - mergeEventSubListEnvelopes
  - file: server/package-rs-client.js
    parent_fn: mergeListEnvelopes
    anchor: results.forEach per-child merge callback
    line: 644
  - file: server/package-rs-client.js
    parent_fn: mergeEventSubListEnvelopes
    anchor: results.forEach per-child merge callback
    line: 831
public_surface: []
behavior_status: likely_intended
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The grant-package fan-out client merges child-RS list envelopes in two sibling functions whose per-child policy is an anonymous forEach callback (cc 10 each) CAPTURING the accumulators (merged, warnings, and in mergeListEnvelopes also the meta object it mutates): a failed child becomes a source_unavailable warning built from a connection_id||source.id||grant_id identity fallback, a successful child contributes its rows each tagged with memberSourceTag. The two callbacks are near-verbatim duplicates of one