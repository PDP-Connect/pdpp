# hone work t1b-package-rs-merge-child-rows-0008 — post rung 'exact-move'
# command: cd /home/tnunamak/.tmp/pdpp-cq-sweep && git diff -U0 -- reference-implementation/server/package-rs-client.js
# expect: warning template and rows-extraction expressions byte-identical inside the new fn; both loop rewires are accumulation-only; mergeSearchEnvelopes untouched
# exit: 0  duration: 0s  verdict: PASS

diff --git a/reference-implementation/server/package-rs-client.js b/reference-implementation/server/package-rs-client.js
index 3538399a6..efa36ae60 100644
--- a/reference-implementation/server/package-rs-client.js
+++ b/reference-implementation/server/package-rs-client.js
@@ -469,0 +470,14 @@ function memberSourceTag(member) {
+function childListContribution(result, child) {
+  if (!result.ok) {
+    return {
+      warning: {
+        code: 'source_unavailable',
+        message: `Source ${child.member.connection_id || child.member.source?.id || child.member.grant_id} returned ${result.status}`,
+        source: memberSourceTag(child.member),
+      },
+    };
+  }
+  const rows = Array.isArray(result.body?.data) ? result.body.data : (Array.isArray(result.body) ? result.body : []);
+  return { rows: rows.map((row) => ({ ...row, source: memberSourceTag(child.member) })) };
+}
+
@@ -646 +660,2 @@ function mergeListEnvelopes(children, results, _path) {
-    if (!r.ok) {
+    const contribution = childListContribution(r, child);
+    if (contribution.warning) {
@@ -648,5 +663 @@ function mergeListEnvelopes(children, results, _path) {
-      warnings.push({
-        code: 'source_unavailable',
-        message: `Source ${child.member.connection_id || child.member.source?.id || child.member.grant_id} returned ${r.status}`,
-        source: memberSourceTag(child.member),
-      });
+      warnings.push(contribution.warning);
@@ -655,3 +666,2 @@ function mergeListEnvelopes(children, results, _path) {
-    const rows = Array.isArray(r.body?.data) ? r.body.data : (Array.isArray(r.body) ? r.body : []);
-    for (const row of rows) {
-      merged.push({ ...row, source: memberSourceTag(child.member) });
+    for (const row of contribution.rows) {
+      merged.push(row);
@@ -833,6 +843,3 @@ function mergeEventSubListEnvelopes(children, results) {
-    if (!r.ok) {
-      warnings.push({
-        code: 'source_unavailable',
-        message: `Source ${child.member.connection_id || child.member.source?.id || child.member.grant_id} returned ${r.status}`,
-        source: memberSourceTag(child.member),
-      });
+    const contribution = childListContribution(r, child);
+    if (contribution.warning) {
+      warnings.push(contribution.warning);
@@ -841,3 +848,2 @@ function mergeEventSubListEnvelopes(children, results) {
-    const rows = Array.isArray(r.body?.data) ? r.body.data : (Array.isArray(r.body) ? r.body : []);
-    for (const row of rows) {
-      merged.push({ ...row, source: memberSourceTag(child.member) });
+    for (const row of contribution.rows) {
+      merged.push(row);
