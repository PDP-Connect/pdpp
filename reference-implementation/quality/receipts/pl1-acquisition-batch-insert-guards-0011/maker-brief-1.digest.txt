# hone work pl1-acquisition-batch-insert-guards-0011 — maker brief digest (attempt 1)
# sha256(full brief)=7682da22388de16af8d03687b389aadac0c82a64c8233a3fd0195f53d69d3250 bytes=9202
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): server/stores/acquisition-batch-store.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, error-message-change, batchid-default-change, guard-order-change, new-dependency, new-module-file.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== WORK PACKET (YAML) ==
candidate_id: pl1-acquisition-batch-insert-guards-0011
created: "2026-07-02T10:10:00.000Z"
repo_sha: 078bd4d00e1c06243d73fa07eada24678e14624b
subsystem: server/stores/acquisition-batch-store
files: [server/stores/acquisition-batch-store.js]
symbols:
  - normalizeInsert
public_surface:
  - "acquisition batch insert validation errors: 'ownerSubjectId is required.', 'connectorId is required.', 'connectorInstanceId is required.', 'artifactSha256 is required for owner-artifact batches.'"
behavior_status: contract
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "normalizeInsert (cc23, scope-fn red_scan empty) validates an owner-artifact acquisition batch then builds the persisted row (including a content-addressed batchId default). It braids REQUIRED-FIELD VALIDATION (four presence guards, each with a distinct thrown Error) with ROW CONSTRUCTION (a large defaulted object literal). Extracting the guard cluster into a named pure assert separates validation from construction — a genuine concern split — leaving normalizeInsert a straight builder and removing the four throw branches from its head. Pure function, no I/O, no grant/consent."
plan:
  transform_class: extract-validation-guard
  instruction: "In server/stores/acquisition-batch-store.js ONLY. Create ONE module-level (column-0) pure named function ABOVE normalizeInsert:\n\nfunction assertAcquisitionBatchInsert(record) — throws on invalid input, returns nothing. Contains VERBATIM the four guards currently at the top of normalizeInsert (L76-79): `if (!record.ownerSubjectId) throw new Error('ownerSubjectId is required.'); if (!record.connectorId) throw new Error('connectorId is required.'); if (!record.connectorInstanceId) throw new Error('connectorInstanceId is required.'); if (!record.artifactSha256) throw new Error('artifactSha256 is required for owner-artifact batches.');`.\n\nSEAM MECHANISM (explicit): assertAcquisitionBatchInsert takes ONE explicit parameter named `record` (== the caller's parameter identifier), so normalizeInsert delegates with a bare identifier. normalizeInsert's body becomes `assertAcquisitionBatchInsert(record); const now = record.now ?? new Date().toISOString(); return { ...the existing object literal unchanged... };`.\n\nHARD PRESERVE: the exact order of the four guards (ownerSubjectId, connectorId, connectorInstanceId, artifactSha256), every Error message byte-for-byte, the `const now = record.now ?? new Date().toISOString();` line staying in normalizeInsert AFTER the assert, and the 