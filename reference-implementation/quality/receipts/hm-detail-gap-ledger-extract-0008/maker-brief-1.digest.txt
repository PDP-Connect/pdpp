# hone work hm-detail-gap-ledger-extract-0008 — maker brief digest (attempt 1)
# sha256(full brief)=914ca919a97f8e1d5d0d7181731212f30c890c324a22ef3cc92eb035e4227b7e bytes=10797
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): runtime/index.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, spine-emission-gate-change, await-order-change, comment-block-deletion, new-dependency, new-module-file, captured-mutable-cells-in-new-handlers, method-calling-threaded-callbacks.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== WORK PACKET (YAML) ==
candidate_id: hm-detail-gap-ledger-extract-0008
created: "2026-07-02T03:38:00.000Z"
repo_sha: e7ede6e50ea9b2f3d60ba908862a846c833d7a5c
subsystem: runtime
files: [runtime/index.js]
symbols:
  - file: runtime/index.js
    parent_fn: handleMsg
    anchor: "case 'DETAIL_GAP' — upsertPendingGap, lease removal, §10-A resolveTerminalGapPolicy/maybeTerminateGap, exactly-once run.detail_gap_recorded"
    line: 3287
  - file: runtime/index.js
    parent_fn: handleMsg
    anchor: "case 'DETAIL_GAP_RECOVERED' — await flushAll() then markGapStatus recovered + run.detail_gap_recovered"
    line: 3478
public_surface:
  - "spine event_types run.detail_gap_recorded (first-sighting-only), run.detail_gap_terminal, run.detail_gap_recovered and their data fields (gap_id, attempt_count, discovered_run_id, detail_locator, list_cursor, last_error, known_gap, terminal_reason)"
behavior_status: contract
ownership: OWN
action: preserve_refactor
proof_class: property_at_risk
execution_gate: autonomous
why_this_matters: "The DETAIL_GAP arm (runtime/index.js:3287) carries three documented, red-teamed invariants braided into the cc-194 closure: (1) exactly-once audit semantics — run.detail_gap_recorded is emitted only when storedGap.discovered_run_id === runId AND the gap_id is new to detailGapRecordedThisRun (the transition-vs-state split; re-emitting unchanged gaps is 'dishonest-by-volume'); (2) §10-A connector-agnostic terminalization — resolveTerminalGapPolicy ALWAYS returns a policy (no if-null skip), so a 404/410/permanent gap can never be silently retried forever, and terminal gaps leave the fillable-pending set while durableDetailGaps/known-gap projection reflect 'terminal'; (3) lease bookkeeping — re-deferred and recovered gaps are removed from allServedGapIds so cleanup cannot double-reset them. DETAIL_GAP_RECOVERED additionally awaits flushAll() BEFORE marking the gap recovered (records land before the ledger says they did). Extracting the pair into named handlers with explicit context removes ~40 cc and makes each invariant locally readable; the inline comment blocks documenting (1) and (2) MUST move with the code."
plan:
  transform_class: explicit-context-extraction
  instruction: "In runtime/index.js ONLY: create module-level (column-0) named async functions handleDetailGapMsg(msg, ctx) and handleDetailGapRecoveredMsg(msg, ctx) with the two arm bodies verbatim, INCLUDING the §10-A and exactly-once comment blocks (they are load-bearing documentation of the invariants). Extend msgC