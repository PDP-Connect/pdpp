# hone work pl1-package-rs-search-envelope-shape-0004 — maker brief digest (attempt 2)
# sha256(full brief)=f6197f6a884d0e9f6ce210d96161d1683923863b4850aa352e61ad01c7e9af08 bytes=23386
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): server/package-rs-client.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, per-row-push-semantics-change, spread-push-over-hits, map-based-hit-accumulation, new-dependency, new-module-file.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #1, from reverted: re-route: extraction class -> codex maker/claude judge (tonight forced claude-maker = wrong pairing, all reverted)). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- lesson: transform failed its own evidence ladder at 'complexity-remeasure' — prior for preserve_refactor×pure_logic×server/package-rs-client down
== WORK PACKET (YAML) ==
candidate_id: pl1-package-rs-search-envelope-shape-0004
created: "2026-07-02T10:03:00.000Z"
repo_sha: "078bd4d00e1c06243d73fa07eada24678e14624b"
subsystem: server/package-rs-client
files: [server/package-rs-client.js]
symbols: [mergeSearchEnvelopes]
public_surface:
  - "package /v1/search merged response body — the search hits container is placed into whichever of data[] / data.data[] / data.results[] / results[] the base child envelope used; has_more; meta.package { member_count, partial, fanout_limit, merged_hit_count, returned_hit_count, source_mix }; meta.warnings"
behavior_status: contract
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "mergeSearchEnvelopes (cc18, scope-fn red_scan empty) fans package children's search results, dedupes+limits the merged hits, and writes the limited hits back into the base envelope. Its cognitive load is the five-way response-container placement branch (L704-715) that decides whether the hits array lands at baseBody.data, baseBody.data.data, baseBody.data.results (+scanned), baseBody.results, or a fresh { results }. That branch is pure structural shaping over baseBody + limitedHits and is fully separable from the accumulation. Extracting it into a named pure helper removes the multi-arm structural branch from the merge function. CRITICAL — DO NOT TOUCH THE HIT-ACCUMULATION LOOP: the sibling packet t1b-package-rs-merge-child-rows-0008 was REJECTED TWICE by independent judges (clm-...-1 codex, clm-...-3 codex) for altering per-row push semantics — first `merged.push(...rows)` spread (JS arg-limit + sparse-array divergence), then `rows.map(...)` (skips sparse holes, pushes undefined). This packet extracts ONLY the post-accumulation response-shape assignment, NEVER the `for (const hit of hits) mergedHits.push(...)` loop, which stays byte-identical."
plan:
  transform_class: extract-response-shape-writer
  instruction: "In server/package-rs-client.js ONLY. Create ONE module-level (column-0) pure nam