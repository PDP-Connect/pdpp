# hone work tst2-ingest-failure-shape-0016 — baseline rung 'red-then-green-mutant-kill'
# command: cd "$REPO_ROOT" && sh -c 'if [ -f test/ingest-failures-oracle.test.js ]; then src=runtime/ingest-failures.js; bak=/tmp/tst2_ingest_failure_shape_0016.bak; cp "$src" "$bak"; perl -0pi -e "s/if (status === 429) return code || 'rate_limit_error';/if (status === 430) return code || 'rate_limit_error';/" "$src"; if cmp -s "$src" "$bak"; then cp "$bak" "$src"; rm -f "$bak"; echo "MUTATION NOT APPLIED"; exit 1; fi; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_0016; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/ingest-failures-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp "$bak" "$src"; cmp -s "$src" "$bak" || { echo "RESTORE FAILED"; rm -f "$bak"; exit 1; }; rm -f "$bak"; if [ $rc -eq 1 ]; then echo "MUTANT KILLED"; exit 0; else echo "MUTANT SURVIVED"; exit 1; fi; else echo "BASELINE: mutation oracle not yet created"; exit 0; fi'
# expect: BASELINE prints BASELINE and exits 0 while the oracle file is absent. POST applies the seeded one-line mutant, runs the new test with an isolated Postgres DB, restores the source with cmp verification, and prints MUTANT KILLED only when the new oracle fails against the mutant.
# exit: 0  duration: 0s  verdict: PASS

BASELINE: mutation oracle not yet created
