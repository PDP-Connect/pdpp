# hone work pl1-semantic-rebuild-flush-dispatch-0005 — maker brief digest (attempt 1)
# sha256(full brief)=143949786281693f1853ef1c9f096dbef78b2b8c026401ea6712ea6de4aae012 bytes=19550
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): server/search-semantic.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, new-dependency, new-module-file, captured-mutable-cells-in-new-helpers, loop-carried-state-extraction.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #2, from reverted: two-strike redesign (b/adjust): fresh re-measure showed the <376 file-excess bar was a fantasy the tree never had (real baseline 395). The 2026-07-05 retry did honest work (target cc 44->33, whole-file 395->385) but was reverted against the impossible bar. Bar corrected to <395; design + seam unchanged; requeued pending.). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- lesson: REDIAGNOSED 2026-07-05 (two-strike review): the design is sound and the extraction genuinely reduces complexity — it failed twice on a FANTASY bar. The file-excess rung hardcoded <376, but the fresh/receipt baseline of search-semantic.js is 395 (never 376). Attempt 1 died on the complexity floor (cc 44->35, needed <34); attempt 2 CLEARED the floor (cc->33) and reduced whole-file excess 395->385, but was reverted against the impossible <376. Action taken: (b) ADJUST — file-excess bar corrected to <395 (fresh-measured); complexity floor <34 kept (honest, retry proved 33). No seam change: readSemanticRecordsPage + flushSemanticIndexEntries dispatch extraction stands. Requeued pending.
== WORK PACKET (YAML) ==
candidate_id: pl1-semantic-rebuild-flush-dispatch-0005
created: "2026-07-02T10:04:00.000Z"
repo_sha: "078bd4d00e1c06243d73fa07eada24678e14624b"
subsystem: server/search-semantic
files: [server/search-semantic.js]
symbols: [rebuildSemanticIndexForStream]
public_surface: []
behavior_status: contract
ownership: OWN
action: preserve_refactor
proof_class: effectful
execution_gate: autonomous
why_this_matters: "rebuildSemanticIndexForStream (cc44, scope-fn red_scan empty — 'pure decomplect target') is the paged semantic-index backfill loop. Two self-contained storage-dispatch concerns are braided into the loop body: (1) the per-page RECORD READ — a usePostgres ternary choosing postgresSemanticRecordsPage vs getMany(referenceQueries.searchSemanticRecordsPageNonDeleted) (L1206-1213); and (2) the per-page ENTRY FLUSH — a three-way branch choosing postgresSemanticIndexInsertMany, index.upsertMany, or a per-entry index.upsert fallback (L1248-1256). Both dispatch blocks depend only on their explicit inputs (usePostgres, page keys / entries) and mutate no loop-carried state (lastId, indexed, scanned stay in the loop). Extracting them into two named async helpers removes both storage-branch clusters from the hot loop, cutting ~12 cc, while the pagination state machine, the c