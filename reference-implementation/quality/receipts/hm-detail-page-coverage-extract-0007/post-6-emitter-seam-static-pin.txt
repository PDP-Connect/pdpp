# hone work hm-detail-page-coverage-extract-0007 — post rung 'emitter-seam-static-pin'
# command: cd /home/tnunamak/.tmp/pdpp-cq-sweep/reference-implementation && node -e 'const fs=require("fs");const s=fs.readFileSync("runtime/index.js","utf8");if(!s.includes("async function handleDetailCoverageMsg")){console.log("baseline: handlers not extracted; vacuous PASS");process.exit(0);}const w=s.replace(/\s+/g," ");const checks=[["page-request signature declares (msg, ctx, readDetailGapPage, writeChildStdin)",/async function handleDetailGapsPageRequestMsg\( ?msg, ?ctx, ?readDetailGapPage, ?writeChildStdin,? ?\)/],["page-request delegation passes bare closure identifiers (msg, msgCtx, readDetailGapPage, writeChildStdin)",/await handleDetailGapsPageRequestMsg\( ?msg, ?msgCtx, ?readDetailGapPage, ?writeChildStdin,? ?\)/],["coverage signature declares (msg, ctx, trackDetailCoverage, emitSpineEventTracked)",/async function handleDetailCoverageMsg\( ?msg, ?ctx, ?trackDetailCoverage, ?emitSpineEventTracked,? ?\)/],["coverage delegation passes bare closure identifiers (msg, msgCtx, trackDetailCoverage, emitSpineEventTracked)",/await handleDetailCoverageMsg\( ?msg, ?msgCtx, ?trackDetailCoverage, ?emitSpineEventTracked,? ?\)/]];let rc=0;for(const c of checks){const ok=c[1].test(w);console.log((ok?"PASS ":"FAIL ")+c[0]);if(!ok)rc=1;}if(/emitSpineEventTracked *:/.test(w)){console.log("FAIL emitSpineEventTracked must never appear as an object key");rc=1;}else{console.log("PASS no emitter object key");}const m=w.match(/const msgCtx = \{.*?\};/);if(!m){console.log("FAIL msgCtx literal not found");rc=1;}else{const b=m[0].replace("emitSpineEvent: emitSpineEventTracked","");if(/readDetailGapPage|trackDetailCoverage|emitSpineEventTracked/.test(b)){console.log("FAIL msgCtx literal gained a key for this packet services (readDetailGapPage / trackDetailCoverage / emitSpineEventTracked are param-seam only; writeChildStdin is grandfathered from sibling 0009)");rc=1;}else{console.log("PASS msgCtx literal frozen for this packet services");}}process.exit(rc);'
# expect: exit 0 and every printed line is PASS. Baseline (handlers not yet extracted): vacuous PASS via the leading guard. Post: pins the seam BOTH judges demanded — each handler declares its closure services as explicit named trailing parameters (parameter name = closure identifier), both delegation call sites pass the bare closure identifiers (emitSpineEventTracked identity is syntactic, not ctx-mediated), no emitSpineEventTracked object key exists anywhere, and the msgCtx literal gained none of the four service names. v2 2026-07-02: whitespace-tolerant (line-wrapped signatures/call sites accepted) with per-check PASS/FAIL diagnostics — the v1 silent single-line grep pin false-negatived on formatting and gave the revision cycle no signal
# exit: 0  duration: 0s  verdict: PASS

PASS page-request signature declares (msg, ctx, readDetailGapPage, writeChildStdin)
PASS page-request delegation passes bare closure identifiers (msg, msgCtx, readDetailGapPage, writeChildStdin)
PASS coverage signature declares (msg, ctx, trackDetailCoverage, emitSpineEventTracked)
PASS coverage delegation passes bare closure identifiers (msg, msgCtx, trackDetailCoverage, emitSpineEventTracked)
PASS no emitter object key
PASS msgCtx literal frozen for this packet services
