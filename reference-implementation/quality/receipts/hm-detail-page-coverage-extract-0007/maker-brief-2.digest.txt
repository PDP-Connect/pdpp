# hone work hm-detail-page-coverage-extract-0007 — maker brief digest (attempt 2)
# sha256(full brief)=3a9df1522c83ce1546b3e4197310eb981032638c9811b0ee732a829d35209b86 bytes=29403
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): runtime/index.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, child-protocol-response-shape-change, new-dependency, new-module-file, captured-mutable-cells-in-new-handlers, method-calling-threaded-callbacks, msgctx-key-addition, ctx-threaded-closure-services.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #2, from reverted: run-8 packet-redesign: honor the two independent judge REJECTs (run-6 codex clm-...-1, run-7 claude clm-...-3) — seam re-authored from msgCtx-threading to explicit named trailing parameters (parameter name = closure identifier, bare-identifier call sites, msgCtx literal frozen) + new emitter-seam-static-pin rung that fails the rejected shapes; complexity floor re-pinned 194->90). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- judge_verdict: claude REJECT (confidence 0.8): The msgCtx object now spreads both `emitSpineEvent: emitSpineEventTracked` and a new `emitSpineEventTracked` key, and the extracted handleDetailCoverageMsg destructures and calls `emitSpineEventTracked` from ctx — but nothing in the diff proves this ctx member is guaranteed identical to the original arm's direct closure reference to the tracked wrapper; it is an added indirection through a context object for an effectful call, which is exactly the kind of service substitution risk flagged by prior sibling packet 0003 and by this packet's own reset history (run-6 REJECT for the same emitSpineEventTracked→ctx substitution). The evidence_required list has no rung that specifically pins the spine-event call target/behavior (e.g., a spy/mock asserting emitSpineEventTracked was invoked with identical arguments before and after) — green integration tests and typecheck do not certify that the ctx-threaded call preserves the tracked wrapper's side effects (tracking/telemetry semantics), only that the JSON payload shape and success path are unchanged in these particular fixtures. Additionally, this same risk pattern was already REJECTed once in this packet's own history (see resets[0] and outcome.judge_verdict from a prior run), and the diff being judged now still has the identical structure that triggered that rejection, so the maker …
== WORK PACKET (YAML) ==
candidate_id: hm-detail-page-coverage-extract-0007
created: "2026-07-02T03:38:00.000Z"
repo_sha: e7ede6e50ea9b2f3d60ba908862a846c833d7a5c
subsystem: runtime
files: [runtime/index.js]
symbols:
  - file: runtime/index.js
    parent_fn: handleMsg
    anchor: "case 'DETAIL_GAPS_PAGE_REQUEST' — readDetailGapPage + DETAIL_GAPS_PAGE_RESPONSE stdin write"
    line: 3604
  - file: runtime/index.js
    parent_fn: handleMsg
    anchor: "case 'DETA