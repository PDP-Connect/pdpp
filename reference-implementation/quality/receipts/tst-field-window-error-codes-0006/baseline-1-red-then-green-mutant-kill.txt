# hone work tst-field-window-error-codes-0006 — baseline rung 'red-then-green-mutant-kill'
# command: cd "$REPO_ROOT" && sh -c 'if [ -f test/record-field-window-error-vocab-oracle.test.js ]; then git diff --quiet -- server/record-field-window.js || { echo "DIRTY source"; exit 1; }; cp server/record-field-window.js /tmp/tst0006-rfw.bak; sed -i "s/if (effectiveFields === null || effectiveFields === undefined) {/if (true || effectiveFields === null || effectiveFields === undefined) {/" server/record-field-window.js; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst0006_vocab; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 300 node --test --test-force-exit test/record-field-window-error-vocab-oracle.test.js >/dev/null 2>&1 || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp /tmp/tst0006-rfw.bak server/record-field-window.js; rm -f /tmp/tst0006-rfw.bak; git diff --quiet -- server/record-field-window.js || { echo "RESTORE FAILED"; exit 1; }; if [ $rc -eq 1 ]; then echo "MUTANT KILLED (grant gate short-circuited to always-visible -> field_not_granted test red, source restored)"; exit 0; else echo "MUTANT SURVIVED"; exit 1; fi; else echo "BASELINE: error-vocab oracle not yet created"; exit 0; fi'
# expect: POST: prints 'MUTANT KILLED' — forcing assertFieldVisibleToGrant to always return early (never throw) makes the field_not_granted case FAIL (the fail-closed gate is bypassed); rc captured (never the dropdb's), source restored byte-clean, ephemeral DB torn down. BASELINE: prints 'BASELINE:' exit 0.
# exit: 0  duration: 0s  verdict: PASS

MUTANT KILLED (grant gate short-circuited to always-visible -> field_not_granted test red, source restored)
