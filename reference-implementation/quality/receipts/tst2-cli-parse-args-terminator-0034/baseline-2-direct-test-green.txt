# hone work tst2-cli-parse-args-terminator-0034 — baseline rung 'direct-test-green'
# command: cd "$REPO_ROOT/../packages/cli" && if [ -f test/parse-args-terminator-oracle.test.js ]; then rc=0; timeout 300 node --test --test-force-exit test/parse-args-terminator-oracle.test.js 2>&1 || rc=1; exit $rc; else echo "BASELINE: test not yet created"; exit 0; fi
# expect: POST: the parseArgs oracle passes. BASELINE: guarded green while absent.
# exit: 0  duration: 0s  verdict: PASS

BASELINE: test not yet created
