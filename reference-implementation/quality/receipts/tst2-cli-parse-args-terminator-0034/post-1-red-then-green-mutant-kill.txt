# hone work tst2-cli-parse-args-terminator-0034 — post rung 'red-then-green-mutant-kill'
# command: cd "$REPO_ROOT/../packages/cli" && if [ -f test/parse-args-terminator-oracle.test.js ]; then src=src/ref/args.js; bak=/tmp/tst2_0034_cli_args.bak; cp "$src" "$bak"; perl -0pi -e "s/positionals\\.push\\(\\.\\.\\.argv\\.slice\\(i \\+ 1\\)\\);/positionals.push(...argv.slice(i));/" "$src"; if cmp -s "$src" "$bak"; then cp "$bak" "$src"; rm -f "$bak"; echo "MUTATION NOT APPLIED"; exit 1; fi; rc=0; timeout 300 node --test --test-force-exit test/parse-args-terminator-oracle.test.js 2>&1 || rc=1; cp "$bak" "$src"; cmp -s "$src" "$bak" || { echo "RESTORE FAILED"; rm -f "$bak"; exit 1; }; rm -f "$bak"; if [ $rc -eq 1 ]; then echo "MUTANT KILLED"; exit 0; else echo "MUTANT SURVIVED"; exit 1; fi; else echo "BASELINE: parse args oracle not yet created"; exit 0; fi
# executed-command: (NOT EXECUTED — portable-path refusal)
# expect: BASELINE guarded green while absent. POST including the -- terminator in positionals must fail the new parser oracle and print MUTANT KILLED after source restore.
# exit: null  duration: 0s  verdict: FAIL (portable-path refusal: foreign absolute path '/tmp/tst2_0034_cli_args.bak' — inside another git checkout, not the current git root (/home/tnunamak/.tmp/pdpp-ops), not the running engine, and no repo-suffix mapping applies. A rung must never measure outside the current repo: rewrite the packet command repo-relative or use $REPO_ROOT/$GIT_ROOT (exported to rung shells))

[engine] rung NOT EXECUTED — foreign absolute path '/tmp/tst2_0034_cli_args.bak' — inside another git checkout, not the current git root (/home/tnunamak/.tmp/pdpp-ops), not the running engine, and no repo-suffix mapping applies. A rung must never measure outside the current repo: rewrite the packet command repo-relative or use $REPO_ROOT/$GIT_ROOT (exported to rung shells)