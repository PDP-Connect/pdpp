# hone work pl1-canonical-sort-entry-validate-0008 — maker brief digest (attempt 3)
# sha256(full brief)=1bba5e91ed48791beb3c739f69402147bf26bb3efb296a370e9aa32d8043cc60 bytes=15787
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): server/record-query-helpers.ts. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: behavior-change, public-shape-change, error-message-change, error-code-change, sortable-vocabulary-change, new-dependency, new-module-file.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #2, from pending: re-anchor .js->.ts after Jul-3 unification in-place TS migration; baselines fresh-measured and unchanged (validateCanonicalSort cc19/file_excess22)). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- lesson: re-anchored 2026-07-05 after the Jul-3 unification migrated server/record-query-helpers.js -> .ts in place (git-follow: unify commit deletes the .js; validateCanonicalSort survives at .ts:109). All command/touchset paths flipped .js->.ts; stale .js-path receipts from the pre-unification runs cleared. Baselines fresh-measured on the .ts tree and UNCHANGED: validateCanonicalSort cognitive=19 (== original baseline 19); file_excess=22 (== original baseline 22); all three invalid_sort messages ('Empty sort field', 'is not advertised as sortable', 'Conflicting sort directions for field') verified literally present in .ts. Thresholds (scope_fn_lt 10, file_excess_lt 22) kept — still honest.
== WORK PACKET (YAML) ==
candidate_id: pl1-canonical-sort-entry-validate-0008
created: "2026-07-02T10:07:00.000Z"
repo_sha: "078bd4d00e1c06243d73fa07eada24678e14624b"
subsystem: server/record-query-helpers
files: [server/record-query-helpers.ts]
symbols: [validateCanonicalSort]
public_surface:
  - "invalidQueryError code 'invalid_sort' with param 'sort' and messages: 'Empty sort field'; \"Sort field '<f>' is not advertised as sortable; check /v1/schema for the canonical sort vocabulary.\"; \"Conflicting sort directions for field '<f>'\" — returned by the record list sort validation"
behavior_status: contract
ownership: OWN
action: preserve_refactor
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "validateCanonicalSort (cc19, scope-fn red_scan empty) parses a caller sort spec and validates each entry against the manifest's advertised sortable vocabulary. It braids a normalization prelude (join/split/trim/filter into entries + sortableFields from cursor_field) with a per-entry validation loop carrying three distinct throw arms (empty field, not-advertised field, conflicting direction) sharing a running `resolved`. The per-entry validation is a self-contained decision given (entry, sortableFields, resolved) → the next resolved {field, direction}. Extracting it into a named pure helper removes the three-arm throw ladder from the loop, cutting ~11 cc, while every error code/param/message and the last-entry-wins resolution stay byte-ident