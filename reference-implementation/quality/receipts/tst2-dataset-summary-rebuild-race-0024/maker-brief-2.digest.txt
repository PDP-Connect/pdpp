# hone work tst2-dataset-summary-rebuild-race-0024 — maker brief digest (attempt 2)
# sha256(full brief)=c6c37da96df1f3a632c93fa1169404e3102845312c95dca377f7c15d15b303fa bytes=9570
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Binding rules:
- Modify ONLY these files (the touchset): test/dataset-summary-rebuild-race-oracle.test.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: non-test-source-change, behavior-change, new-dependency, shared-db-usage, git-command.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== WORK PACKET (YAML) ==
candidate_id: tst2-dataset-summary-rebuild-race-0024
created: "2026-07-06T23:11:00.000Z"
repo_sha: current-tree-no-git-sandbox
subsystem: server/dataset-summary-read-model
files: [test/dataset-summary-rebuild-race-oracle.test.js, server/dataset-summary-read-model.js]
symbols: [rebuildDatasetSummaryProjection]
public_surface:
  - "dataset summary projection rebuild must not claim fresh results when a concurrent delta supersedes its generation"
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: effectful
execution_gate: autonomous
why_this_matters: "Existing dataset-summary tests cover route shape and dirty rows, but rg found no by-name oracle for the rebuild generation race or superseded rebuild metadata. This pins projection rebuild idempotency beyond the record-index oracle class."
plan:
  transform_class: add-seeded-mutation-oracle
  instruction: "Create test/dataset-summary-rebuild-race-oracle.test.js. Seed a rebuilt dataset summary projection, arrange a rebuild dependency that advances the projection generation with a record/blob delta before the guarded commit, run rebuildDatasetSummaryProjection, and assert the returned projection is stale/idle with last_error exactly 'dataset summary projection rebuild superseded by concurrent delta' rather than fresh. The test must not alter production code."
expected_quality_gain: high
owner_attention_reduction: medium
product_impact: medium
risk:
  blast_radius: subsystem
  reversibility: trivial
  silent_wrongness_cost: high
  property_at_risk: "dataset summary rebuild must be idempotent under races and must not publish stale fresh metadata"
authoring_evidence:
  - kind: coverage-sweep
    detail: "rg -n 'dataset-summary-rebuild-idempotency|supersededRebuildMetadata|rebuildDatasetSummaryProjection.*superseded|concurrent delta.*rebuild' test"
    result: "no existing by-name oracle"
evidence_required:
  - rung: red-then-green-mutant-kill
    command: 'cd "$REPO_ROOT" && if [ -f test/dataset-summary-rebuild-race-oracle.test.js ]; then src=server/dataset-summary-read-model.js; bak=/tmp/tst2_0024_dataset_summary.bak; cp "$src" "$bak"; perl -0pi -e "s/dataset summary projection rebuild superseded by concurrent delta/dataset summary projection rebuild superseded/" "$src"; if cmp -s "$src" "$bak"; then cp "$bak" "$src"; rm -f "$bak"; echo "MUTATION NOT APPLIED"; exit 1; fi; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_0024; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0;