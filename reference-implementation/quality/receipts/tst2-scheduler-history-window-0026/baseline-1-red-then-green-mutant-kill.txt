# hone work tst2-scheduler-history-window-0026 — baseline rung 'red-then-green-mutant-kill'
# command: cd "$REPO_ROOT" && if [ -f test/scheduler-history-window-oracle.test.js ]; then src=server/queries/controller/list-scheduler-run-history.sql; bak=/tmp/tst2_0026_scheduler_history.bak; cp "$src" "$bak"; perl -0pi -e "s/ORDER BY completed_at DESC, id DESC/ORDER BY completed_at ASC, id ASC/" "$src"; if cmp -s "$src" "$bak"; then cp "$bak" "$src"; rm -f "$bak"; echo "MUTATION NOT APPLIED"; exit 1; fi; export PGHOST=localhost PGPORT=55432 PGUSER=pdpp PGPASSWORD=pdpp; db=pdpp_hone_tst2_0026; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; createdb -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; rc=0; PDPP_TEST_POSTGRES_URL=postgres://pdpp:pdpp@localhost:55432/$db timeout 600 node --test --test-force-exit test/scheduler-history-window-oracle.test.js || rc=1; dropdb --if-exists -h localhost -p 55432 -U pdpp $db >/dev/null 2>&1; cp "$bak" "$src"; cmp -s "$src" "$bak" || { echo "RESTORE FAILED"; rm -f "$bak"; exit 1; }; rm -f "$bak"; if [ $rc -eq 1 ]; then echo "MUTANT KILLED"; exit 0; else echo "MUTANT SURVIVED"; exit 1; fi; else echo "BASELINE: mutation oracle not yet created"; exit 0; fi
# expect: POST changes the SQLite newest-window subquery to oldest-window; the new oracle kills it and source restoration is verified.
# timeout: 1800s (engine-default)
# exit: 0  duration: 0s  verdict: PASS

BASELINE: mutation oracle not yet created
