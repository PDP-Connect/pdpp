# hone work tst2-progress-provider-budget-0012 — baseline rung 'diff-scope'
# command: cd "$REPO_ROOT" && sh -c 'test -z "$(git diff --name-only -- runtime/progress-validators.js)" && echo SRC-CLEAN || echo SRC-DIRTY'
# expect: prints SRC-CLEAN; only the new test file remains changed.
# exit: 0  duration: 0s  verdict: PASS

SRC-CLEAN
