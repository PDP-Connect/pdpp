# hone work tst2-detail-gap-page-request-0014 — baseline rung 'diff-scope'
# command: cd "$REPO_ROOT" && sh -c 'test -z "$(git diff --name-only -- runtime/detail-gap-paging.js)" && echo SRC-CLEAN || echo SRC-DIRTY'
# expect: prints SRC-CLEAN.
# exit: 0  duration: 0s  verdict: PASS

SRC-CLEAN
