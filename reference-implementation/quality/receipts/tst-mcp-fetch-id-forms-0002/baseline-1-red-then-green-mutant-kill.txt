# hone work tst-mcp-fetch-id-forms-0002 — baseline rung 'red-then-green-mutant-kill'
# command: cd "$REPO_ROOT/../packages/mcp-server" && sh -c 'if [ -f test/fetch-id-form-contract.test.js ]; then git diff --quiet -- src/tools.js || { echo "DIRTY src/tools.js"; exit 1; }; cp src/tools.js /tmp/tst0002-tools.bak; sed -i "s/if (ref.connectionId \&\& query.connection_id === undefined) {/if (false \&\& ref.connectionId \&\& query.connection_id === undefined) {/" src/tools.js; rc=0; timeout 300 node --test --test-force-exit test/fetch-id-form-contract.test.js || rc=1; cp /tmp/tst0002-tools.bak src/tools.js; rm -f /tmp/tst0002-tools.bak; git diff --quiet -- src/tools.js || { echo "RESTORE FAILED"; exit 1; }; if [ $rc -eq 1 ]; then echo "MUTANT KILLED (embedded-connection forwarding disabled -> test red, source restored)"; exit 0; else echo "MUTANT SURVIVED"; exit 1; fi; else echo "BASELINE: fetch id-form test not yet created"; exit 0; fi'
# expect: POST: prints 'MUTANT KILLED' — disabling the embedded-connection-forwarding branch (the ChatGPT-dead-end regression) makes the self-contained-id case FAIL because query.connection_id is no longer set; rc captured, source restored byte-clean. BASELINE: prints 'BASELINE:' exit 0.
# exit: 0  duration: 0s  verdict: PASS

BASELINE: fetch id-form test not yet created
