# hone work tst-mcp-fetch-id-forms-0002 — maker brief digest (attempt 1)
# sha256(full brief)=8648f037c2ea27a2f2360f86ea04abbde1dedbcec6104df2158ae598adc15345 bytes=12947
# first 4096 chars follow

You are the MAKER in a repo-quality engine, executing exactly ONE work packet in this repository (your current working directory). The packet below is the entire contract.
Path-coordinate note: Codex is running from the git root so its workspace-write sandbox covers the whole repository. The touchset in this brief has been rewritten to git-root-relative paths; follow the Binding rules touchset over any stale relative path prose in plan.instruction.
Binding rules:
- Modify ONLY these files (the touchset): packages/mcp-server/test/fetch-id-form-contract.test.js. Creating, editing, deleting, or renaming ANY other file voids the whole run — the engine reverts everything.
- Obey every not_allowed item: non-test-source-change, behavior-change, mocking-away-the-fetch-handler, new-dependency.
- Do exactly what plan.instruction says. No scope creep, no drive-by fixes, no comment or formatting churn outside the named functions.
- Do NOT commit, stage, branch, or run any git write operation. Do NOT add dependencies. Do NOT touch anything under quality/.
- Do NOT run the test suite; the engine runs the packet's evidence_required commands itself after you finish.
- When done, reply with a short summary: which functions changed, what transform you applied, and why behavior is preserved.
- If you conclude the code is ALREADY CORRECT and the packet premise is a non-defect, make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: validated-non-defect — <one-line rationale>
- If you CANNOT act on plan.instruction as written (target missing, contradiction with the actual code, instruction not executable), make NO edits and end your reply with one line exactly of the form: HONE-VERDICT: unactionable — <one-line reason>
== PRIOR ATTEMPT — the judge's exact demands ==
This packet was attempted before and deliberately reopened (reset #3, from skipped: same basis as read-selector reset). The prior outcome below is the exact bar the previous attempt failed to clear — address it directly; do not repeat the failed approach.
- lesson: maker (claude) violated the touchset; brief forbids it explicitly — treat as provider reliability signal
- skip_reason: touchset-violation: maker modified packages/mcp-server/test/fetch-id-form-contract.test.js outside touchset [reference-implementation/../packages/mcp-server/test/fetch-id-form-contract.test.js] (both git-root-relative); ALL changes reverted
== WORK PACKET (YAML) ==
candidate_id: tst-mcp-fetch-id-forms-0002
created: "2026-07-02T10:05:00.000Z"
repo_sha: "078bd4d00e1c06243d73fa07eada24678e14624b"
subsystem: mcp-server/fetch
files: ["../packages/mcp-server/test/fetch-id-form-contract.test.js", "../packages/mcp-server/src/tools.js"]
symbols: [parseRecordResultId, ConflictingConnectionIdError]
public_surface:
  - "MCP tool fetch (packages/mcp-server/src/tools.js:641) — id-form acceptance: self-contained `connection_id/stream:record_id` (connection forwarded as query connection_id, no extra arg needed); legacy `stream:record_id` + optional connection_id arg; conflicting embedded-vs-argument connection_id -> ConflictingConnectionIdError (code conflicting_connection_id). Resolves to GET /v1/streams/{stream}/records/{record_id}."
behavior_status: contract
ownership: OWN
action: generate_evidence
proof_class: pure_logic
execution_gate: autonomous
why_this_matters: "The fetch tool's self-contained-id contract is the load-bearing fix for the 2026-06-09 ChatGPT dead-end (self-contained-result-id.test.js documents it): a search hit id must carry its own connection so fetch(id) needs no second handle. But the id-FORM DISPATCH inside the fetch handler — parseRecordResultId splitting `connection_id/stream:record_id` vs legacy `stream:record_id`, forwarding the embedded connection_id into the RS query, and rejecting an embedded-vs-argument connection_id MISMATCH via ConflictingConnectionIdError — has no by-name test asserting the resulting RS request path/connection scope per id form. self-contained-result-id.test.js proves the end-to-end journey but does not pin the three id forms and the conf