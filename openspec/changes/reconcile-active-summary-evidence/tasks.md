## 1. Contract and Sequencing Gate

- [x] 1.1 Resolve every first Sol P1 in the artifacts: composite reset-safe decimal checkpoint, shared repair fence, monotonic terminal CAS, orthogonal evidence components, barrier-before-cache, and scope-safe orphan rules.
- [x] 1.2 Amend the older active `define-stream-coverage-freshness-evidence` delta to canonical record-snapshot count authority and prove the deterministic archive/apply order (older change, then this `MODIFIED` delta) composes to exactly one count authority.
- [x] 1.3 Strictly validate this change and all OpenSpec artifacts, then obtain a same-session GPT-5.6 Sol LAND verdict at >=95% with no unresolved P0/P1 before implementation.
- [x] 1.4 Wait for the local-ingest-throughput owner to produce a reviewed/gated commit; inspect and integrate that commit before touching overlapping schema, record, coordinator, or startup files. (PR #334 / `8f62f17d5` merged to `origin/main`; independently re-verified by reading actual code, not commit messages — all 4 prior P1s and 5 P2s confirmed fixed. Residual: the real-Postgres+real-local-child oracle exists but is gated off by default; this session ran it directly, see task 6.3.)
- [x] 1.5 Re-read the landed writer inventory/coordinator API and prove summary repair uses the same fence domain without entering record/device acceptance. (`withConnectorInstanceWrite` re-entrant capability-token fence confirmed as the single fence domain across 11 existing call sites; summary repair in `connector-summary-evidence-engine.ts` uses the exact same primitive, never a second lock domain, never inside device/record acceptance.)

## 2. Failing Cross-Backend Oracles First

- [x] 2.1 Add a parameterized SQLite/real-Postgres production-entry-point journey for missing, dirty/failed, zero, canonical/retained-only unexpected, and missing/malformed-manifest evidence. (`test/reconcile-active-summary-evidence-oracle.test.js`; all 11 real cases (+1 Postgres-gated) GREEN, including malformed manifest, after the concept-correct `resolveSummaryManifest`/`manifest_declaration` fix in `ref-control.ts`; extended this cycle with 5 new retained-bytes convergence cases.)
- [x] 2.2 Add lost-marker changed-ingest, semantic no-op, absent/repeated delete, accepted replay, partial-prefix resume, direct write/delete, and post-commit projection/index failure cases. (Closed per Sol P1 finding on device-batch coverage: `test/device-batch-summary-evidence-convergence.test.js` reuses `device-ingest-conformance.test.js`'s exact device-batch durable-prefix entry-point harness for accepted replay, partial-prefix resume, and manifest-registration race — 6/6 observed green including un-skipped real-Postgres cases. Lost-marker changed-ingest remains covered by the oracle.)
- [x] 2.3 Add stream reset and connector-wide invalidation followed by reinsertion at the same per-stream version, including live records whose counter row is deliberately absent; prove the generation and composite checkpoint change on both backends. (RECLOSED per fourth-cycle fix for third Sol REVISE verdict P2.2: `list-instance-streams-by-connector.sql` now unions `records` with `version_counter` on SQLite; `postgresDeleteAllRecordsForConnector`'s discovery query now also unions `version_counter` on Postgres. New tests in `test/record-reset-generation-checkpoint.test.js` ("connector-wide reset discovers and clears a counter-only namespace", SQLite + real-Postgres) seed a `version_counter` row with ZERO live/history/blob rows and prove `deleteAllRecordsForConnector`/`postgresDeleteAllRecordsForConnector` discover it, clear the counter, and advance `record_reset_generation`. Both genuinely fail without the fix (git-stash revert-and-observe verified) and pass with it, on both backends.)
- [x] 2.4 Add `>2^53 - 1`, UTF-8 stream ordering, and byte-identical SQLite/Postgres decimal-checkpoint fixtures. (`test/record-source-checkpoint.test.js`.)
- [x] 2.5 Add ingest-at-final-check, two-reconciler, two-fold, late-terminal-event, startup/read overlap, repair failure, and terminal non-regression fixtures. (RECLOSED per fourth-cycle fix for third Sol REVISE verdict P1.1: `markTerminalFactsFailedForAllRows`/`markAllConnectorSummaryEvidenceDiscoveryFailed` now return an in-memory typed failed-row overlay (built from a pre-failure snapshot read, degrading only the failed component, preserving every other known field) whenever their own durable marker write also fails; `observeConnectorSummaryEvidence` now merges the fold's overlay into its `failedRows` result alongside the repair engine's. New probes 3/4 in `test/reconcile-summary-evidence-failure-persistence.test.js` (SQLite) and `test/reconcile-summary-evidence-failure-persistence-postgres.test.js` (real Postgres) force the exact simultaneous double faults Sol reproduced — fold-write + terminal-facts-marker-write both rejected; discovery (`version_counter` renamed unreadable) + discovery-failed-marker-write both rejected — starting from a genuinely current/fresh row, through the real `listConnectorSummaries` production entry point in one call, asserting the affected component reads non-current and `ProjectionReliable=false`. All 4 new tests genuinely fail without the fix (git-stash revert-and-observe verified) and pass with it.)
- [x] 2.6 Add full/scoped orphan cleanup with sibling, paused, draft/setup, and revoked preservation. (`test/connector-summary-evidence-engine-orphan-cleanup.test.js` proves complete-vs-scoped + sibling non-interference directly on the engine; revoked/paused preservation covered by `test/connector-summary-read-model.test.js`'s "keeps connections with zero records"/"materializes revoked lifecycle evidence" cases.)
- [x] 2.7 Add warm-cache/stale-refresh, direct list/scoped, cookie, owner diagnostics, scheduler, singleton detail, and ambiguous detail barrier cases. (RECLOSED per fifth-cycle fix for fourth Sol REVISE verdict P1.1: added a bounded, idempotent, set-based backfill `UPDATE` for pre-existing terminal `spine_events` rows whose `connector_instance_id` column is `NULL` but whose `data_json` carries a genuine `connector_instance_id`/`connection_id` — `migrateSpineEventsConnectorInstanceIdBackfill` on SQLite (restricted to the 4 terminal event types, using the same partial index the fold itself relies on; naturally idempotent since the `WHERE connector_instance_id IS NULL` clause matches zero rows after the first successful run — explicitly NOT the rejected per-row `source_kind`/`source_id` full-table-scan pattern), and the equivalent inline `UPDATE` in the Postgres bootstrap DDL. New `test/spine-events-connector-instance-id-backfill.test.js` (SQLite + real Postgres) creates an old-schema-shaped row (`data_json` identity, column `NULL`) BEFORE a simulated reboot (`closeDb`/`initDb` or `closePostgresStorage`/`initPostgresStorage`), then proves the real SCOPED fold, the real mounted route, AND the startup sweep all converge the historical fact post-backfill — plus that a genuinely unattributable legacy event (no identity in `data_json` at all) correctly stays `NULL`, never fabricated. Genuinely fails without the fix (git-stash revert-and-observe verified) and passes with it, on both backends.)
- [x] 2.8 Add cross-axis cases: dirty retained bytes with current canonical counts, failed terminal facts with current counts, terminal facts unobserved versus checkpointed-empty current, known zero with unknown coverage, and complete coverage with zero records. (RECLOSED alongside 2.5: probe 3 in `test/reconcile-summary-evidence-failure-persistence.test.js` now proves "failed terminal facts with current counts" for the genuine fold-failure-plus-marker-failure double fault, not merely the previously-covered single-fault case.)

## 3. Exact Source Checkpoint and Repair

- [x] 3.1 Add equivalent SQLite/Postgres `connector_instances.record_reset_generation` migrations with non-null zero default and decimal-text reads.
- [x] 3.2 Advance the generation by the number of distinct stream namespaces with a pre-reset counter row or live canonical record in the same canonical stream/connector reset transaction; cover counterless live-record drift, preserve the generation across invalidation, erase it with connection deletion, and advance neither component only when no counter or live count/recency input changes. (Connection-deletion erasure relies on the row being dropped by the existing cascade, not separately re-tested here; the other four properties are directly proven in `test/record-reset-generation-checkpoint.test.js`.)
- [x] 3.3 Normalize `{reset_generation, streams[]}` without JavaScript numeric coercion, using unsigned base-10 strings and UTF-8 byte ordering.
- [x] 3.4 Expand batched discovery across the complete/scoped canonical set and classify missing, dirty, record/identity/manifest/terminal/retained mismatches without N+1 reads or locks on current rows. (Scoped SQLite discovery now issues one batched `IN (...)` query instead of one point query per requested id; Postgres uses `= ANY($1::text[])`. Also found and fixed a genuinely vacuous pre-existing N-slope instrumentation — `db.prepare` reassignment never actually intercepted calls through `server/db.js`'s cached-prepare Proxy; rewritten to patch `Database.prototype.prepare` directly, verified fixed-cost at both N=1 and N=25.)
- [x] 3.5 Repair only candidates under the landed shared instance writer fence and one backend transaction; return the exact fenced row and row-shaped stale/failed evidence on lock/read/write failure. (RECLOSED alongside 2.5: the row-shaped-failure contract now extends to discovery and terminal-fold, not only repair-candidate — see 2.5's probes 3/4.)
- [x] 3.6 Implement complete-census and exact-point scoped orphan deletion rules without inferring sibling absence.
- [x] 3.7 Add bounded best-effort startup reconciliation using the same primitive; observation-time repair remains the correctness gate. (RECLOSED per fifth-cycle fix for fourth Sol REVISE verdict P1.2: `foldConnectorSummaryStreamFacts` now accepts `{maxDurationMs, maxEvents}`, checks the budget BETWEEN batches (never mid-batch), and — when exhausted — writes every participant's durable checkpoint at the EXACT cursor position the drain reached (floored at that participant's own baseline, so a fast connection sharing a pass with a slow one never regresses) instead of unconditionally draining to the pass's full high-water mark. `runBoundedSummaryEvidenceSweep` threads each page's remaining time budget (plus an optional `maxEventsPerFold`) into its own fold call, reports the WHOLE sweep `incomplete` when any page's fold does not converge within its budget, sets `resumeAfterId` to the cursor BEFORE that still-incomplete page (so a follow-up resumes the SAME page's connections, not past them), and gates `prunedComplete` on BOTH complete canonical census AND every page's fold genuinely converging. New `test/connector-summary-evidence-fold-budget-resume.test.js` (SQLite + real Postgres) reproduces Sol's exact probe (one connection, 2,001 events, tiny budget) and proves: the fold genuinely stops short of the high-water mark and writes a real partial checkpoint; repeated bounded calls resume (never restart/skip) and converge to exactly the unbounded-oracle value; a multi-page sweep with one connection carrying a large history reports the whole sweep incomplete, skips complete pruning, and a follow-up sweep resumes the exact same still-incomplete page while leaving already-converged connections untouched. Genuinely fails without the fix (git-stash revert-and-observe verified) and passes with it, on both backends.)

## 4. Terminal, Stream, and Health Semantics

- [x] 4.1 Make terminal folds snapshot-bounded and baseline-CAS/monotonic; preserve terminal columns during record repairs and refuse older-pass regression.
- [x] 4.2 Expose independent typed `record_snapshot`, `terminal_facts`, `manifest_declaration`, and `retained_bytes` evidence with closed sanitized reason codes. (`ConnectorSummary`/`shapeEvidenceRow` now carry a real fourth typed `retained_bytes_evidence: {state, as_of, reason_code}` component, populated by a new bidirectional `retainedBytesNeedsRepair()` comparison that also closes the prior one-directional missing-vs-clean convergence bug. Deliberately not fed into `evidenceUnreliableSources`/`ProjectionReliable`, per design.md's "Health boundary" — retained-bytes failure makes bytes unavailable but does not itself degrade connection health.)
- [x] 4.3 Evolve `stream_records` to exhaustive `declaration_state`/`count_state` invariants with dormant retained history, active-only totals, and re-add stale/unknown evidence. The production manifest-registration transaction advances a durable per-connection generation and dirties summary evidence atomically; repair clears terminal facts at that generation boundary, so unobserved remove/re-add cannot replay pre-removal evidence. SQLite and dedicated Postgres production-path regressions prove two advances and require a post-boundary terminal collection.
- [x] 4.4 Make canonical stable records the count/recency authority and retained-size the byte/history/blob authority only; keep provider coverage independent.
- [x] 4.5 Feed every non-current record/terminal/manifest component—including never-observed terminal facts—and explicit current-generation unexpected streams into `ProjectionReliable`; dormant history remains diagnostic only. Generation-keyed undeclared-write provenance and summary invalidation commit in the same connection writer/transaction; legacy fingerprint-only violations migrate to negative historical generations.
- [x] 4.6 Enforce one actor-aware current-manifest authority at list, detail, aggregate, field-window, lexical, semantic, and hybrid read boundaries before storage/index/delegate access. Missing internal manifests fail closed with `stream_not_declared`; stale client grants retain normative `grant_invalid`; owner unknown stream filters remain successful empty results. Production-adapter regressions prove no storage/delegate call after rejection on SQLite and Postgres adapters.

## 5. Central Consumer Boundary

- [x] 5.1 Put direct list/scoped calls, cookie routes, owner diagnostics, scheduler probes, and singleton connection detail behind one internal observation barrier; remove route-owned correctness calls. (RECLOSED alongside 2.7: the barrier's own terminal-event fold phase is now genuinely scope-safe internally, not just at the route layer.)
- [x] 5.2 Remove TTL/stale summary value reuse and retain only equivalent in-flight promise coalescing, so no pre-repair verdict can bypass the barrier.
- [x] 5.3 Make zero/multi-connection catalog detail omit connection health/counts with typed unresolved/ambiguous evidence instead of merging siblings. (`ConnectorDetail`'s `connection_health`/`owner_state`/`rendered_verdict` are now nullable and `total_records: number | null`, `streams: []`; `buildUnresolvedConnectorDetail` no longer synthesizes any health/verdict input — genuine omission, not a fabricated zero. `test/connector-detail-connection-resolution.test.js` updated to assert `null`/`[]` omission.
  BRANCH-OWNED REGRESSION found and fixed during final re-verification (not part of Sol's original findings — surfaced by isolated base-vs-candidate re-run per the owner's directive that the whole branch, not just today's fixes, is the acceptance unit): the commit implementing this task (`e6610b946`) routed `getConnectorDetail` through the same catalog-visibility-gated (`isPublicReferenceConnector`) `projectConnectorSummaryForInstance` every LIST surface uses. `getConnectorDetail` is reached by an owner-addressed connector_id, not catalog browsing, so that gate is the wrong question for it — it made a private/unlisted connector's real, unambiguous, already-resolved default-account connection invisible through its own detail route (`collection-report-projection-e2e.test.js` 5/5 and `collection-profile.test.js` 21/129 regressed). Root-caused to TWO independent, additive defects, both fixed:
  (a) `getConnectorDetail`'s connection resolution applied catalog-visibility gating in two places (`projectConnectorSummaryForInstance`, `isActiveVisibleConnectorInstance`); both now take a `requireCatalogVisibility` option (default `true`, preserving list/route behavior unchanged) that `getConnectorDetail` passes `false` for.
  (b) The zero/multi-connection `buildUnresolvedConnectorDetail` omission over-corrected: it hardcoded `streams: []`, but declared stream NAMES are a connector-level catalog fact owned by the registered manifest, not per-connection evidence (distinct from `record_count`/`last_updated`, which genuinely are per-connection and stay `null`) — `manifest_excerpt`/`display_name`/`manifest_version` were already correctly exempt from the omission, `streams` was not. Now built from `manifest.streams` via the same honest-unobserved `buildStreamSummary(stream, null)` shape a resolved connection's never-observed stream uses. `test/connector-detail-connection-resolution.test.js`'s three resolution-state tests updated to assert declared stream names present with `record_count: null`, not `streams: []`.
  A THIRD contributing cause was a genuine, pre-existing test-fixture bug, not a product defect: `collection-report-projection-e2e.test.js`'s `issueOwnerToken` defaulted to subject `test_user`, while `getConnectorDetail`/every owner-dashboard read is intentionally hardcoded to the single reference-server owner (`REFERENCE_OWNER_SUBJECT_ID`/`OWNER_AUTH_DEFAULT_SUBJECT_ID` = `owner_local`, a real security boundary, not an oversight) — `collection-profile.test.js`'s equivalent helper already correctly defaulted to `owner_local`. Fixed by changing the default to `owner_local` to match. AN EARLIER, REJECTED APPROACH during this investigation attempted to fix the fixture mismatch by collapsing every owner-token write to `owner_local` in product code (`resolveOwnerConnectorNamespace`, `ownerSubjectIdForBindings`, `resolveGrantManifest`, `search.js`/`search-semantic.js` fan-in) — this was explicitly rejected by the owner and fully reverted: it broke real multi-subject behavior (an owner token issued with a distinct subject, e.g. `e2e_owner`, must still materialize AND read back under that real subject; only fixtures that intentionally want `owner_local` dashboard visibility should issue a token with that subject explicitly). No product-code owner-subject resolution logic changed for this task; the fix is entirely in `ref-control.ts`'s connection-resolution/omission boundary plus the one fixture default.
  New end-to-end regression test `test/connector-detail-default-account-and-ambiguity.test.js` proves the no-explicit-instance/default-account journey (using an explicit `owner_local` token, the supported dashboard-visible journey) resolves to real, visible data on an unlisted connector's own detail route AND that a genuinely zero-connection connector still correctly omits per-connection health/counts (while still surfacing declared stream names) via the typed unresolved projection — both properties together, on the real HTTP path. Two `collection-profile.test.js` assertions that predated this architecture change and asserted the old always-connector-wide-visible behavior for a zero-connection run were updated to assert the ratified spec's typed omission instead (design.md "Health boundary" / spec.md: "With zero or multiple connections it SHALL omit connection health/counts" — deliberately symmetric, not an accident); the run/spine/progress-event assertions in those same tests, which verify real unrelated behavior, were left untouched.)
- [x] 5.4 Remove summary read failure fallback to `[]`; preserve owner-safe output while forcing explicit per-connection projection failure. (RECLOSED alongside 2.5: discovery/terminal-fold failures whose marker write also fails now DO force explicit projection failure via the in-memory overlay.)

## 6. Throughput and Backend Integration

- [x] 6.1 Prove device changed-ingest, partial-prefix resume, accepted replay, direct writers/deletes, connector invalidation, manifest/backfill ordering, and index failures preserve both throughput fencing and summary convergence. (RECLOSED alongside 2.3: "connector invalidation" coverage now exercises the counter-only namespace case on both backends.)
- [x] 6.2 Prove the summary primitive uses the one SQLite/Advisory-lock coordinator domain, never holds two instance fences, and adds no record-acceptance rollback dependency. (`connector-summary-evidence-engine.ts`'s repair path calls `withConnectorInstanceWrite` exactly once per candidate, the same fence every record-ingest/delete path uses; derived repair runs entirely outside any record/device acceptance transaction.)
- [x] 6.3 Run every forced fixture against SQLite and a real disposable Postgres database; Postgres cases may not skip in the acceptance receipt. (The earlier 84/84 forced-Postgres receipt is historical. This integration re-ran its affected SQLite/dedicated-Postgres production matrix with no skips: 26/26 pass, including manifest generation registration, old-violation migration, historical terminal-fact fencing, undeclared-write provenance, deletion cleanup, actor-aware rejection-before-storage, and local STATE privacy/parser parity.)
- [x] 6.4 Instrument N=1 and N=25 current-list reads: fixed discovery/evidence query count, K-only candidate repair work, and scoped-read query bounds on both backends. (RECLOSED alongside 2.7: the new `connector-summary-evidence-scoped-fold-unrelated-terminal-history.test.js` measures ROWS scanned/returned (not prepare/pool-call counts) against 4,001 unrelated terminal events on both backends, closing the exact methodology gap Sol identified. The pre-existing connection-count N-slope test (`connector-summary-evidence-scoped-route-n-slope.test.js`) remains valid as a complementary, narrower regression guard and continues to pass.)

## 7. Verification and Landing

- [x] 7.1 Run focused summary, record, delete/reset, terminal fold, retained-size, health, cache/consumer, startup, throughput, and owner-console suites. (The integrated focused SQLite/dedicated-Postgres matrix passes 26/26 without skips; console view-model and stream suites pass.)
- [x] 7.2 Run changed-file quality gates, generated-contract checks, workspace typechecks, and repository local-CI signoff with raw receipts. (Integrated 2026-07-21: reference and polyfill full suites pass; all workspace typechecks pass; strict OpenSpec validation and diff check pass; touched-file lint is non-positive versus the base baseline. The exact integration receipt records command-level evidence.)
- [x] 7.3 Run `openspec validate reconcile-active-summary-evidence --strict` and `openspec validate --all --strict`. (Revalidated after integration prose/task updates: both named changes pass and `--all --strict` reports 74 passed, 0 failed.)
- [x] 7.4 Read every touched file and grep affected semantic names/patterns for stale or parallel contracts, including the old retained-size count authority and value-cache paths. (Confirmed zero remaining `return_fresh`/`return_stale_refresh`/`freshUntil`/`staleUntil` production references; old rebuild/reconcile/count-aggregation functions in `connector-summary-read-model.ts` fully removed, not just superseded.)
- [x] 7.5 Obtain a separate GPT-5.6 Sol final evidence verdict over the actual diff and raw oracle receipts; resolve every P0/P1. (Closed by a fresh independent Opus 4.8 final semantic gate at 96% LAND confidence in `tmp/workstreams/summary-evidence-opus-final-0717.md`, followed by the GPT-5.6 Sol RI owner's direct inspection of the exact P1 closure diff, merge tree, receipts, and branch state. No P0/P1/P2 remained.)
- [ ] 7.6 Open and merge a PR only after every deterministic/local gate and the independent verdict passes; do not deploy, mutate live state, use credentials/payloads, run hosted CI, or reset credits. (Not done in this integration: no push, merge, deployment, hosted CI, live mutation, or credential/payload use.)
