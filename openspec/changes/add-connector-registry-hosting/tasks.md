# Tasks

## 1. Runtime Topology

- [x] Add the `registry-mock` service to `docker-compose.neko.yml`.
- [x] Make the reference service wait for the registry mock in the n.eko overlay.
- [x] Keep connector IDs on the existing `registry.pdpp.org` manifest identity.

## 2. Validation

- [x] Validate the OpenSpec change.
- [x] Validate the compose overlay.
- [ ] Rebuild and redeploy the local Docker stack from pushed branch artifacts.
- [ ] Verify manifest fetch from the reference container.
