## 1. Current-State Audit

- [x] 1.1 Inventory existing dashboard/session owner control routes for connectors, connections, runs, schedules, diagnostics, delete/revoke, and display-name mutation. (Read-only audit: `tmp/workstreams/ri-owner-agent-control-audit-v1-report.md`, "Route inventory table".)
- [x] 1.2 Inventory owner-agent bearer authorization paths and identify which owner-session operations can safely share handlers with owner-agent REST. (Audit report "Auth boundary table" + "Safe implementation lanes".)
- [x] 1.3 Capture the current Amazon evidence: template `amazon` listing, record-level `connection_id`, registry-URL fallback `display_name`, and missing owner-agent connection-initiation path. (Audit report "Amazon / multi-connection evidence".)
- [x] 1.4 Record the selected route-family decision in `design.md`: extend `/_ref/*` for owner-agent bearer access or introduce a cleaner owner REST family. (Audit report "Route-family recommendation": introduce `/v1/owner/*`; realized by `GET /v1/owner/connections` and `GET /v1/owner/control`.)

## 2. Contract And Metadata

- [x] 2.1 Define owner-agent control metadata in root/protected-resource discovery, including entrypoint URL, action families, and unsupported-action semantics. (`pdpp_owner_agent_onboarding.control_surface` hint + bearer-authed `GET /v1/owner/control` capability document, both projected from `buildOwnerAgentControlSurface`; supported families carry method + URL, owner-mediated/unsupported families are named with a typed `status` and reason. Lane `ri-owner-agent-control-entrypoint-v1`.)
- [x] 2.2 Define connector template and connection instance response shapes with `connector_id`/`connector_key`, `connection_id`, deprecated `connector_instance_id` compatibility, `display_name`, label status, lifecycle status, and supported actions. (Connection-instance shape landed via `ownerListConnections` reference-contract op + `OwnerConnectionSchema`: `connection_id`, deprecated `connector_instance_id`, `connector_id`/`connector_key`, `display_name`, `label_status`, lifecycle fields, and per-instance `supported_actions` projected from the shared control catalog. Connector-template shape now landed via `ownerListConnectorTemplates` reference-contract op + `OwnerConnectorTemplateSchema`: `connector_id`/`connector_key`, `display_name`, `connector_modality`, `stream_count`, `connection_count`, embedded `owner_connection_summary` rows (`connection_id`, deprecated `connector_instance_id`, label/lifecycle fields), and template-level `supported_actions`. `GET /v1/owner/connector-templates` lists registered templates plus the reference local-collector catalog; Amazon is truthfully marked browser-bound with unsupported `initiate_connection`, while codex/claude-code advertise the supported local-collector enrollment intent. Tests pin the route and control-catalog advertisement.)
- [x] 2.3 Define typed connection-intent response shapes for OAuth, browser assistance, upload/import, local-collector enrollment, and unsupported connectors. (`ownerCreateConnectionIntent` reference-contract op + `OwnerConnectionIntentRequestSchema` / `OwnerConnectionIntentNextStepSchema` / `OwnerConnectionIntentResponseSchema`: `next_step.kind` enum reserves `open_url` / `complete_browser_assistance` / `upload_file` / `enroll_local_collector` / `unsupported`; response carries `connector_modality` and `connection_active: false`. Lane `ri-owner-agent-connection-initiation-clean-v1`.)
- [x] 2.4 Add typed error envelopes for ambiguous connector-only actions, unsupported actions, missing owner-agent action family, and unsafe provider step. (Contract-level envelopes landed. (a) **Ambiguous connector-only action**: the shared `ErrorObjectSchema` (`pdpp/common/PdppError`) now declares the optional `available_connections` (typed `pdpp/common/ErrorAvailableConnection` items: `connection_id` + `connector_id`/`connector_key`/`display_name`/`label_status`) and `retry_with` fields that the runtime `pdppError` helper + the existing `AmbiguousConnectionError` (code `ambiguous_connection`) already emit — closing a latent contract lie where `additionalProperties: false` made those fields illegal. (b) **Unsupported action** and (c) **missing/owner-mediated action family** are typed as response data, not HTTP errors: each `supported_actions` entry and each `GET /v1/owner/control` family carries `status: supported|owner_mediated|unsupported` + reason, so an agent never probes a 404. (d) **Unsafe provider step** is typed via the connection-intent `next_step.kind: "unsupported"` with a named-gap reason. The 401-only `resource_metadata`/`next_step` hints `pdppError` emits are also now declared. Proven by `packages/reference-contract/test/owner-control-contract.test.js` (ambiguity envelope validates, plain typed error validates, undeclared field still fails closed). Firing the ambiguity error needs a connector-only-addressed mutating route, which is task 6.2's instance-scoping lane. Lane `ri-owner-agent-control-next-v1`.)

## 3. Authorization And Audit

- [ ] 3.1 Add explicit owner-agent bearer allowlisting for the selected owner control routes without making `/mcp` accept owner bearers. (Partial: `GET /v1/owner/control`, `GET /v1/owner/connections`, the mutating rename route `PATCH /v1/owner/connections/{connectionId}`, the four schedule pause/resume routes (`POST /v1/owner/connections/{connectionId}/schedule/{pause,resume}` + `POST /v1/owner/connectors/{connectorId}/schedule/{pause,resume}`), and the two schedule delete routes (`DELETE /v1/owner/connections/{connectionId}/schedule` + `DELETE /v1/owner/connectors/{connectorId}/schedule`) are all gated by `requireToken` + `requireOwner`; client/`mcp_package` bearers → 403, missing bearer → 401, `/mcp` owner-bearer rejection re-pinned by `test/owner-connection-rename.test.js` and `test/owner-connection-schedule.test.js`. Remaining mutating control routes — run-now/connection-delete/revoke — remain for later lanes. Lanes `ri-owner-agent-instance-ops-v1`, `ri-owner-agent-schedule-lifecycle-v2`.)
- [ ] 3.2 Share control operation handlers between browser owner sessions and owner-agent bearers where semantics match, while keeping auth adapters separate. (Partial: rename shares the connector-instance store `setDisplayName` semantics between the cookie-authed `PATCH /_ref/connections/:id` and the owner-bearer `PATCH /v1/owner/connections/:connectionId`. Schedule pause/resume shares the controller `setScheduleEnabled` semantics and schedule delete now shares the controller `deleteSchedule` semantics (204 on delete, typed 404 when no schedule existed, scheduler refresh on success) — both owner-scoped via the connector-instance namespace resolver — between the cookie-authed `/_ref/.../schedule/{pause,resume}` + `/_ref/.../schedule` DELETE routes and the owner-bearer `/v1/owner/.../schedule/{pause,resume}` + `/v1/owner/.../schedule` DELETE routes, under separate `requireOwnerSession` vs `requireToken`+`requireOwner` adapters. Proven by `test/owner-connection-schedule.test.js` ("resume is blocked when the connector refresh policy forbids automation" pins the shared eligibility gate; the delete suite pins the shared 204/404 semantics). Run-now handler sharing remains for later lanes. Lanes `ri-owner-agent-rename-control-v1`, `ri-owner-agent-instance-ops-v1`, `ri-owner-agent-schedule-lifecycle-v2`.)
- [ ] 3.3 Add non-secret audit evidence for owner-agent mutations: actor kind, client id/name, target resource, operation, result, and request id. (Partial: owner-agent connection rename emits `owner_agent.connection.rename` spine evidence; owner-agent schedule pause/resume/delete emit `owner_agent.connection.schedule` spine evidence for success, typed ambiguity (`connector_id` selector), schedule-not-found (including the delete no-op 404), and client-token authorization failure. Tests assert actor kind, client identity, target `connection_id`/`connector_key`, selector, operation (`pause_schedule`/`resume_schedule`/`delete_schedule`), outcome, request id, and that bearer tokens are not logged (`token_id: null`). Run-now/connection-delete/revoke mutation audit remains for later owner-agent routes.)
- [x] 3.4 Add revocation/authorization tests proving a revoked owner-agent credential cannot perform read or control operations. (`test/owner-connection-schedule.test.js` → "a revoked owner-agent credential cannot read connections or control a schedule": issues an owner bearer, proves it reads (`GET /v1/owner/connections` → 200) and controls (pause → 200), then revokes it the way RFC 7592 client deletion does (cascade-revoke the issuing client's tokens via `authTokensRevokeByClientId`); the revoked credential's read and resume both return 401 `authentication_error`, and the schedule row is verifiably untouched by the revoked control attempt. Lane `ri-owner-agent-instance-ops-v1`.)

## 4. Connection Discovery And Labels

- [x] 4.1 Implement owner-agent connector-template listing with links or embedded summaries for configured connection instances. (`GET /v1/owner/connector-templates`, bearer-authed; embeds related connection summaries and template-level `supported_actions`; lane completed by owner follow-through after worker stop.)
- [x] 4.2 Implement owner-agent connection-instance listing with owner-meaningful labels or explicit label-needed state. (`GET /v1/owner/connections`, bearer-authed; lane `ri-owner-agent-connections-list-v1`.)
- [x] 4.3 Ensure display-name fallback values such as registry URLs are exposed as fallback/label-needed rather than treated as final SLVP labels. (`label_status: owner_set | fallback` via `projectStorageDisplayName`.)
- [x] 4.4 Implement or extend owner-agent rename support so a trusted agent can label Amazon instances as `the owner personal` and `Shared Amazon`. (`PATCH /v1/owner/connections/{connectionId}`, bearer-authed via `requireToken` + `requireOwner`; reuses the connector-instance store `setDisplayName` rename semantics shared with the cookie-authed `/_ref` PATCH; response re-projects through `projectOwnerConnection` so a labeled row reports `label_status: owner_set` and a follow-up `GET /v1/owner/connections` reflects the new `display_name`. Two-Amazon `the owner personal` / `Shared Amazon` acceptance covered by `test/owner-connection-rename.test.js`. Lane `ri-owner-agent-rename-control-v1`.)

## 5. Connection Lifecycle Intents

- [x] 5.1 Implement typed connection-intent creation for at least one supported connector path without completing provider authentication silently. (`POST /v1/owner/connections/intents`, bearer-authed via `requireToken` + owner guard. Local-collector connectors (`claude-code`, `codex`) mint a real single-use enrollment code via the shared `deviceExporterStore.createEnrollmentCode`; the owner's collector performs any provider step locally. Lane `ri-owner-agent-connection-initiation-clean-v1`.)
- [x] 5.2 Return `open_url`, `complete_browser_assistance`, `upload_file`, `enroll_local_collector`, or `unsupported` next-step types as appropriate. (Emits `enroll_local_collector` for proven local-collector connectors and typed `unsupported` for browser-bound, API/network-only, and unknown connectors, each with a reason naming the missing primitive. `open_url` / `complete_browser_assistance` / `upload_file` are contract-reserved for future primitives.)
- [ ] 5.3 Add Amazon-specific acceptance coverage proving a trusted owner agent can initiate the second-account flow up to the owner-mediated next step. (Partial/honest: Amazon's intent is covered by `test/owner-connection-intent.test.js` returning a typed `unsupported` whose reason names the missing browser-collector primitive — the acceptance-permitted alternative to faking it. Flipping Amazon to a real enroll next step is gated on the browser-collector enrollment packet in `design.md` ("Resolved: Amazon second-account implementation packet").)
- [x] 5.4 Ensure connection instances are not marked active until provider authorization, upload/import, or local enrollment completes. (Every intent response carries `connection_active: false`; the route writes no `connector_instances` row. Test "the minted enrollment code is genuine" proves the instance only materializes on device enroll + ingest, after the intent.)

## 6. Instance-Scoped Operations

- [ ] 6.1 Make run-now, schedule, pause, resume, diagnostics, delete/revoke, and rename operations instance-scoped where they affect configured bindings. (Partial: rename (`PATCH /v1/owner/connections/{connectionId}`), schedule pause/resume (`POST /v1/owner/connections/{connectionId}/schedule/{pause,resume}`), and schedule delete (`DELETE /v1/owner/connections/{connectionId}/schedule`) are instance-scoped by `connection_id`; the connector-only schedule routes (`POST /v1/owner/connectors/{connectorId}/schedule/{pause,resume}` + `DELETE /v1/owner/connectors/{connectorId}/schedule`) resolve to a single configured binding via the owner-scoped connector-instance namespace resolver, auto-selecting a sole active connection or raising typed ambiguity. Run-now, diagnostics, and connection-delete/revoke remain owner-mediated/unsupported and open. Lanes `ri-owner-agent-instance-ops-v1`, `ri-owner-agent-schedule-lifecycle-v2`.)
- [x] 6.2 Reject connector-only stateful actions with typed ambiguity when multiple instances exist, including available `connection_id` values and labels. (The connector-only schedule routes catch the store's `ambiguous_connector_instance` and re-throw the typed `ambiguous_connection` (HTTP 409) error carrying `available_connections: [{ connection_id, display_name? }]` (owner-meaningful labels via `projectBindingForWire`) and `retry_with: "connection_id"`. Proven by `test/owner-connection-schedule.test.js` → "owner-agent connector-only action rejects two active connections with typed ambiguous_connection" (two active Amazon connections → 409 listing both `connection_id`s + labels `the owner personal`/`Shared Amazon`). Lane `ri-owner-agent-instance-ops-v1`.)
- [x] 6.3 Preserve single-instance compatibility where a connector-only operation can safely auto-select the only configured instance. (The connector-only schedule routes resolve through `resolveOwnerConnectorNamespace(connectorId)`, which auto-selects the sole active connection when exactly one exists. Proven by `test/owner-connection-schedule.test.js` → "owner-agent connector-only pause auto-selects the single active connection" (the audit records the auto-selected concrete `connection_id`). Lane `ri-owner-agent-instance-ops-v1`.)
- [x] 6.4 Verify public read results and owner-agent control listings agree on `connection_id` and `display_name` after rename. (Rename agreement covered by `test/owner-connection-rename.test.js`; schedule-control agreement covered by `test/owner-connection-schedule.test.js` → "owner-agent listing agrees on connection_id and reflects the paused schedule after control" (a `GET /v1/owner/connections` row reports the same `connection_id` with `schedule.enabled` flipped to false after an owner-agent pause). Lane `ri-owner-agent-instance-ops-v1`.)

## 7. CLI, Docs, And Agent Guidance

- [x] 7.1 Extend `pdpp owner-agent status` or a new subcommand to show non-secret control capabilities and connection listing. (New focused subcommand `pdpp owner-agent control` (`packages/cli/src/owner-agent/control.js`, wired in `command.js` + top-level help in `index.js`). It reads the stored credential like `status`, then fetches the bearer-authed `GET /v1/owner/control` capability document and `GET /v1/owner/connections` listing and prints a non-secret report: each action family with its `status` (`supported`/`owner_mediated`/`unsupported`) + method/URL, and each connection's `connection_id`, connector identity, and label/label-needed state. The bearer is sent as an `Authorization` header and never printed; a 401/403 maps to a bounded `control_unauthorized` (exit 4) pointing at `status`. Covered by four new cases in `test/owner-agent.test.js` (capability+connection listing with no-bearer-leak assertion, zero-connection path, unauthorized→exit 4, not_onboarded) and a live-server assertion block added to `test/owner-agent-reference-smoke.test.js`. Lane `ri-owner-agent-cli-docs-v1`.)
- [x] 7.2 Add owner-agent guidance for initiating new connections, labeling instances, and avoiding raw bearer/token output. (New `docs/agent-skills/pdpp-owner-agent/references/control-surface.md` reference covers capability discovery (`pdpp owner-agent control` / `GET /v1/owner/control`), listing instances vs templates, labeling `fallback`/label-needed rows with `rename_connection`, and initiating a typed connection intent (`POST /v1/owner/connections/intents`) with `enroll_local_collector`/`unsupported`/reserved next-step branching — including the honest Amazon second-account `unsupported` stop. A "Secret hygiene" section forbids printing the bearer, the intent `enrollment_code`, owner-session cookies, and `whsec_` secrets. Wired into `SKILL.md` (new job 5, Core-workflow §6, enrollment-code + owner_mediated/unsupported stop conditions) and `daisy-runbook.md` (new Step 5b). Lane `ri-owner-agent-cli-docs-v1`.)
- [x] 7.3 Update dashboard/operator copy so trusted local agents have a smooth connection-management path distinct from debug bearer copy. (Verified-no-change-needed: the owner-agent tokens page `apps/console/src/app/dashboard/deployment/tokens/page.tsx` already titles itself "Owner-agent access", leads with the trusted onboarding card ("Set up trusted local owner automation without pasting bearer material"), explicitly demotes the manual bearer to "for debugging", and the `OwnerScopeCallout` routes ordinary MCP clients to the scoped-grant flow and states `/mcp` rejects owner bearers on purpose. No stale debug-token phrase presents bearer-copy as the owner-agent path, so no copy edit was warranted. Lane `ri-owner-agent-cli-docs-v1`.)
- [x] 7.4 Document that routine chat-hosted agents should still use scoped grants/MCP and not owner-agent admin credentials. (`control-surface.md` "Boundary" section states trusted local owner agents MAY use owner-bearer `/v1/owner/*`, routine chat-hosted/external/task-scoped agents MUST use a scoped client grant over `/mcp` or scoped `/v1/*` via `pdpp-data-access`, and `/mcp` rejects owner bearers by design. Reinforced in `SKILL.md` and in `packages/cli/README.md` ("Routine chat-hosted and task-scoped agents should use the grant-scoped `pdpp connect` / MCP path instead, not an owner bearer."). Lane `ri-owner-agent-cli-docs-v1`.)

## 8. Validation

- [x] 8.1 Add local integration tests for metadata discovery, owner-agent control authorization, connection listing, label-needed state, rename, and typed ambiguity. (Metadata discovery + control authorization: `test/owner-control-entrypoint.test.js`; connection listing + label-needed state: `test/owner-connections-list.test.js`; rename: `test/owner-connection-rename.test.js`; the previously-missing typed-ambiguity path is now covered by `test/owner-connection-schedule.test.js` ("connector-only action rejects two active connections with typed ambiguous_connection"), which fires a real connector-only-addressed mutating route and asserts the 409 `ambiguous_connection` envelope with `available_connections` + `retry_with`. Lane `ri-owner-agent-instance-ops-v1`.)
- [x] 8.2 Add connection-intent tests for supported, browser-assisted, and unsupported connector types. (`test/owner-connection-intent.test.js`: local-collector success, browser-bound Amazon typed unsupported, API/network-only Gmail typed unsupported, unknown connector, invalid inputs, client-token rejection, missing bearer, `/mcp` owner-bearer rejection, and control-surface discoverability.)
- [x] 8.3 Run `openspec validate add-owner-agent-control-surface --strict` and `openspec validate --all --strict`. (Both pass green this lane: change validates; `--all --strict` reports 35 passed, 0 failed. Lane `ri-owner-agent-instance-ops-v1`.)
- [x] 8.4 Run targeted reference implementation tests for owner-agent auth, connector routes, connection instance storage, and MCP owner-bearer rejection. (Green: all `test/owner-*.test.js` (114 tests inc. the schedule suite's 8 new delete tests), `test/control-actions.test.js` + `test/ref-connectors-routes.test.js`, `test/route-contract-validation.test.js`, `test/owner-auth.test.js`, and `test/provider-metadata.test.js` + `test/rs-protected-resource-metadata-operation.test.js` (125 across those six suites) all pass; `/mcp` owner-bearer rejection re-pinned by the schedule suite. Lanes `ri-owner-agent-instance-ops-v1`, `ri-owner-agent-schedule-lifecycle-v2`.)
- [ ] 8.5 Run a live Daisy/Simon-equivalent smoke proving a trusted owner agent can list connection instances, label one, initiate a new Amazon connection intent, and stop at the owner-mediated provider step.
