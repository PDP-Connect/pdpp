## Why

The rejected 910-file RI checkpoint mixed formatter output, semantic rewrites,
critical backend files, and generated OpenAPI without a PostgreSQL proof. Diagnostic
counts can fall through selection shrinkage or unsafe rule changes.

## What Changes

- Establish a fingerprinted Biome/Ultracite selection and diagnostic baseline.
- Adjudicate rules and exclusions before cleanup, with zero untriaged findings and no metric gaming.
- Keep generated artifacts generator-owned and gate byte drift.
- Rebuild cleanup as bounded policy, formatter, syntax, and semantic cohorts; never land the 910-file checkpoint as one unit.

## Capabilities

- Added: `reference-implementation-quality-tooling`

## Impact

The change affects Biome policy/configuration, selection and receipt tooling, bounded
authored-source cleanup, generated-artifact checks, and diagnostic exception records.
It does not change protocol semantics or require mass JS/MJS conversion.
