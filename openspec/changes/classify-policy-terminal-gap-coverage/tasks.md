## 1. Projection and storage

- [x] 1.1 Define shared policy-terminal reason classification.
- [x] 1.2 Add reason-scoped per-stream detail-gap aggregates for both stores.
- [x] 1.3 Exclude only policy-terminal rows from repair-blocking coverage.

## 2. Verification

- [x] 2.1 Add deterministic policy-versus-defect regression coverage.
- [x] 2.2 Run focused tests, typecheck, Ultracite, strict OpenSpec validation,
  and diff checks (the package typecheck retains one pre-existing unrelated
  test-fixture failure).
