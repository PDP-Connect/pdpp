## 1. Runtime Recovery Governor

- [x] 1.1 Add pure recovery-decision helpers that classify queued detail gaps by reason, attempt metadata, next eligible time, and provider work domain. (`reference-implementation/runtime/recovery-decision.ts`)
- [x] 1.2 Add tests proving run-cap and retry-budget deferrals remain non-source-pressure recovery work. (`reference-implementation/test/recovery-decision.test.js`, including real durable `last_error.class` rows)
- [x] 1.3 Add tests proving provider-pressure gaps block ordinary manual retry until the next eligible time. (`recovery-decision.test.js`)
- [x] 1.4 Add tests proving unrelated provider work domains do not block each other. (`recovery-decision.test.js`)
- [x] 1.5 Add the standing starvation invariant test: an active source-pressure cooldown must not make non-pressure queued recovery ineligible, and stale pressure rows without fresh evidence must not re-arm cooldown (regression shape: the live 51-holds-942 ChatGPT residue). (`filterFreshPressureRows` keeps stale pressure rows out of cooldown math; controller manual-run and scheduler dispatch gates now compute cooldown only from fresh pressure rows. Covered by `recovery-decision.test.js`, `manual-provider-safety.test.js`, and `scheduler-cooldown-recovery-eligibility.test.js`.)
- [x] 1.6 Add per-item quarantine helpers and tests: poison item reaches its per-item threshold, is quarantined with evidence and a terminal class, remains visible in accounting, and siblings keep draining. (`reference-implementation/runtime/recovery-quarantine.ts` pure decision; `maybeQuarantineGap` in `server/stores/terminal-gap-classifier.js` reuses the durable `terminal` status with a distinct `quarantined` class routed to `connector_defect`/`system_issue`; `reference-implementation/test/recovery-quarantine.test.js`)

## 2. Recovery Execution Integration

- [x] 2.1 Thread the recovery admission decision into the runtime path that serves `DETAIL_GAPS_PAGE_REQUEST` pages. (`readDetailGapPage` in `runtime/index.js` now computes `resolveRecoveryAdmission` over the store's candidate rows via `summarizeDetailGapAdmission` and returns page-level admission evidence. Recorded, NOT enforced — the store's `status='pending' AND next_attempt_after<=now` gate still selects the served set, so a below-budget owner_required/connector_defect pending row keeps its terminalization budget; gating the page on class/domain-cooldown here would be a behavior change and belongs to the scheduler/manual seams. Covered by `connector-detail-gap-store.test.js`.)
- [x] 2.2 Ensure recovery pages preserve attempt metadata and provider-budget state across pages within one run. (Attempt metadata is preserved by the existing `markGapStatus('in_progress')` increment + `upsertPendingGap` ON CONFLICT keeping `attempt_count`; the admission evidence now reads that metadata per page. Existing attempt-persistence acceptance tests still green.)
- [x] 2.3 Wire ordinary owner-started retry/refresh through the same recovery admission gate, excluding only explicit force/debug paths. (The manual gate `validateRunNowPreconditions` respects the same provider-pressure cooldown, excludes `force`, carries the connector-neutral `recoveryAdmissionReason: "cooldown"` denial class on `ControllerError`, and the shared HTTP envelope now surfaces `recovery_admission_reason`, `next_eligible_at`, and `pending_pressure_gap_count`. Covered by `manual-provider-safety.test.js`, `ref-error-status.test.js`, and full route coverage in `ref-connectors-routes.test.js`.)
- [x] 2.4 Add regression coverage that repeated owner clicks cannot bypass a provider-pressure cooldown. (`reference-implementation/test/manual-provider-safety.test.js`)
- [ ] 2.6 Record admission decisions (outcome, reason class, next eligible time, forced flag) and expose them to owner-only diagnostics; test that a denial can answer "why didn't it run". (Partial: page-serving admission evidence — admitted/deferred counts, deferral reason classes, earliest next-eligible time — is now recorded on the `DETAIL_GAPS_PAGE_RESPONSE` / `DETAIL_GAPS_START_ADMISSION` progress events and on the manual denial's `recoveryAdmissionReason`. Remaining: a durable owner-only diagnostics store/read surface for admission decisions and the forced-flag audit, plus the HTTP query that answers "why didn't the most recent attempt run".)
- [x] 2.5 Add idempotency and crash-accounting coverage: re-attempt after an interrupted attempt does not duplicate records; interrupted attempts count and repeated interruption escalates to connector/system issue. (`reference-implementation/test/recovery-quarantine.test.js`: durable-identity dedup via `records` UNIQUE(connector_instance_id, stream, record_key) + ingest upsert; `markGapStatus('in_progress')` counts each attempt before connector work and the crash-reclaim path resets in_progress→pending without decrementing; repeated interruption climbs `attempt_count` and escalates through the same per-item quarantine path as deterministic failure)
- [ ] 2.7 Wire the stall watchdog: eligible work with no attempt beyond the cadence window becomes an observable system condition; test that it observes without force-admitting.

## 3. Amazon Proof Connector

- [ ] 3.1 Move Amazon order-item detail recovery onto the connector-neutral recovery admission path.
- [x] 3.2 Preserve Amazon's per-run detail-attempt cap as a run blast-radius limit, not as the cross-run recovery scheduler. (`classifyAmazonDetailFailure("deferred_budget") === "run_cap_deferred"` and existing cap behavior remains resumable)
- [ ] 3.3 Classify Amazon detail failures into planned cap, transient DOM/no-progress, provider pressure, owner-required repair, or connector defect. (Partial: planned cap, transient no-progress, and owner-required repair are connector-local and covered; provider-pressure/connector-defect durable escalation needs runtime classifier/quarantine support.)
- [ ] 3.4 Add fixture-backed tests for repeated Amazon detail-page no-progress escalating to connector/system issue rather than owner retry busywork. (Partial: sign-in redirect owner-repair busywork is covered; the runtime-owned escalation now lands — repeated transient no-progress crosses a per-item no-progress budget and quarantines to a durable `terminal`/`quarantined` (connector/system) issue via `maybeQuarantineGap`, wired into the runtime `DETAIL_GAP` handler and proven in `test/recovery-quarantine.test.js`. Remaining: the Amazon-driven fixture-backed end-to-end run that exercises this through a real connector transcript.)

## 4. Connection Health And Actionability

- [x] 4.1 Add typed recovery state to the rendered verdict or the connection-health projection consumed by owner surfaces. (`apps/console/src/app/(console)/lib/source-recovery-state.ts`)
- [x] 4.2 Replace indefinite "Checking" projection paths with active-check, queued-recovery, cooldown, unknown-unmeasured, read-failure, or system-issue states. (PR #190 split inactive unknown from active Checking; this tranche routes recovery states to named copy across dashboard/Sources/Syncs/source-detail. Inactive durable backlogs route to named recovery even against an adversarial "Checking" pill (`source-actionability.test.ts` task-4.2 test); read-failure uses the scoped `records-read-failure-banner`, not per-source Checking; system-issue routes via `recoveryStateGroup`. The console never invents "Checking" for an inactive state.)
- [x] 4.3 Update shared source-actionability grouping so queued/cooling recovery is passive progress, eligible recovery is owner-runnable, and connector defects are system issues. (`source-actionability.ts` recovery routing)
- [x] 4.4 Add dashboard, Sources, Syncs, and source-detail tests proving group counts equal rendered rows and no inactive queued recovery row says "Checking". (Dashboard: `standing-view-model.test.ts` asserts every `sourceWorkSection.countLabel` equals its rendered `rows.length` and the inactive-recovery row is passive progress, never Checking. Syncs: `syncs-model.test.ts` asserts `band.needsReview === failureCards.length` and the inactive recovery card is passive, never needs-you/Checking. Source-detail: `connection-diagnostics.test.ts` + `source-recovery-state.test.ts`.)
- [x] 4.5 Add stalled-recovery projection coverage: queued/catching-up carries recency evidence, and eligible work with no attempt beyond the cadence window renders as a system issue rather than indefinite passive progress. (The source-detail `ConnectionDiagnostics` now arms the stall watchdog with the server render instant and `RECOVERY_STALL_CADENCE_MS` (6h) threaded from the `force-dynamic` page; a stale attempt floor beyond the window renders as a `stalled` system condition. Covered by `source-recovery-state.test.ts` (behavioral) and `connection-diagnostics.test.ts` (wiring).)
- [x] 4.6 Implement the source-row UI contract: one concrete primary sentence, one evidence line, and at most one primary action. (Recovery rows and the source-detail recovery panel follow the contract: one primary sentence, evidence rendered separately, and no bypass action field on the panel view-model. Pinned by the `source-recovery-state.test.ts` "one primary sentence, evidence separate, no bypass action" invariant across active/queued/cooling/stalled steps.)
- [x] 4.7 Add source-detail recovery panel view-model support for current step, progress counts, next eligible attempt, blocker, and recent non-secret evidence. (`buildRecoveryPanelViewModel` + `RecoveryPanel`)
- [x] 4.8 Add owner-surface tests proving active recovery names the work, such as "Syncing order details", instead of generic "Checking".
- [x] 4.9 Add owner-surface tests proving unsafe retries show wait or blocker copy and no normal retry CTA.

## 5. Validation And Live Proof

- [x] 5.1 Run `openspec validate add-connector-neutral-recovery-governor --strict`.
- [x] 5.2 Run targeted runtime detail-gap, scheduler/manual-provider-safety, rendered-verdict, and console source-actionability tests.
- [ ] 5.3 Deploy from clean `main` after tests pass and no active run would be interrupted.
- [ ] 5.4 Run one supervised Amazon recovery batch and verify it recovers eligible work, respects any cooldown, and leaves the owner UI with typed recovery state rather than repeated retry instructions.
- [ ] 5.5 Verify the live ChatGPT residue (942 non-pressure gaps held by stale pressure rows) begins draining under the new eligibility rule without manual data cleanup, and keep that dataset as the regression proof.
