## 1. Runtime Recovery Governor

- [x] 1.1 Add pure recovery-decision helpers that classify queued detail gaps by reason, attempt metadata, next eligible time, and provider work domain. (`reference-implementation/runtime/recovery-decision.ts`)
- [x] 1.2 Add tests proving run-cap and retry-budget deferrals remain non-source-pressure recovery work. (`reference-implementation/test/recovery-decision.test.js`, including real durable `last_error.class` rows)
- [x] 1.3 Add tests proving provider-pressure gaps block ordinary manual retry until the next eligible time. (`recovery-decision.test.js`)
- [x] 1.4 Add tests proving unrelated provider work domains do not block each other. (`recovery-decision.test.js`)
- [x] 1.5 Add the standing starvation invariant test: an active source-pressure cooldown must not make non-pressure queued recovery ineligible, and stale pressure rows without fresh evidence must not re-arm cooldown (regression shape: the live 51-holds-942 ChatGPT residue). (`filterFreshPressureRows` keeps stale pressure rows out of cooldown math; controller manual-run and scheduler dispatch gates now compute cooldown only from fresh pressure rows. Covered by `recovery-decision.test.js`, `manual-provider-safety.test.js`, and `scheduler-cooldown-recovery-eligibility.test.js`.)
- [x] 1.6 Add per-item quarantine helpers and tests: poison item reaches its per-item threshold, is quarantined with evidence and a terminal class, remains visible in accounting, and siblings keep draining. (`reference-implementation/runtime/recovery-quarantine.ts` pure decision; `maybeQuarantineGap` in `server/stores/terminal-gap-classifier.js` reuses the durable `terminal` status with a distinct `quarantined` class routed to `connector_defect`/`system_issue`; `reference-implementation/test/recovery-quarantine.test.js`)

## 2. Recovery Execution Integration

- [x] 2.1 Thread the recovery admission decision into the runtime path that serves `DETAIL_GAPS_PAGE_REQUEST` pages. (`readDetailGapPage` in `runtime/index.js` now computes `resolveRecoveryAdmission` over the store's candidate rows via `summarizeDetailGapAdmission` and returns page-level admission evidence. Recorded, NOT enforced — the store's `status='pending' AND next_attempt_after<=now` gate still selects the served set, so a below-budget owner_required/connector_defect pending row keeps its terminalization budget; gating the page on class/domain-cooldown here would be a behavior change and belongs to the scheduler/manual seams. Covered by `connector-detail-gap-store.test.js`.)
- [x] 2.2 Ensure recovery pages preserve attempt metadata and provider-budget state across pages within one run. (Attempt metadata is preserved by the existing `markGapStatus('in_progress')` increment + `upsertPendingGap` ON CONFLICT keeping `attempt_count`; the admission evidence now reads that metadata per page. Existing attempt-persistence acceptance tests still green.)
- [x] 2.3 Wire ordinary owner-started retry/refresh through the same recovery admission gate, excluding only explicit force/debug paths. (The manual gate `validateRunNowPreconditions` respects the same provider-pressure cooldown, excludes `force`, carries the connector-neutral `recoveryAdmissionReason: "cooldown"` denial class on `ControllerError`, and the shared HTTP envelope now surfaces `recovery_admission_reason`, `next_eligible_at`, and `pending_pressure_gap_count`. Covered by `manual-provider-safety.test.js`, `ref-error-status.test.js`, and full route coverage in `ref-connectors-routes.test.js`.)
- [x] 2.4 Add regression coverage that repeated owner clicks cannot bypass a provider-pressure cooldown. (`reference-implementation/test/manual-provider-safety.test.js`)
- [x] 2.6 Record admission decisions (outcome, reason class, next eligible time, forced flag) and expose them to owner-only diagnostics; test that a denial can answer "why didn't it run". (Page-serving admission evidence — admitted/deferred counts, deferral reason classes, earliest next-eligible time — is recorded on the `DETAIL_GAPS_PAGE_RESPONSE` / `DETAIL_GAPS_START_ADMISSION` progress events and on the manual denial's `recoveryAdmissionReason`. The durable owner-only READ now lands: `GET /v1/owner/connections/:connectionId/diagnostics` (`ref-control.ts` `getOwnerConnectionDiagnostics` → `buildOwnerConnectionDiagnosticsRecovery`) carries a `recovery.admission` block that RE-DERIVES `resolveRecoveryAdmission` over the connection's durable `connector_detail_gaps` rows — no new store, substrate unchanged per design.md rollback — and answers "why didn't the most recent attempt run" via a top-line `why_not_now` reason (owner_required > system_issue > budget > cooldown) plus `next_eligible_at`. Pure decision `summarizeRecoveryAdmissionDiagnostics` in `runtime/recovery-decision.ts`; covered by `recovery-decision.test.js` (pure) and `owner-connection-diagnostics.test.js` (end-to-end cooldown + owner_required denials). Explicit owner-agent and owner-session force paths now emit non-secret `owner_agent.connection.run` audit events with `forced: true|false`, `run_id`, actor, selector, and connection id; covered by `owner-connection-run.test.js` and `ref-connectors-run-force-route.test.js`.)
- [x] 2.5 Add idempotency and crash-accounting coverage: re-attempt after an interrupted attempt does not duplicate records; interrupted attempts count and repeated interruption escalates to connector/system issue. (`reference-implementation/test/recovery-quarantine.test.js`: durable-identity dedup via `records` UNIQUE(connector_instance_id, stream, record_key) + ingest upsert; `markGapStatus('in_progress')` counts each attempt before connector work and the crash-reclaim path resets in_progress→pending without decrementing; repeated interruption climbs `attempt_count` and escalates through the same per-item quarantine path as deterministic failure)
- [x] 2.7 Wire the stall watchdog: eligible work with no attempt beyond the cadence window becomes an observable system condition; test that it observes without force-admitting. (`deriveRecoveryStall` in `runtime/recovery-decision.ts` is a pure observe-only decision: a row counts toward a stall only when `resolveRecoveryAdmission` would ADMIT it now — correctly-deferred cooldown/owner-required/defect work is never a stall — and it fires when the newest attempt across eligible rows is older than the cadence window (`RECOVERY_STALL_CADENCE_MS` = 6h, matching the console surface's constant) or no attempt was ever recorded. Surfaced runtime-side on the owner-only diagnostics read (`recovery.stall.{stalled,eligibleCandidates,lastAttemptAt}` via `buildOwnerConnectionDiagnosticsRecovery`), visible to owner-only diagnostics as a system condition rather than silent queue rot. Observe-only is proven: `recovery-decision.test.js` pins that it never mutates input rows and never admits deferred work; `owner-connection-diagnostics.test.js` proves an eligible-but-unattempted backlog surfaces `stalled: true` while remaining admissible. It is a report only — it never force-admits or bypasses a cooldown. Complements the console-projection stall watchdog (task 4.5, `apps/console/.../source-recovery-state.ts`), reading the durable gap rows' real attempt recency rather than the projected backlog.)
- [x] 2.8 Classify browser-profile attach/session-close failures as runtime-retryable, not connector-upgrade defects. (`runtime/index.js` now maps the known CDP `Network.setCacheDisabled` / `session closed` browser-profile failure to `retry_by_runtime`; `collection-profile.test.js` pins the terminal known-gap recovery hint. The scheduler retry classifier now lets an explicit terminal `known_gaps[].recovery_hint={action:"retry_by_runtime", retryable:true}` override a conservative `connector_error.retryable:false`, and scheduled managed-controller runs consume that signal inside the existing retry budget before recording a terminal failure. Covered by `scheduler-managed-surface-routing.test.js`.)
- [x] 2.9 Add owner-started recovery continuation: when a successful run makes durable detail-gap progress and eligible non-pressure detail gaps remain for the same connection, the controller starts a bounded `recoveryOnly` continuation run. Durable progress includes recovered gaps and terminalized/quarantined poison items, whether surfaced through the runtime `detail_gaps` result or the terminal `known_gaps` carrier, so a connector can remove blockers without forcing repeated owner clicks. Prove that continuation starts after recovered progress, starts after terminal/quarantine progress, starts after live-shaped terminal known-gap progress, does not start after no-progress, and does not start for owner-required remaining work. (`reference-implementation/runtime/controller.ts`, `reference-implementation/test/controller-run-now-state-namespace.test.js`; direct controller test + `pnpm --dir reference-implementation typecheck` green.)

## 3. Amazon Proof Connector

- [x] 3.1 Move Amazon order-item detail recovery onto the connector-neutral recovery admission path. (`recoverPendingOrderItemDetailGapsBeforeForwardRun` consumes runtime-served `detailGaps` / `requestDetailGapPage` before forward collection, paginates until pending work is empty, preserves run caps as envelope-local stops, and suppresses forward collection only for recovery-only or exhausted budget cases. Covered by Amazon integration tests for paged recovery, recovery-only drain, no-scope skip, zero-budget legacy stop, and detail-budget exhaustion.)
- [x] 3.2 Preserve Amazon's per-run detail-attempt cap as a run blast-radius limit, not as the cross-run recovery scheduler. (`classifyAmazonDetailFailure("deferred_budget") === "run_cap_deferred"` and existing cap behavior remains resumable)
- [x] 3.3 Classify Amazon detail failures into planned cap, transient DOM/no-progress, provider pressure, owner-required repair, or connector defect. (Amazon emits connector-neutral classes in `last_error.class`: `run_cap_deferred`, `transient_no_progress`, and `owner_repair_required`; provider pressure remains distinct through runtime reason classification; repeated deterministic no-progress now escalates through runtime quarantine to connector/system issue. Covered by Amazon integration tests plus `connector-detail-gap-store.test.js` runtime full-row classification for Amazon-shaped `retry_exhausted` + `run_cap_deferred` rows.)
- [x] 3.4 Add fixture-backed tests for repeated Amazon detail-page no-progress escalating to connector/system issue rather than owner retry busywork. (Amazon fixture tests prove parse-missing/no-progress detail recovery re-defers as `temporary_unavailable` + `transient_no_progress`, not owner retry or source pressure. Runtime tests prove repeated Amazon-shaped no-progress re-deferrals cross the per-item threshold and become durable `terminal`/`quarantined` connector/system issues, while planned run-cap re-deferrals remain queued. Covered by `packages/polyfill-connectors/connectors/amazon/integration.test.ts` and `reference-implementation/test/connector-detail-gap-store.test.js`.)

## 4. Connection Health And Actionability

- [x] 4.1 Add typed recovery state to the rendered verdict or the connection-health projection consumed by owner surfaces. (`apps/console/src/app/(console)/lib/source-recovery-state.ts`)
- [x] 4.2 Replace indefinite "Checking" projection paths with active-check, queued-recovery, cooldown, unknown-unmeasured, read-failure, or system-issue states. (PR #190 split inactive unknown from active Checking; this tranche routes recovery states to named copy across dashboard/Sources/Syncs/source-detail. Inactive durable backlogs route to named recovery even against an adversarial "Checking" pill (`source-actionability.test.ts` task-4.2 test); read-failure uses the scoped `records-read-failure-banner`, not per-source Checking; system-issue routes via `recoveryStateGroup`. The console never invents "Checking" for an inactive state.)
- [x] 4.3 Update shared source-actionability grouping so queued/cooling recovery is passive progress, eligible recovery is owner-runnable, and connector defects are system issues. (`source-actionability.ts` recovery routing)
- [x] 4.4 Add dashboard, Sources, Syncs, and source-detail tests proving group counts equal rendered rows and no inactive queued recovery row says "Checking". (Dashboard: `standing-view-model.test.ts` asserts every `sourceWorkSection.countLabel` equals its rendered `rows.length` and the inactive-recovery row is passive progress, never Checking. Syncs: `syncs-model.test.ts` asserts `band.needsReview === failureCards.length` and the inactive recovery card is passive, never needs-you/Checking. Source-detail: `connection-diagnostics.test.ts` + `source-recovery-state.test.ts`.)
- [x] 4.5 Add stalled-recovery projection coverage: queued/catching-up carries recency evidence, and eligible work with no attempt beyond the cadence window renders as a system issue rather than indefinite passive progress. (The source-detail `ConnectionDiagnostics` now arms the stall watchdog with the server render instant and `RECOVERY_STALL_CADENCE_MS` (6h) threaded from the `force-dynamic` page; a stale attempt floor beyond the window renders as a `stalled` system condition. Covered by `source-recovery-state.test.ts` (behavioral) and `connection-diagnostics.test.ts` (wiring).)
- [x] 4.6 Implement the source-row UI contract: one concrete primary sentence, one evidence line, and at most one primary action. (Recovery rows and the source-detail recovery panel follow the contract: one primary sentence, evidence rendered separately, and no bypass action field on the panel view-model. Pinned by the `source-recovery-state.test.ts` "one primary sentence, evidence separate, no bypass action" invariant across active/queued/cooling/stalled steps.)
- [x] 4.7 Add source-detail recovery panel view-model support for current step, progress counts, next eligible attempt, blocker, and recent non-secret evidence. (`buildRecoveryPanelViewModel` + `RecoveryPanel`)
- [x] 4.8 Add owner-surface tests proving active recovery names the work, such as "Syncing order details", instead of generic "Checking".
- [x] 4.9 Add owner-surface tests proving unsafe retries show wait or blocker copy and no normal retry CTA.

## 5. Validation And Live Proof

- [x] 5.1 Run `openspec validate add-connector-neutral-recovery-governor --strict`.
- [x] 5.2 Run targeted runtime detail-gap, scheduler/manual-provider-safety, rendered-verdict, and console source-actionability tests.
- [x] 5.3 Deploy from clean `main` after tests pass and no active run would be interrupted. (Initial tranche deployed clean `origin/main` `959bd0266` on 2026-07-06 after `controller_active_runs=0`; `COMPOSE_PROJECT_NAME=pdpp scripts/reference-stack.sh up --build-app` and `COMPOSE_PROJECT_NAME=pdpp scripts/reference-stack.sh verify` both completed `reference-stack: ok`; served revision `v0.18.12-96-g959bd0266`. Closeout tranche `c3ae0c4e4` was deployed the same day from a clean detached `origin/main` worktree after `controller_active_runs=0`; `up --build-app` and `verify` again completed `reference-stack: ok`; served revision `v0.18.12-103-gc3ae0c4e4`.)
- [x] 5.4 Run one supervised Amazon recovery batch and verify it recovers eligible work, respects any cooldown, and leaves the owner UI with typed recovery state rather than repeated retry instructions. (Live supervised owner run `run_1783369989300` for Amazon `cin_a8ec003e6d441205d646f178` on 2026-07-06 completed `succeeded`, emitted `run.detail_gap_recovered` events, recovered order-item detail gaps from 550 to 748 (+198), released the browser surface, and left remaining work typed as planned continuation (`run_cap_deferred` / `deferred_budget`) or transient no-progress (`temporary_unavailable` / `transient_no_progress`). Owner diagnostics after completion reported recovery admission `candidates=100`, `admitted=100`, `deferred=0`, and `stall.stalled=false`, proving the next batch is runnable rather than stuck behind owner retry busywork.)
- [x] 5.5 Verify the live ChatGPT residue (942 non-pressure gaps held by stale pressure rows) begins draining under the new eligibility rule without manual data cleanup, and keep that dataset as the regression proof. (Live post-deploy durable state for scheduled ChatGPT `cin_11deac1e728b244aaeb56765` shows every known detail gap recovered, including `retry_exhausted/run_cap_deferred=1425`, `retry_exhausted=686`, `upstream_pressure/upstream_pressure_deferred=220`, `upstream_pressure=197`, and rate-limited rows; there are no pending or in-progress ChatGPT detail gaps. Owner diagnostics for both ChatGPT connections reported recovery admission `candidates=0`, `admitted=0`, `deferred=0`, and `stall.stalled=false`, proving the stale-pressure residue no longer blocks non-pressure recovery.)
- [x] 5.6 Deploy the owner-started recovery continuation tranche and verify Amazon `cin_a8ec003e6d441205d646f178` drains or quarantines remaining eligible order-item gaps without repeated owner clicks. (Follow-up proof after PR #259: live run `run_1783464023399` recovered additional work and started continuation `run_1783464424703`; the continuation failed before connector execution because it leased a dead dynamic browser surface whose CDP endpoint was gone. PR #260 added one dynamic-surface readiness failover: invalidate/stop the stale surface, reacquire one fresh surface for the same run, then fail closed if readiness still fails. Deployed clean `origin/main` `a54611355` on 2026-07-07 after `controller_active_runs=0`; `COMPOSE_PROJECT_NAME=pdpp scripts/reference-stack.sh up --build-app` and `COMPOSE_PROJECT_NAME=pdpp scripts/reference-stack.sh verify` both completed `reference-stack: ok`; served revision `v0.19.3-25-ga54611355`. Live proof run `run_1783466388429` completed `succeeded`, recovered/terminalized order-item detail gaps, and automatically started continuation `run_1783466879005` without another owner click. That continuation hit the exact stale dynamic-surface readiness failure, released the stale surface, reacquired a fresh surface, reached `run.browser_surface_ready`, started, recovered detail gaps, completed `succeeded`, and started continuation `run_1783467240512`. The next continuation repeated the stale-surface failover path and emitted `run.detail_gap_recovered`, proving the loop continues useful work rather than dead-ending on a dead CDP endpoint.)
