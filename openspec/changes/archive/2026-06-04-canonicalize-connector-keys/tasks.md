## 1. Identity Inventory

- [x] 1.1 Inventory every active use of URL-shaped connector ids, stale aliases, `legacy`, `legacy_default`, `connector_instance_id` public aliases, and delimiter-parsed connector selection values. (Tranche 1: inventory captured in worker report `tmp/workstreams/ri-canonical-connector-keys-full-report.md` covering polyfill + reference manifests, runtime storage paths, MCP/AS surfaces, legacy alias sites, OpenAPI mentions, and SQL queries.)
- [x] 1.2 Classify each occurrence as active runtime contract, migration-only, test fixture, root protocol doc, reference doc, or generated artifact. (Tranche 1: classification recorded in the same worker report; manifest URLs + legacy alias map are active runtime, SQL queries are storage layer, OpenAPI mentions are generated contract artifacts, `legacy-local-connector-manifest-alias.test.js` is migration-only test fixture.)
- [x] 1.3 Produce a canonical key mapping table for first-party connectors and local aliases, including unmapped/custom behavior. (Tranche 1: codified in `reference-implementation/server/connector-key.js` as `FIRST_PARTY_CONNECTOR_KEYS` + `NATIVE_CONNECTOR_KEYS` + `LEGACY_LOCAL_ALIASES`, pinned by `test/connector-key.test.js`; unknown URLs/strings fail closed to `null` per design §3.)

## 2. OpenSpec And Contract Alignment

- [x] 2.1 Validate this OpenSpec change with `openspec validate canonicalize-connector-keys --strict`. (Tranche 1: validated clean after adding canonical connector-key helpers and hosted MCP picker meta cleanup.)
- [x] 2.2 Cross-check `expose-connection-identity-on-public-read`, `agent-consent-bundling`, and MCP adapter specs for contradictory `connector_id`/`connector_key` wording. (2026-05-29 audit recorded in design Decision 9: public-read owns `connection_id` plus deprecated `connector_instance_id`, this change owns connector type `connector_key`, and the agent-consent/MCP deltas supersede older neutral/connector-id wording without contradiction.)
- [x] 2.3 Decide whether any root `spec-core.md` or `spec-collection-profile.md` language must change in this tranche or whether it requires a separate protocol-spec change. (Decision recorded in design Decision 10: root protocol/Collection Profile URI-shaped `connector_id` language remains unchanged in this reference-implementation tranche; any root rename or protocol split requires a separate protocol-spec change.)

## 3. Migration

- [x] 3.1 Add canonical connector-key helpers and first-party URL/alias mapping tests. (Tranche 1: `reference-implementation/server/connector-key.js` exports `canonicalConnectorKey`, `canonicalConnectorKeyFromManifest`, `connectorKeyFromRegistryUrl`, `isRegistryUrlConnectorId`, `isLegacyLocalAlias`; covered by `test/connector-key.test.js` (15 cases) including allowlist parity guards against drift in `packages/polyfill-connectors/manifests/` and `reference-implementation/manifests/`.)
- [x] 3.2 Add dry-run migration that reports every table/field to be rewritten and fails on ambiguous/unmapped identifiers. (Tranche 3.2: `reference-implementation/scripts/canonical-connector-keys/` (`cli.mjs`, `inspect.mjs`, `inspect.test.mjs`) discovers every `connector_id` column via `information_schema`, classifies each distinct value against the canonical allowlist, unwraps `local-device:<inner>[:source_instance_id]` storage forms, counts `source_binding_json.kind` placeholders and legacy display-name rows, and walks the known JSONB surfaces with embedded `connector_id` payloads (`grants.grant_json` `$.source.id`, `grants.storage_binding_json` `$.connector_id`, `grant_package_members.source_json` `$.id`, `pending_consents.params_json` `$.source_binding.id` + `$.storage_binding.connector_id`) so embedded identifiers are classified through the same `classifyConnectorId` helper and fail closed on unmapped active-table values. `grant_packages.package_json` and `connector_instances.source_binding_json` carry no embedded `connector_id` and are reported as informational row counts. All dynamic table/column identifiers are wrapped in `quotePgIdentifier` to escape embedded double-quotes. Exits non-zero by default when any active-table unmapped value is found; `--allow-unmapped` bypasses for review. Read-only — no writes. 26 fixture-backed tests cover column-level mapped/ambiguous/unmapped, JSONB mapped/unmapped extraction, missing JSONB columns, and the identifier-quote helper escape behavior. Tranche 3.2b extends the dry-run with a three-tier surface taxonomy (`active | backup | scratch`) via `classifyTableSurface(tableName)`, adds `surfaceClass` to each table report entry, and adds `totalUnmappedRowsActive`, `totalUnmappedRowsBackup`, `totalUnmappedRowsScratch`, and `hasUnmappedActive` summary fields so the dry-run separates active migration readiness from backup/scratch evidence. CLI exit code now gates on `hasUnmappedActive` only; backup/scratch unmapped rows warn but do not fail. `--include-backup-tables` flag is plumbed and parsed for the future §3.3 write migration but does not change dry-run scanning. Human report groups tables by tier. 38 fixture-backed tests.)
- [x] 3.3 Add write migration that rewrites connector manifests, storage bindings, source bindings, grants, grant packages, records/history, blobs, indexes, schedules, state, runs, diagnostics, coverage/gaps, and event subscriptions. (Tranche 3.3: `reference-implementation/scripts/canonical-connector-keys/writer.mjs` adds `rewriteStoredValue()` (URL/legacy/wrapped-local-device → canonical), `JSONB_REWRITE_SHAPES` (paired one-for-one with `inspect.mjs::JSONB_CONNECTOR_ID_SHAPES`: `grants.grant_json` `$.source.id`, `grants.storage_binding_json` `$.connector_id`, `grant_package_members.source_json` `$.id`, `pending_consents.params_json` `$.source_binding.id` + `$.storage_binding.connector_id`), `planFromReport()`, `applyPlan()`, and `migrate()`. Discovery still flows through `inspect()` so every `connector_id`-bearing table (connector manifests, storage bindings, records/history, blobs, indexes, schedules, runs/state, diagnostics, coverage/gaps, event subscriptions) is rewritten through the same `classifyConnectorId` mapping. Direct columns rewrite one distinct value at a time via `UPDATE table SET connector_id = $1 WHERE connector_id = $2`; JSONB surfaces re-fetch each row by primary key (`grant_id`, `(package_id, grant_id)`, `device_code`) and re-encode the rewritten payload. Every write happens inside a single transaction opened on a dedicated `pg.Client` (`BEGIN`/`COMMIT`/`ROLLBACK`); any per-statement row-count mismatch or thrown error rolls back to the pre-migration snapshot. Migration is dry-run-first (`migrate(driver)` returns the plan without writing unless `apply: true`), fail-closed (`hasUnmappedActive` aborts before any write; `--allow-unmapped` is diagnostic-only), and backup-aware (backup-tier tables are scanned and reported but NOT rewritten without explicit `--include-backup-tables`; scratch-tier tables are never rewritten because they are ephemeral test scaffolding). Local-device wrappers collapse to the bare canonical connector key and drop any trailing `:<encoded source_instance_id>` segment because connection isolation belongs on `connector_instance_id`, not `connector_id`. Row counts on every touched table are snapshotted before/after and asserted equal inside the transaction. CLI: `cli.mjs write [--apply] [--include-backup-tables] [--allow-unmapped] [--json]`. 25 fixture-backed `writer.test.mjs` tests cover URL rewrites, JSONB rewrites with unrelated fields preserved, wrapped-local-device collapse (with and without trailing source_instance_id), unmapped active aborts before writes, backup/scratch are skipped by default, backup-tier unmapped rows DO block writes with `--include-backup-tables`, mid-flight column/JSONB failures roll back atomically, row counts preserved, idempotent on re-run.)
- [ ] 3.4 Validate migration against a backup fixture with URL-shaped ids and aliases, proving row counts, grants, data, and record hydration are preserved. (Tranche 3.4 fixture/backup-restore validation landed: `reference-implementation/scripts/canonical-connector-keys/run-backup-restore-validation.sh` + `fixtures/backup-restore-seed.sql` + `verify-backup-restore.mjs` perform a true restore-then-migrate cycle against a DISPOSABLE database that carries the **real reference schema** (`pg_dump --schema-only` of the live reference DB, including the real `backup_*`/`cleanup_*`/`compact_*_backup_*`/`fix_*`/`mig_*` tiers). The seed spans every identity shape — URL-shaped first-party (`https://registry.pdpp.org/connectors/gmail`→`gmail`), legacy alias (`claude_code`→`claude-code`), wrapped local-device (`local-device:codex:cin_…`→`codex`), already-canonical (`spotify`, must not rewrite), JSONB-embedded ids across `grants.grant_json`/`storage_binding_json`, `grant_package_members.source_json`, `pending_consents.params_json`, and a `backup_*`-tier negative-control row. The harness captures per-table row counts, `pg_dump -Fc` → `pg_restore` into a fresh DB (the backup+restore step), runs `cli.mjs inspect` then `write --apply` on the restored DB, and asserts 38 invariants: row counts preserved on all 14 touched tables; every active-tier `connector_id` column + JSONB id is the bare canonical key with zero URL/legacy/wrapped stragglers; all 3 grants + 2 package members preserved with canonical ids inside the JSONB and unrelated `connection_id` untouched; records hydrate under the canonical key and join their instances with payload/version-counter/blob-binding intact; the backup-tier table is left untouched by default and IS rewritten under `--include-backup-tables`; and a second `write --apply` rewrites nothing (idempotent). Last run: **38/38 + idempotency pass**, `openspec validate --strict` clean, writer 28 / inspect 41 unit suites green. **Tranche 3.4b (no-human HTTP read-surface strengthening) landed:** the harness now ALSO boots the reference app in-process against the migrated restore and asserts the LIVE HTTP read path, not just SQL. New: `scripts/canonical-connector-keys/verify-http-surfaces.mjs` (step 9b) + `seed-real-manifests.mjs` (step 3b patches the real first-party manifests into the seed before the dump so the read path resolves streams) + an `oauth_clients` device-flow row in the seed so an owner token can be minted. It boots `startServer({ storageBackend:'postgres', databaseUrl:<restored>, ownerAuthPassword:'' })`, mints an owner token for the seed owner (`owner_sub_1`), and asserts **15 invariants over HTTP**: owner record hydration under the canonical key for gmail/codex/spotify; **the same owner read with the stale URL-shaped `connector_id` resolves canonically (Decision 8) over the wire**; single-record reads; `/v1/search` list envelope; owner dashboard connection hydration via `GET /v1/owner/connections` under canonical keys (claude-code/codex/gmail/spotify) with no registry URL; grant-package membership + child cascade via `GET /_ref/grant-packages[/:id]` with no registry URL in owner-visible payloads; and an unregistered connector type returns a typed 4xx, not a 5xx. The verifier closes the process-global Postgres pool on teardown so the disposable-DB drop cannot crash it after checks pass. Deterministic unit coverage: `verify-http-surfaces.test.mjs` (8/8, no DB) pins the pure helpers + seed-coverage contract. Last full harness run: **38/38 SQL + 15/15 HTTP + idempotency + backup-tier opt-in, exit 0, disposable DBs cleaned up**. **Residual for owner sign-off (now smaller — HTTP surfaces are no-human-proven, only the data source remains live-gated):** the harness uses the real schema but author-controlled *synthetic* data. The one remaining owner step is to run this same restore→migrate→verify(SQL)→verify(HTTP) cycle against a restore of the operator's **own production backup** — i.e. `pg_restore` the operator dump into the disposable `RESTORED_DB` (or point `LIVE_DB` at the operator DB), then `node scripts/canonical-connector-keys/cli.mjs write --apply` + `verify-http-surfaces.mjs --owner-subject <operator_subject>` against it; the dashboard/`/v1/streams`/grant-package/record HTTP spot-checks that previously needed manual browser clicking are now asserted by code. Box left unchecked pending that owner-run against real production data. **Tranche 3.4c (owner-run packet + data-agnostic verifier) landed:** the prior residual guidance to run `verify-http-surfaces.mjs --owner-subject <operator_subject>` against production was wrong — that verifier (and `verify-backup-restore.mjs`) hard-code the synthetic seed's grant ids, record counts, owner subject, and streams, so they FAIL on real data. Added `scripts/canonical-connector-keys/verify-production-invariants.mjs`: a DATA-AGNOSTIC post-migration verifier that asserts only invariants true for any dataset — zero non-canonical (`https://…`/legacy-alias/`local-device:`-wrapped/malformed) stragglers in every active `connector_id` column AND in the four active JSONB surfaces (reusing `JSONB_CONNECTOR_ID_SHAPES` + `classifyTableSurface` so it stays in lockstep with the migration), all `connectors` rows canonical, and row-count parity vs a `--before` snapshot (with `connectors` allowed to SHRINK via intentional parent-collapse, never grow). Its straggler predicate correctly does NOT flag valid custom connector keys outside the first-party allowlist (where `canonicalConnectorKey` returns null), avoiding a false-positive on custom-connector deployments; 7 deterministic unit tests in `verify-production-invariants.test.mjs` pin this incl. the custom-key guard. The synthetic harness now runs this verifier as step 9c against the migrated restore (**17/17 data-agnostic invariants** alongside 38/38 SQL + 15/15 HTTP). The full owner runbook is `docs/operator/canonical-connector-keys-production-restore-packet.md` — exact env vars, inputs, restore-into-disposable-DB commands, before-snapshot capture, inspect→write→verify→idempotency sequence, HTTP spot-check, no-secrets evidence list, unmapped-value troubleshooting (never `--allow-unmapped --apply`), cleanup, and the precise Codex-acceptance bar. `docs/operator/live-proof-packet.md` Gate 7 now points at that packet. Box STILL unchecked: no production-backup restore cycle has run in this lane; the box flips only when the operator completes the packet against real production data.)

## 4. Runtime And Surface Cleanup

- [x] 4.1 Update first-party manifests to use `connector_key` plus `manifest_uri`. (Tranche 4.1: every registry-backed first-party manifest under `packages/polyfill-connectors/manifests/` and `reference-implementation/manifests/` now declares `connector_key` plus matching `manifest_uri`. Top-level `connector_id` is retained as a compatibility alias until later runtime/docs slices remove active reads of that field. Guarded by `reference-implementation/test/connector-key.test.js` manifest parity coverage.)
- [x] 4.2 Update manifest registration and lookup to key by `connector_key`. (Tranche 4.2: `canonicalConnectorKeyFromManifest` prefers explicit `connector_key`; `registerConnector` accepts `connector_key`-only manifests, stores connector rows under the canonical key, persists `connector_key`, preserves registry provenance as `manifest_uri`, and keeps `connector_id` as a stored compatibility alias carrying the canonical key. `getConnectorManifest` continues to resolve canonical keys directly and legacy URL aliases through canonical lookup. Covered by `reference-implementation/test/hosted-mcp-picker-canonical-collapse.test.js`, `reference-implementation/test/as-operations.test.js`, and connector discovery checks.)
- [x] 4.3 Update runtime/storage/read/search/blob/event-subscription code to require canonical connector keys in active paths. (Local-device storage slice landed: design Decision 7 fixes the storage-key/admission mismatch where local-device records were written under the bare canonical key but read/migrated under a `local-device:` prefix, and where a legacy-alias enroll (`claude_code`) produced a catalog-vs-instance FK failure. The device-exporter enroll path now canonicalizes `connector_id` once at the boundary so the `connectors` row, `connector_instances` row, `device_source_instances` row, and record storage target all agree on the canonical key; ingest/heartbeat/gap routes compare connector ids canonically (`sameConnectorType`); the dashboard read path (`recordStorageConnectorIdForConnection`) drops the `local-device:` prefix; and the SQLite + Postgres startup migrations relocate legacy `local-device:<id>:<source>` rows to the bare canonical key and canonicalize `device_source_instances.connector_id`. Covered by `test/device-exporter-routes.test.js`, `test/device-exporter-state-routes.test.js`, and `test/ref-connectors-connection-projection.test.js`. Read/admission slice landed: design Decision 8 makes the read/admission path symmetric with the already-canonical write path. A stale grant or owner read scope carrying a URL-shaped `connector_id` previously failed `connection_not_found` because admission enumerated active connections under the literal URL while instances/records were keyed canonically. `resolveReadRequestBindings` (shared by owner reads, grant-scoped client reads, and search fan-in) now canonicalizes `storageBinding.connector_id` before `listActiveByConnector`; `resolveOwnerReadScope` canonicalizes the owner read storage binding and source descriptor; the `GET /v1/blobs/:blob_id` route canonicalizes the actor connector id before matching blob bindings; and the `/_ref/connections` + `/_ref/connector-instances` list routes canonicalize the `connector_id` query filter. Covered by `test/assistant-readiness-smoke.test.js` (10/10), `test/query-contract.test.js` (42/42), `test/connector-instance-admission-routes.test.js` (7/7), and two new `resolveReadRequestBindings` URL-alias regression guards in `test/storage-fan-in-read-contract.test.js`. The full suite no longer reports any `connection_not_found` failures. Scheduler readiness slice landed: `checkFirstPartyLocalSourceReadiness` now canonicalizes the connector id before Codex/Claude Code filesystem checks, preventing post-migration canonical keys from falsely bypassing local-source readiness. Covered by `test/scheduler.test.js` canonical local-source regression. Grant-scoped state slice landed (finding F3): `resolveGrantScopedStateGrant` now canonicalizes BOTH the request path `:connectorId` and the grant storage-binding connector id before the grant-scope comparison, so `GET|PUT /v1/state/:connectorId` tolerates a URL-shaped path or stale URL-shaped binding; covered by a `test/pdpp.test.js` URL-path regression test. Non-admission emit/filter slice landed (the prior "event-spine, control-plane" remainder), each execution-confirmed by a failing-then-passing regression: (GAP A) the autonomous scheduler `buildConnectors` canonicalizes `schedule.connector_id` before manifest/path lookup and the emitted connector entry so non-controller-written schedule rows run and emit spine `source.id`/`actor_id` + run-history + last-run under the canonical key (`test/scheduler.test.js` regression); (GAP B) `operations/ref-source-webhook-ingest` canonicalizes the configured/resolved connector id before ingest/scheduler/run callbacks, retaining the raw `sourceId` for dedup (`test/ref-source-webhook-ingest-operation.test.js` regression); (GAP C) `server/routes/ref-spine-correlations.ts::parseListFilters` canonicalizes the owner `connector_id` filter for `/_ref/{traces,grants,runs}` via `ctx.canonicalConnectorKey`, matching the `/_ref/connections` fix, so URL-shaped filters match canonically-keyed spine rows (`test/ref-spine-correlations-connector-filter.test.js`, 6 tests). The broad URL-vs-canonical output-assertion drift in `cli`/`pdpp`/`control-actions`/`scheduler`/`scheduler-doctor`/`event-spine`/`collection-profile`/search/records/conformance tests is fully resolved (see task 6.2; 36 connector-key-affected test files / 873 tests certified green). Owner-initiated control-plane/search/blob paths were confirmed already-canonical at their `resolveOwnerConnectorNamespace`/`resolveOwnerReadScope` boundaries. Active-runtime canonicalization for 4.3 is complete; client event-subscription delivery keys by `subscription_id` and carries no connector-id comparison.)
- [x] 4.4 Update hosted MCP package and consent selection parsing to use opaque or structured selection values, not delimiter-split connector ids. (Tranche 1: picker now emits `base64url(JSON.stringify({connector_id, connection_id}))` selection values; AS POST handler decodes via `parseHostedMcpSelections` from `server/hosted-mcp-selection.js`. Encoder/parser unit-tested and HTTP-layer regression-tested. Payload field name will flip from `connector_id` to `connector_key` once the broader canonical-key migration lands.)
- [x] 4.5 Update owner dashboard, Explore, Records/Connections, event subscriptions, deployment tokens, and grant package surfaces to display connector names and connection names without registry URLs or legacy labels. (Tranche 2 partial: hosted MCP package picker meta sub-line renders the canonical `connector_key` instead of the registry URL via `canonicalConnectorKey(connectorId)`. Tranche 4.5 closeout: web and console dashboard display code routes connector type labels, connector-name fallbacks, source labels, Explore chips/cards/peek headers, Records/Connections rows and breadcrumbs, Schedules rows, run-stream browser-assistance context, device-exporter source cards, deployment diagnostics, Overview/Jump rows, grants, and grant-package child rows through `formatConnectorKeyForDisplay`, `formatConnectorNameForDisplay`, or `formatSourceForDisplay`. The helpers strip registry URLs, local-device wrappers, and legacy labels from display-only paths while leaving routing, filters, action payloads, hidden form values, and stored identifiers unchanged. Hosted MCP picker follow-up `1344614c` sanitizes URL-shaped/default connection display names out of owner-visible copy while preserving real human connection names; regression coverage is `hosted-mcp-oauth.test.js` (`picker hides URL-shaped default connection labels from owner-visible copy`) plus authenticated local UAT on the rebuilt stack showing `visible_registry_url=false` across 19 source rows. Event-subscription and deployment-token pages carry no connector/source labels to rewrite in this slice; event-subscription invariants and both app type checks were run with the display cleanup.)
- [x] 4.6 Update local-collector setup/config paths to advertise and accept canonical connector keys only. (Verified 2026-05-29 by RI owner-delegate: the local-collector setup/config surfaces advertise and accept only canonical keys. CLI `pdpp collector <advertise|enroll|run>` and `@pdpp/local-collector` examples use bare canonical keys (`claude_code`, `codex`) with no URL-shaped ids; `docs/operator/local-collector-runbook.md` and the compat-shim doc `reference-implementation/docs/local-device-exporter.md` both teach `--connector claude_code|codex` only. The device-exporter advertise/enroll/ingest runtime canonicalizes `connector_id` at every boundary — `reference-implementation/server/routes/ref-device-exporters.ts` enroll (`canonicalConnectorKey(enrollment.connectorId)`), `sameConnectorType` comparison, and source-instance projection — so the catalog row, instance row, device-source-instance row, and storage target agree on one canonical key. Covered by `reference-implementation/test/device-exporter-routes.test.js` (enrolls with legacy alias `claude_code` and asserts catalog/instance/storage/enroll-response all canonicalize to `claude-code`) and `device-exporter-state-routes.test.js` (legacy `local-device:` rows relocate to bare canonical key). Residual `legacy` mentions in the shim doc describe the `source_instance_id` device-binding selector and the `PDPP_LOCAL_DEVICE_QUEUE` lane — accurate descriptions of compatibility surfaces, not non-canonical connector identity. No setup/config path advertises or requires a URL-shaped connector id or `legacy_default`.)
- [x] 4.7 Close post-gate direct record-storage canonicalization gap. `records.js` and `postgres-records.js` now canonicalize first-party URL/legacy connector ids before deriving storage keys and default connection ids, so direct helper calls and composed-origin fixture ingest cannot write records under URL-shaped ids while owner reads look under canonical keys. Regression coverage: `test/records-instance-namespace.test.js` proves URL-shaped ingest stores as `spotify` and is readable through both canonical and URL-alias storage targets; `test/composed-origin.test.js` proves composed-origin owner-token stream metadata and consent approval return canonical source identity.
- [x] 4.8 Close post-gate direct runtime and owner-mutation canonicalization gaps. `runtime/index.js` now canonicalizes the supplied connector id once at the `runConnector` and `loadSyncState` boundaries, so START env, ingest/state URLs, spine `actor_id`/`source.id`, traces, attention rows, and detail gaps use the canonical connector key even when the caller still passes a first-party manifest URL. `rs-mutation.ts` also canonicalizes owner ingest/delete and owner state path/query connector ids before building mutation/state contexts, so traces and diagnostics report the operational key. Regression coverage: `test/control-plane.test.js` keeps seeding with the URL-shaped Spotify manifest id, but expects run summaries, `connector_id` filters, and runtime timeline `actor_id` to resolve as `spotify`; `test/cli.test.js`, `test/pdpp.test.js`, and `test/event-spine.test.js` prove owner mutation/state/runtime traces use `spotify`; `test/assistant-readiness-smoke.test.js` proves representative spine sources canonicalize. Full `pnpm --dir reference-implementation run test` passed on 2026-05-31 after these fixes.

## 5. MCP And Client Surface

- [x] 5.1 Update MCP tool schemas/descriptions to prefer `connection_id` for source disambiguation and canonical connector keys for connector type metadata. (Tranche 5.1: `@pdpp/mcp-server` tool descriptions and `connection_id` input-schema guidance now point clients to `GET /v1/schema`, typed `available_connections` envelopes, canonical `connector_key` metadata, and concrete `connection_id` retry selectors. The deprecated `connector_instance_id` alias remains accepted for compatibility but is explicitly demoted in schema guidance. Regression coverage in `packages/mcp-server/test/canonical-mirror.test.js` asserts descriptions prefer `connection_id` + `connector_key` and do not advertise registry URLs as connector identity.)
- [ ] 5.2 Verify Claude and ChatGPT MCP flows can approve multiple connections, inspect streams, query records, and create event subscriptions without URL-shaped connector ids. (Claude side is live-proven as of 2026-05-31: external Claude reconnected to the hosted MCP integration, inspected stream/schema text after the MCP text-readability fix, queried readable YNAB records, exercised `ambiguous_connection -> connection_id` retry with canonical `connector_key`/`connection_id` entries, saw distinct `claude_code` multi-instance connections, and completed create/list/get/send-test/delete event-subscription lifecycle without URL-shaped connector ids in owner-facing tool guidance. ChatGPT MCP flow remains open; do not mark this complete until ChatGPT is also live-proven or the task is explicitly narrowed. Residual is narrow and specific: ChatGPT registration refresh (stale 6-tool surface -> delete/re-add -> current 14-tool surface) and a successful `messages` `query_records` retry with a `connection_id` were observed, but the **ChatGPT event-subscription create/list/get/send-test/delete lifecycle has not been driven**, so the "create event subscriptions" clause is unproven for ChatGPT — this is exactly why the prior reconcile attempt was owner-rejected for overclaim. The mechanical, acceptance-grade owner-run proof and Codex pass/fail bar are in `docs/operator/chatgpt-mcp-canonical-proof-packet.md` (indexed from `docs/operator/live-proof-packet.md` Gate 2). A stale 6-tool surface is host registration drift fixed by delete + re-add, not a backend defect.)
- [x] 5.3 Verify package-token event-subscription creation selects exactly one child grant by `connection_id` and returns typed ambiguity when required. (Tranche 5.3: `package-rs-client.js` `availableConnectionsList` now emits `grant_id` + `connector_key` instead of URL-shaped `connector_id`, matching the spec requirement that typed error envelopes advertise canonical connector-key language. `memberSourceTag` adds `connector_key` while retaining `connector_id` for existing fanout source-tag consumers. Regression tests added to `reference-implementation/test/package-rs-client.test.js` assert `grant_id`, `connector_key`, `connection_id` presence and `connector_id` absence in `ambiguous_connection` and `not_found` envelopes for event-subscription create and query-records paths, plus source-tag compatibility.)

## 6. Docs And Fixtures

- [x] 6.1 Update reference docs, CLI help, MCP README, and dashboard copy to use canonical connector keys and `manifest_uri` provenance. (Verified 2026-05-29 by RI owner-delegate, corroborated by two independent read-only audits: `packages/mcp-server/README.md`, `reference-implementation/cli/index.js` help text, `docs/operator/**` (selfhost-quickstart, hosted-mcp-setup, local-collector-runbook), and dashboard copy in `apps/web|console/src/app/dashboard` carry **no** URL-shaped connector id or `legacy`/`legacy_default` label as the active operational identity. Dashboard display routes through the `connector-display.ts` sanitizer (strips registry URLs/`local-device:` prefix, maps `legacy*`→"default connection"); the device-exporter docs teach `--connector claude_code|codex` only. The single `https://registry.pdpp.org/connectors/spotify` in `apps/web/content/docs/reference-implementation.md` is a root-protocol-style architectural example (out of scope, same class as `spec-core.md`). MCP tool descriptions advertise canonical `connector_key` + `connection_id` with 0 registry-URL hits.)
- [x] 6.2 Update fixtures/tests so URL-shaped connector ids appear only in migration tests or root protocol examples that are intentionally left unchanged. (Completed 2026-05-29 by RI owner-delegate across commits `63307b0b`, `a5842e91`, `c9d03ed2`, `51ad8326`. The canonicalize-connector-keys runtime now rejects URL-shaped connector manifests at validation (`server/auth.js` requires a slug `connector_key`) and returns canonical ids; stale test fixtures/assertions that used `https://test.pdpp.org/connectors/<slug>` or `https://registry.pdpp.org/connectors/<non-first-party-slug>`, or asserted URL-shaped ids, were migrated to canonical keys across 22 test files + 1 shared helper (`test/helpers/record-read-conformance.js`). Deliberate migration-tolerance / legacy-alias round-trip guards (e.g. `storage-fan-in` `FIRST_PARTY_URL_CONNECTOR_ID`, `hosted-mcp-selection` URL round-trips, `ref-connectors-routes` pre-migration sim, `legacy-local-connector-manifest-alias`) were intentionally left URL-shaped. Final certification run: 36 connector-key-affected test files, 873 tests, **all green, 0 fail, 0 skip**. Zero `(test|registry).pdpp.org/connectors` URL fixtures remain in `reference-implementation/test/` outside the intentional guards.)
- [x] 6.3 Grep the tree for URL-shaped connector ids, `legacy_default`, user-visible `legacy`, and delimiter form patterns; read every affected file before marking this complete. (Completed 2026-05-29 by RI owner-delegate via a full-tree grep over `reference-implementation packages apps docs openspec` with every affected file read and classified. Verdict: **ZERO active-runtime-defects.** The active operational key is `connector_key` end-to-end; all residual URL/`legacy_default`/`legacy`/`local-device:` hits classify as intentional: (a) `manifest_uri` provenance + retained `connector_id` URL on first-party manifests (runtime keys off `connector_key` via `canonicalConnectorKeyFromManifest`); (b) migration-only — `scripts/canonical-connector-keys|migrate-*`, the `db.js`/`postgres-storage.js` relocate blocks (`localDeviceConnectorId`/`legacyLocalDeviceConnectorId` called only inside `migrate*LocalDeviceConnectorInstances`), and the `LEGACY_LOCAL_ALIASES` read-tolerance map; (c) test fixtures/guards incl. the deliberate `legacy-local-connector-manifest-alias` migration-tolerance test; (d) root-protocol docs (`spec-core.md`/`spec-collection-profile.md`, retained per Decision 6); (e) honest-compat docs describing `source_instance_id`/`PDPP_LOCAL_DEVICE_QUEUE` as legacy fields; (f) the `apps/*/dashboard/lib/connector-display.ts` display sanitizer (maps `legacy*`→"default connection", strips URL/`local-device:`). Delimiter form: the unsafe `connector_id:connection_id` `:`-split selection is GONE from active code — hosted-MCP selection uses opaque base64url(JSON) (task 4.4); only docstrings/test-names reference the removed form. Every `server/` URL hit is in a comment adjacent to a `canonicalConnectorKey(...)` boundary normalization. No fixes required.)

## 7. Deployment Validation

- [x] 7.1 Run targeted unit/contract tests for migration, manifest registration, grant package consent, MCP adapter, event subscriptions, owner-token routes, records/search hydration, and local collector config. (Run 2026-05-29 on the workstream branch: migration helpers `connector-key` 20/20; manifest registration `as-operations` 41/41, `hosted-mcp-oauth` 28/28, `hosted-mcp-selection` 17/17, `connector-instances-acceptance` 6/6, `polyfill-manifest-reconcile-invalidation` 11/11; grant-package consent + MCP adapter `package-rs-client` 22/22, `@pdpp/mcp-server` 75/75; event subscriptions — full 9-file suite 57/0 + `mcp-event-subscription-e2e` 2/2; owner-token routes `dcr-per-owner-token` 1/1, token-page invariants 3/3; records/search hydration `query-contract` 42/42, `storage-fan-in-read-contract` 29/29, `search-fan-in-host-shell` 8/8, `lexical-retrieval` 25/25, `hybrid-retrieval` 14/14, `semantic-retrieval` 40/40, `records-nullable-*` 10/10, `record-read-conformance`(+memory/falsifiability) 19/19, `aggregate-time-buckets` 13/13, `schema-granted-connections` 12/12; local collector `device-exporter-routes` 9/9, `device-exporter-state-routes` 11/11, `assistant-readiness-smoke` 10/10; plus `pdpp` 115/115, `cli` 139/139, `scheduler` 38/38, `control-actions` 23/23, `scheduler-doctor` 13/13, `event-spine` 46/46, `collection-profile` 100/100, spine-correlation + source-webhook regressions. Certified: 36 connector-key-affected files, 873 tests, all green.)
- [x] 7.2 Run `openspec validate canonicalize-connector-keys --strict` and relevant broader validation. (Run repeatedly on the workstream branch: `openspec validate canonicalize-connector-keys --strict` → valid after each task-mark/edit; `pnpm --dir reference-implementation run check` (ultracite) → 67 files, no fixes; `pnpm --dir apps/web run types:check` and `pnpm --dir apps/console run types:check` → ✓; `tsc --noEmit` clean on touched `.ts` files; `git diff --check` clean on every commit.)
- [x] 7.3 Deploy to the local Docker reference instance and verify `/dashboard`, `/dashboard/event-subscriptions`, `/dashboard/deployment/tokens`, `/dashboard/explore`, `/oauth/authorize`, and `/mcp`. (Verified 2026-05-31 after deploying `1344614c` with `scripts/reference-stack.sh up --build-app` and built-in `verify` passing. Authenticated local operator-console smoke through the web container returned `/dashboard=200`, `/dashboard/event-subscriptions=200`, `/dashboard/deployment/tokens=200`, `/dashboard/explore=200`; `/oauth/authorize` route returned the expected `400` for a missing client and the separate authenticated hosted-picker UAT returned `200` with 19 source rows; `/mcp` through the web proxy returned expected `401` for missing bearer. This was a route/surface smoke, not a visual QA pass.)
- [x] 7.4 Verify the owner's acceptance target: Claude can reconnect through MCP, set up event subscriptions, and Daisy can receive/use an owner token for management-feature testing. (Verified 2026-05-31. External Claude reconnected to hosted MCP, listed tools, inspected readable streams/schema/records, exercised multi-connection `ambiguous_connection` retry by `connection_id`, and completed event-subscription create/list/get/send-test/delete with public HTTPS callback delivery. Daisy/local owner-agent path was live-proven: device flow issued and stored an owner credential, owner bearer introspection/REST use succeeded for `/v1/streams` (list envelope under `data`, 146 entries in the owner smoke), and `/mcp` rejected the owner bearer as designed. The accepted owner-agent contract is owner-level REST/control-plane access; `/_ref` remains owner-session-scoped rather than owner-bearer-scoped.)
