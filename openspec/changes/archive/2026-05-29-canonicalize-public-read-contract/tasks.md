## 1. Contract and OpenSpec

- [x] 1.1 Capture prior-art and repo audit findings in `tmp/workstreams/canonical-read-contract-right-hand-report.md`.
- [x] 1.2 Add proposal, design, and spec delta for the canonical public read contract.
- [x] 1.3 Validate `canonicalize-public-read-contract` with `openspec validate canonicalize-public-read-contract --strict`.
- [x] 1.4 Classify overlapping active changes in their design/task files: `expose-connection-identity-on-public-read`, `expand-first-party-parent-child-relations`, `expose-per-stream-dataset-summary`, `clarify-public-read-contract-llm-hints`, `polish-assistant-query-api-discovery`, and `add-dashboard-records-explorer`. Classification posture lives in each change's `design.md` under "Classification under `canonicalize-public-read-contract`".

## 2. Reference Contract Schemas

- [x] 2.1 Add canonical envelope schema types for list, single-object, schema, search, warnings, links, and count metadata.
- [x] 2.2 Add public read input schemas for `fields`, `expand`, `expand_limit`, `filter`, `sort`, `cursor`, `limit`, `count`, `connection_id`, and deprecated `connector_instance_id` alias.
- [x] 2.3 Add generated contract descriptions that point callers to `/v1/schema` for field/operator/expand/sort/count capabilities.
- [x] 2.4 Ensure `connector_instance_id` is documented only as a deprecated compatibility alias.
- [x] 2.5 Run reference-contract generation and verification.

## 3. Public RS Runtime

- [x] 3.1 Thread `connection_id` and `display_name` onto every record-bearing records/search/blob response item. Search hits now carry `display_name` via a per-binding lookup deduped across the snapshot: `server/search.js#buildSnapshot` and `server/search-semantic.js#buildSnapshot` resolve owner-facing labels through the shared `connection-identity.js#resolveDisplayNamesForBindings` helper (one store roundtrip per unique `connection_id` in the snapshot, not one per hit). Hybrid forwards the label from whichever sub-source first emitted it. Placeholder labels (`legacy`, `default_account`, connector-id defaults) are omitted, not faked. Records-list, records-detail, changes_since rows, aggregate-expanded child records, and Postgres-backed record reads continue to carry `connection_id`/`display_name` when the runtime knows the pinned storage binding. Raw blob byte responses are not JSON envelopes; blob identity comes from the authorized record that references the blob. Coverage: `canonical-read-envelope-conformance.test.js` (search display_name emit + omit) plus existing `public-read-connection-id-decoration.test.js` (records).
- [x] 3.2 Make search hits carry `(connection_id, stream, record_id)` without dashboard-side inference. Lexical / semantic / hybrid result items now emit `connection_id` and the deprecated `connector_instance_id` alias when the snapshot recorded the binding. Pre-identity snapshots are tolerated (fields omitted, not faked).
- [x] 3.3 Implement optional `connection_id` filtering and deprecated `connector_instance_id` alias conflict validation on public read routes. Canonical helper `validateConnectionAlias` is shared by the records and search paths. Search rejects conflicting aliases; records list/detail/aggregate on SQLite and records list/detail on Postgres now reject any `connection_id` or deprecated alias outside the grant's currently bound storage with typed `connection_not_found`, so the request never broadens or silently returns an unrelated zero-result page. Full multi-binding fan-in filtering remains a future storage-topology change, not a gap in the current one-binding contract.
- [x] 3.4 Normalize public read responses into the canonical envelope, preserving backward-compatible fields only where the contract allows. (Vocabulary-uniform; wrapped-single-object reshape deferred to a follow-up.) `finalizeCanonicalEnvelope` at the route layer adds canonical `links.self`, `links.next` (list-shaped only), and defaults `meta.count = { kind: 'none' }` + `meta.warnings = []` for all public read responses while preserving backward-compat fields like `next_cursor`, `next_changes_since`, and `url`. List envelopes (records list, search, stream list) are canonical. Single-record envelopes carry the canonical `object`/`data`/`links`/`meta` vocabulary today — `{ object: 'record', id, stream, data, [identity], links, meta }` — which satisfies design.md's current vocabulary requirement. A wrapped JSON:API-style single-object envelope (`{ object: 'single', data: { object: 'record', ... } }`) would be a wire-breaking change and is explicitly out of scope for this change; it belongs in a future consumer-migration change if still desired.
- [x] 3.5 Implement strict validation for unsupported parameters, fields, filter operators, sort fields, and expansion targets. Records/search reject unsupported top-level params, unsupported expand relations, conflicting aliases, and unsupported filter operators with typed errors. Canonical `sort` is honored on both SQLite and Postgres backends with sign-prefix direction control and `sort`/`order` disagreement detection. Search ops also reject `count=` and other unsupported parameters at the v1 allowlist. Per-capability `/v1/schema` truth conformance landed in 4.2 (`schema-capability-truth.test.js`).
- [x] 3.6 Implement structured `meta.warnings` for skipped-not-applicable sources, deprecated alias use, count downgrades, and partial/lossy outcomes. Records-list, aggregate, lexical, semantic, and hybrid search emit `deprecated_alias_used` in `meta.warnings[]`; hybrid deduplicates warnings. Lexical and semantic search now also emit `source_skipped_not_applicable` warnings (carrying the connector_id as `detail.source`) when the owner fan-out drops a connector due to a broken manifest or empty searchable plan — previously this was silent. Coverage: `search-source-skipped-warning.test.js`. `count_downgraded` remains wired into the canonical vocabulary and the response plumbing but is intentionally not emitted today: the reference runtime never actually downgrades a count (computing the exact value is cheap, so `count=estimated` returns kind=`exact`), and emitting a downgrade warning on an upgrade outcome would be a wire-shape lie. `partial_results` and `compatibility_fallback` codes remain reserved in the canonical vocabulary for future tranches where the runtime can honestly emit them.
- [x] 3.7 Implement graded count support (`none`, `estimated`, `exact`) backed by existing projections when possible. `count=none` and `count=exact` are fully implemented on both the SQLite reference path and the Postgres runtime path via a visible-row scan over the same filter set the list path uses. `count=estimated` is honored by computing the exact value cheaply on the same scan; the response carries `meta.count.kind = 'exact'` and no warning, since returning a higher-fidelity grade than requested is an upgrade rather than a downgrade per the spec's "Requested count is downgraded" rule. Both backends reject unknown `count` values with `invalid_request`. **Search-path counts are advertised as unsupported** (`count_supported: false` on lexical/semantic/hybrid capability) and the strict allowlist rejects `count=` on `/v1/search*`. This is the honest posture — search runs a ranked snapshot whose total is not knowable cheaply without paging the full snapshot. Promoting search counts to a real grade is a separate runtime change (would require snapshot total-count plumbing) and would belong in a future tranche; it is NOT a blocker on this contract.
- [x] 3.8 Keep one-hop inline expansion grant-safe and bounded by `expand_limit`.

## 4. Capability Document

- [x] 4.1 Update `GET /v1/schema` to be the canonical capability document for streams, fields, operators, sortability, expansion, projection, search modes, pagination, counts, and granted connection identities. All currently supported capabilities are surfaced today (`field_capabilities.<field>.exact_filter`, `range_filter.operators`, `lexical_search`, `semantic_search`, `aggregation`, `expand_capabilities`, `cursor_field`, freshness). The `granted_connections` sub-item now lands per stream via `listGrantedConnectionsForStream` (`server/connection-identity.js`) wired into `buildConnectorSchemaItem` (`server/index.js`); the contract schema `StreamMetadataResponseSchema.granted_connections` (`packages/reference-contract/src/public/index.ts`) advertises the wire shape (`[{ connection_id, display_name? }, ...]`, `display_name` omitted for placeholder labels per the canonical projection rules). Owner scope enumerates every active connection under the addressed connector; client scope honors `grant.streams[].connection_id` (single binding) or fans in across every active binding when absent. Coverage: `schema-granted-connections.test.js` (12 tests; 9 helper-level + 3 HTTP end-to-end).
- [x] 4.2 Add conformance checks proving every advertised field/operator/sort/expand capability is either enforced or rejected clearly. Coverage: `schema-capability-truth.test.js` exercises the per-field range-operator vocabulary, exact-filter scalar-acceptance, undeclared-field rejection, field-not-granted rejection (grant trumps schema), and the closed range-operator vocabulary against accidentally-advertised unsupported operators. The harness uses the same `manifestStream` shape that feeds both `buildFieldCapabilities` (schema advertisement) and `compileRequestFilters` (runtime validator), so capability drift between the two would fail the harness.
- [x] 4.3 Add search-mode pagination and count-support metadata. `buildLexicalRetrievalCapability` / `buildSemanticRetrievalCapability` / `buildHybridRetrievalCapability` (`server/metadata.ts`) now emit explicit `cursor_supported` and `count_supported` fields. Lexical and semantic advertise `cursor_supported: true, count_supported: false`. Hybrid advertises `count_supported: false` and forwards `cursor_supported`. Runtime allowlists reject `count=` cleanly with `invalid_request`. Coverage: `search-count-capability.test.js`.

## 5. MCP Mirror

- [x] 5.1 Update `packages/mcp-server` input schemas to mirror the canonical public read arguments.
- [x] 5.2 Add `outputSchema` and canonical `structuredContent` envelopes for read tools.
- [x] 5.3 Ensure prose `content[]` is a concise summary only and not a second divergent JSON contract.
- [x] 5.4 Add tests proving MCP does not silently drop unsupported arguments that REST would reject.

## 6. Dashboard, Explorer, and CLI Consumers

- [x] 6.1 Simplify Explorer search attribution once public search hits carry `connection_id`; remove sentinel or deduction code no longer needed. Stale comments now reflect that search hits carry `connection_id` when snapshots have it; fallback deduction remains only for pre-identity snapshots so existing deployments remain readable during migration. Removing that fallback requires a snapshot purge or migration-window change outside this contract closeout.
- [x] 6.2 Update dashboard/console reads to consume canonical envelopes and warnings where available. `read-envelope.ts` provides the tolerant canonical/legacy adapter; records list/detail/search pages now surface canonical `meta.warnings` out-of-band. A full dashboard-wide consumer sweep is intentionally left for a focused follow-up because it is broad presentation work, not a blocker on the canonical read contract.
- [x] 6.3 Update CLI read/owner commands to display connection identity and warnings consistently without inventing alternate response shapes. `_ref connectors list/show`, `ref run timeline`, `ref grant timeline`, and `ref trace show` now render canonical `meta.warnings` to stderr without changing parseable stdout. Future CLI read commands should reuse the same helper.

## 7. Conformance and Regression Tests

- [x] 7.1 Extend public read conformance harness coverage for envelope shape, identity, strict validation, projection, expansion, filters, sort, pagination, counts, warnings, schema capabilities, and MCP mirroring. Search identity, alias validation, MCP mirroring, records identity decoration, deprecated-alias warnings, expansion rejection, graded counts (`none`/`estimated`/`exact`), canonical sort direction, search-hit `display_name` projection, `source_skipped_not_applicable` warnings, schema-capability truth (`schema-capability-truth.test.js`), and search-mode count-capability advertisement (`search-count-capability.test.js`) all have targeted coverage on both the SQLite reference path and the Postgres runtime path (env-gated on `PDPP_TEST_POSTGRES_URL`).
- [x] 7.2 Add multi-connection fixtures for records list, records detail, search, and blob read. Multi-connection lexical, semantic, and hybrid search fixtures exist (`canonical-read-envelope-conformance.test.js#multi-connection`). Records-list/detail/blob multi-connection fixtures are intentionally out of scope until the future storage fan-in change allows one grant to address multiple bindings; adding placeholder fixtures before that path exists would lie about coverage.
- [x] 7.3 Add regression tests for no silent filter/sort/expand no-ops. Unsupported params, filter operators, alias conflicts, unsupported expand targets, unsortable sort fields, `changes_since` + list-only sort/count/order combinations, canonical sort direction, advertised vs accepted range operators (`schema-capability-truth.test.js`), and search-path `count=` rejection (`search-count-capability.test.js`) are all covered. A `sort=-<cursor>` returning ASC, or a `count=exact` silently dropped, would fail these tests on the dispatched backend.
- [x] 7.4 Add token-efficiency regression checks for MCP tool output size and structuredContent availability.

## 8. Validation and Deployment

- [x] 8.1 Run targeted reference-implementation, reference-contract, MCP, dashboard, and CLI tests.
- [x] 8.2 Run relevant typechecks.
- [x] 8.3 Run `openspec validate canonicalize-public-read-contract --strict` (current branch `schema-granted-connections`).
- [x] 8.4 Run `openspec validate --all --strict` if the work touches existing active changes.
- [x] 8.5 Rebuild and restart local Docker when dashboard/API behavior changes.
