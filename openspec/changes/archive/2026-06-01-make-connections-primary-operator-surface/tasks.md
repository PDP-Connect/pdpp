## 1. Console Navigation

- [x] 1.1 Promote Connections to a top-level dashboard nav item and remove it from the Explore subnav.
- [x] 1.2 Update navigation invariants/tests so Explore, Jump, and Connections remain distinct.

## 2. Run Controls

- [x] 2.1 Update the shared primary-action classifier so browser-bound existing connections remain owner-runnable.
- [x] 2.2 Update records row and detail-page tests for ChatGPT/browser-bound run visibility and local-device non-clickable guidance.

## 3. Validation

- [x] 3.1 Run focused console tests for navigation, records rows, and connection detail behavior.
- [x] 3.2 Run `openspec validate make-connections-primary-operator-surface --strict`.
