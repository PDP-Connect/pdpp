## 1. Runtime Spec Discovery

- [ ] Inventory shipped reference-runtime behavior from code, tests, docs, and `add-polyfill-connector-system`.
- [ ] Identify which behavior is reference-specific versus owned by root PDPP Collection Profile semantics.
- [ ] Decide which pending APCS runtime behaviors are ready to graduate into canonical requirements.

## 2. Canonical Spec Delta

- [ ] Expand the `reference-implementation-runtime` spec with normative requirements and scenarios.
- [ ] Add or update tests/docs only after requirements are approved.

## Acceptance Checks

- [ ] `openspec validate add-reference-runtime-spec --strict`
- [ ] `openspec validate --all --strict`
